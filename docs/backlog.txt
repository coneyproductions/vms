* [ ] **TIM-02** False-positive Ticket Integrity / sold-out add-on classification: make the core-VMS Ticket Integrity diagnostics sold-aware for mapped add-ons so legitimately sold-out capacity-limited add-ons classify as healthy instead of red corruption warnings. Logged 2026-04-03 by explicit user reprioritization. Canonical scope authority: `wp-content/vms-dev/docs/build_specs/VMS_TIM-02_False_Positive_Ticket_Integrity_2026-04-03.md`, sourced from archived spec `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS — Ticket Integrity Monitor/vms-build-spec-false-positive-ticket-integrity-2026-04-03.md`. Initial guardrails: keep the work inside core `vms`, limit scope to integrity-report / forensics / sold-aware normalization paths, preserve real mismatch detection and repair behavior, and do not broaden this pass into public ticket UI redesign, ADD-01, or unrelated standalone-plugin work unless the diagnostics fix exposes a direct blocker.
* [ ] **ADD-01** Availability & Date Dispatch: build a core-VMS internal dispatch module that lets operators identify missing/open vendor assignments, target eligible vendors for a specific Event Plan/date, email secure single-click availability requests, capture structured YES/NO responses, write those responses back into canonical VMS availability with audit logging, and assign a responder back onto the Event Plan without inventing a second vendor/availability system. Logged 2026-04-02 by explicit user reprioritization. Canonical scope authority: `wp-content/vms-dev/docs/build_specs/VMS_ADD-01_Availability_Date_Dispatch_2026-04-02.md`, sourced from archived spec `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS - ADD — Availability & Date Dispatch/ADD Build Spec`. Initial implementation guardrails: keep the work inside core `vms`, reuse the existing module loader / vendor availability resolver / notification stack, fail safe if the module is disabled or token handling fails, and do not broaden this pass into unrelated ticketing, vendor portal redesign, or standalone plugin work unless the ADD flow proves a direct blocker.
* [ ] **TICK-ADDON-VERIFY-01** Hard-enforce verification-restricted ticket eligibility on the server side during add-to-cart, cart validation, and checkout so unverified users (including admins in preview/test flows) cannot complete protected ticket purchases just because the front end allowed selection. Logged 2026-03-20 after the add-on/cart-first recovery pass proved the ticket/add-on flow but exposed missing enforcement for restricted First Responder-style tickets. Source hardening first landed in `0.2.24.268` via `vms/includes/integrations/ticketing-rules-v2.php` and `vms/docs/build_specs/VMS_TICKETING_verified_ticket_enforcement_ui_touchups_2026-03-20.md`; follow-up in `0.2.24.269` adds the missing add-on cart-rule checkout hook plus cart/checkout CTA gating so add-on-only carts cannot continue through the normal checkout path while blockers are present. Keep open until the user confirms both restricted-ticket and add-on-only live behavior, including any express-checkout buttons that may be rendered by external payment plugins.

Tracker format (as of **2026-03-04**, America/Chicago)
Please ask for more context or explination for total clarity of intent
---

## 1) Critical Bugs / Regressions

* Canonical bug tracker moved to `wp-content/vms-dev/docs/bugs.txt`.
* Do not duplicate `BUG-*` entries in this file.
* Keep this backlog focused on committed/planned non-bug work items.
* Customer Experience Improvements priority directive (2026-02-24): prioritize customer-facing UX improvements and ticketing experience hardening until user re-prioritizes.
* Audit reset directive (2026-03-01): execution order is (1) stabilization/verification debt (`BUG-18`, `PROC-03`, deferred ticketing tests), then (2) highest-impact ticketing UX/regression gaps (`TICK-10`, `TICK-06`, `TICK-07`, then discount UX tasks), then (3) new admissions capability track (`PUB-05`).
* Additional task directive (2026-03-01): Pass Claims Forecast-First Claim-Only scope is now explicitly tracked under `PUB-05` using `DEBUGGING/vms-doc-archive/vms-build-specs/Guest List & Comp Admission/VMS_BuildSpec_PassClaims_ForecastFirst_2026-02-28.md`.
* TOP PRIORITY enhancements directive (2026-03-01): next execution order is (1) `NOTICE-01` implement `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Status Notices/vms-status-notices-build-spec.md`, then (2) return immediately to admissions Guest List/Pass Claims track `PUB-05` (`DEBUGGING/vms-doc-archive/vms-build-specs/Guest List & Comp Admission/VMS_BuildSpec_PassClaims_ForecastFirst_2026-02-28.md`).
* Thread-wrap reprioritization directive (2026-03-02): next primary implementation task is `EPCSV-01` using `DEBUGGING/vms-doc-archive/vms-build-specs/Event Plan CSV Import/VMS_BUILD_SPEC_Event_Plan_CSV_Import.md`; after `EPCSV-01`, resume remaining `PUB-05` and `NOTICE-01` follow-up work.
* New-thread kickoff directive (2026-03-02): after wrapping the current `PUB-05` door-flow polish thread, next primary implementation task is `SQUARE-01` using `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Data Tools Square Sync/VMS-BuildSpec-DataTools-Square-POS-Actuals.md` (Data Tools Square POS Actuals provider foundation). Superseded by later same-day reprioritization below.
* Thread-wrap reprioritization directive (2026-03-02, explicit user direction): next primary implementation task is `VIO-01` using `DEBUGGING/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/vms-data-tools_vendor-invite-orchestrator_build-spec_2026-03-02.md`; keep `SQUARE-01` queued as immediate Data Tools follow-up unless a higher-priority bug/regression is introduced.
* Thread-wrap reprioritization directive (2026-03-04, emergency repair): historical directive pointed to `TICK-11` using `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Ticketing/VMS-Ticket-UI-v2-Rebuild-Build-Spec-2026-03-04.md` (Ticket UI v2 rebuild/hard repair); this stream was later superseded in backlog tracking by the atomic ticketing follow-up spec `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Ticketing/VMS_BUILD_SPEC_atomic_ticket_add_to_cart_0.2.24.139.md` plus subsequent resolved ticketing follow-ups.
* Thread-wrap reprioritization directive (2026-03-04, post-emergency triage): next primary implementation task is `BUG-20` Safari entitlement parity hardening (cross-browser Ticket UI v2 reliability) using the `TICK-11` build spec guardrails and Safe Mode fallback constraints; immediate follow-up is `BUG-21` compact mobile sticky CTA sizing.
* Thread-wrap completion directive (2026-03-07): `VIO-01` Vendor Invite Orchestrator foundation is now implemented in `vms-data-tools`; with no open critical bug ahead of it in `bugs.txt`, resume `SQUARE-01` as the immediate queued Data Tools follow-up unless a higher-priority bug/regression is introduced.
* Thread-wrap completion directive (2026-03-07, `SQUARE-01` wrap): `SQUARE-01` Square POS actuals foundation is now source-implemented in `vms-data-tools`; operator-directed live Square sync / Event Plan snapshot click-through QA is explicitly deferred/backlogged for later. This queue position was later superseded by the operator-provided `VIO-02` next-phase spec below.
* Thread-wrap reprioritization directive (2026-03-07, explicit spec handoff): execute `VIO-02` using `DEBUGGING/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/Vendor Invite Orchestrator — Next Phase Spec.md`; this supersedes the earlier retention-controls-only wording for `VIO-02`. Scope is (1) Open Dates hotfix, (2) Vendor Portal Opportunities + interest submissions, and (3) admin Interest Queue + Assign action. Keep invite-log retention controls queued separately as `VIO-03` unless the user reprioritizes again.
* Thread-wrap reprioritization directive (2026-03-07, explicit user direction): next primary implementation task is `VERIFY-01` using `wp-content/plugins/vms-doc-archive/vms-build-specs/Member Verification Form/vms-verification-form-fix-build-spec.md`; this temporarily supersedes queued `VIO-03` for the next thread only. Keep `VIO-03` invite-log retention controls and the deferred live Square sync / Event Plan snapshot click-through QA backlogged unless the user reprioritizes again.
* Thread-wrap completion directive (2026-03-08, source validation): `VERIFY-01` is already implemented in `vms/includes/integrations/ticketing-verifications.php`; the My Account dashboard render path now echoes `vms_ticketing_verification_render_panel(0)` directly without an outer `wp_kses_post()` wrapper, so the plugin-rendered verification form controls are preserved. Resume `VIO-03` invite-log retention controls as the immediate queued follow-up; keep the deferred live Square sync / Event Plan snapshot click-through QA backlogged unless the user reprioritizes again.
* Thread-wrap reprioritization directive (2026-03-08, explicit user direction): with no open `BUG-*` entry ahead of it in `wp-content/plugins/vms/docs/bugs.txt`, next primary implementation task is `TICK-14` using `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Ticketing/vms-ticket-sync-repair-build-spec.md` (enabled ticket self-heal / repair). Keep `VIO-03` invite-log retention controls and the deferred live Square sync / Event Plan snapshot click-through QA backlogged unless the user reprioritizes again.
* Thread-wrap completion directive (2026-03-08, `TICK-14` wrap): `TICK-14` is now implemented in `wp-content/plugins/vms/includes/integrations/ticketing-phase-b.php`; Ticketing v2 preview now treats enabled mapped `draft`/`private`/hidden ticket products as repairable `update` actions, ticket sync hash decisions now include the enabled state, enabled commit paths explicitly restore publish/visibility, and preview repair copy is consolidated into one operator-facing sentence. Resume `VIO-03` as the next primary task. No dedicated archived `VIO-03` build spec exists yet, so the next implementation thread should obtain a focused build spec or an explicit spec-waiver before code changes. Keep the deferred live Square sync / Event Plan snapshot click-through QA backlogged unless the user reprioritizes again.
* Thread-wrap reprioritization directive (2026-03-08, explicit user direction): temporarily make next primary implementation task `TICK-15` using `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Ticketing/VMS-build-spec-ticketing-ui-polish.md` (Ticket UI layout polish + verification link + unlock scroll + Woo product clarity). This temporarily supersedes queued `VIO-03` while its focused build spec is still pending. After `TICK-15`, resume `VIO-03` invite-log retention controls unless the user reprioritizes again. Keep the deferred live Square sync / Event Plan snapshot click-through QA backlogged unless the user reprioritizes again.
* Thread-wrap completion directive (2026-03-08, `TICK-15` wrap): `TICK-15` ticketing polish is now source-implemented in core VMS across front-end ticket/add-on UI assets, ticket UI settings plumbing, and admin ticket-editor verification copy/registry plumbing. Resume `VIO-03` as the next primary task using `wp-content/plugins/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/vio-03-invite-log-retention-controls-build-spec.md`. Keep the deferred live Square sync / Event Plan snapshot click-through QA backlogged unless the user reprioritizes again.
* Thread-wrap reopening directive (2026-03-08, explicit user follow-up): `TICK-15` is STILL NOT COMPLETE. Resume `TICK-15` as the next primary task using `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Ticketing/VMS-build-spec-ticketing-ui-polish.md` before starting `VIO-03`. Remaining blockers called out by operator review: the `Tickets` heading is still too close to the preceding TEC details/cost block, spacing between the final entitlement card and the subtotal block is still inadequate, mobile entitlement rows with an image plus longer title/description are still collapsing the text column so aggressively that content wraps into a one-word-per-line stack while price/qty crowd the title area, and `wp-content/plugins/vms/assets/css/vms-ticketing-front.css` now shows stacked selector duplication (`single-tribe_events` appears 125+ times) that should be consolidated instead of continuing to layer edits. After `TICK-15` is truly closed, resume `VIO-03` using `wp-content/plugins/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/vio-03-invite-log-retention-controls-build-spec.md` unless the user reprioritizes again.
* Thread-wrap emergency reprioritization directive (2026-03-08, explicit user direction): next primary implementation task is `DISC-01` using `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Commerce Discounts/vms-build-spec-vms-commerce-discounts-square-compatible-cart-adjustments.md`. This emergency fix supersedes reopened `TICK-15`, queued `VIO-03`, and other non-bug backlog work for the next thread. After `DISC-01`, resume `TICK-15`, then `VIO-03`, unless the user reprioritizes again.
* Thread-wrap reprioritization directive (2026-03-13, explicit user direction): next primary task is `BUG-25` using `wp-content/plugins/vms/docs/bugs.txt` as the scope authority (newly created Event Plan tickets/entitlements not saving with stale Preview warning). Keep `BUG-23` queued behind it, and keep `DISC-01` explicitly paused unless the user reopens it; do not automatically resume non-bug backlog work after `BUG-25`.
* Thread-wrap reprioritization directive (2026-03-13, post-`BUG-25` closure): next primary task is `BUG-23` using `wp-content/plugins/vms/docs/bugs.txt` as the scope authority. `BUG-25` is now closed as a client-side Ticketing v2 stale-preview comparison regression; keep `DISC-01` explicitly paused unless the user reopens it, and do not automatically resume non-bug backlog work ahead of `BUG-23`.
* Thread-wrap completion directive (2026-03-13, `BUG-23` wrap): `BUG-23` is now closed in `vms-ops-console-premium/includes/venues.php` and `vms-ops-console-premium/includes/rest/routes.php`; Ops runtime venue selection now treats only published non-template venues as active/selectable, which removed the redundant selector in a true one-published-venue environment while preserving the broader fallback path for no-published-venue recovery. Temporary local validation used a reversible `Test Venue` publish -> draft -> publish status toggle to prove the pre-fix payload bug and the post-fix hidden-selector behavior. No automatic next task is assigned from this wrap because `DISC-01` remains explicitly paused unless the user reopens it.
* Thread-wrap completion directive (2026-03-13, `TICK-15` closeout): reopened ticket UI layout blockers are now resolved in core VMS across ticket CSS source modules, regenerated front CSS, entitlement mobile layout, and a new `tests/e2e/tick15-ticket-ui-layout.spec.ts` fixture. Resume `VIO-03` as the next primary task using `wp-content/plugins/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/vio-03-invite-log-retention-controls-build-spec.md`; keep `DISC-01` paused unless the user explicitly reopens it, and keep logged `UX-21` / `VEND-14` specs queued without automatic reprioritization.
* Thread-wrap completion directive (2026-03-13, `VIO-03` wrap): `VIO-03` invite-log retention controls are now implemented in `vms-data-tools` across the VIO DB layer, a new `includes/vendor-invites/retention.php` service, Invite Logs admin UI/tour/CSS updates, and a new Chromium regression spec `vms/tests/e2e/vio03-invite-log-retention.spec.ts`. Invite Logs now defaults to `Active`, exposes preview -> commit `Archive` / `Restore` / `Purge Eligible Archived Logs` controls, writes retention audit snapshots before purge, and no longer offers direct failed-log deletion. Verification on 2026-03-13: `php -l` PASS on touched Data Tools PHP files, Chromium Playwright PASS for `tests/e2e/vio03-invite-log-retention.spec.ts` plus `tests/e2e/smoke-admin.spec.ts`, and direct local DB probe confirmed the new invite-log retention columns and `wp_vms_vendor_invite_retention_audit` table. No automatic next task is assigned from this wrap because `DISC-01` remains explicitly paused and no later thread-wrap directive names the next primary implementation task; keep the deferred live Square sync / Event Plan snapshot click-through QA and logged `UX-21` / `VEND-14` specs queued unless the user reprioritizes.
* Validation update (2026-03-13, explicit user report): `DISC-01` VMS Commerce Discounts Square-compatible cart adjustments are working properly on the live site. Current source in `vms-commerce-discounts` already matches the Square-safe item-adjustment/native-discount architecture (`includes/class-vms-discounts-cart.php`, `includes/class-vms-discounts-square-bridge.php`, `includes/class-vms-discounts-order.php`, `vms-build.txt` `0.2.1`), so close `DISC-01` as live-validated and do not reopen it unless a new discount/Square regression is reported.
* Thread-wrap reprioritization directive (2026-03-13, explicit user direction): next primary implementation task is `OPS-06` using `wp-content/plugins/vms-doc-archive/vms-build-specs/Ops Console Private Club/vms-ops-console-premium-0.1.33-build-spec-id-age-403.md`. This user-directed Ops Console hardening pass supersedes raw backlog-order candidates `SCHED-04`, `CSS-01`, `CSS-02`, and the logged `VEND-14` follow-up for the next thread. After `OPS-06`, re-scan `bugs.txt` + `backlog.txt` before committing another Section 12 task.
* Thread-wrap completion directive (2026-03-14, `OPS-06` validation wrap): `OPS-06` is now source-validated, Chromium fixture-covered, and Local loopback runtime-validated for authenticated shell load, bootstrap/presence success, manual-member create, adult scan, under-21 scan, not-found prefill, and create-then-rescan recovery. Keep `OPS-06` open only for real operator/device validation of camera scan UX, scan-only-user gating, and stale-session messaging in the browser UI; local WebKit rerun remains blocked by a missing Playwright WebKit binary in this environment.
* New-thread kickoff directive (2026-03-14, explicit user direction): use the canonical Next Thread Kickoff Summary protocol from `wp-content/plugins/vms/docs/VMS_MASTER_HANDOFF.txt` / `wp-content/plugins/vms/docs/Thread Wrap Protocol.txt`, explicitly telling the next thread to read the handoff plus canonical checklist/backlog/bugs before starting. SECTION 12 next primary implementation task is `TICK-16` ticket template sales-end failsafe using the logged operator requirement in this backlog. After `TICK-16`, take up `UX-22` Event Plans list date visibility/sortability unless the user reprioritizes.
* Thread-wrap completion directive (2026-03-14, `TICK-16` wrap): `TICK-16` is now implemented in core VMS `0.2.24.188` across the ticket template apply path, Ticketing v2 admin UI, Event Plan ticketing markup, and a new Chromium regression `tests/e2e/tick16-ticket-template-sales-end-failsafe.spec.ts`. Saved templates now pause behind an explicit confirmation when any ticket `Sales end` is before the current show date/time, operators can choose `Apply template as saved` or `Apply template and reset Sales end`, stale current-config dates surface an always-visible warning panel with a one-click reset path, and default-template auto-apply no longer silently carries older Sales end values forward. Bug + Backlog Priority Scan Result for the next task: no open `BUG-*` items remain, and the top open non-bug candidate is now `UX-22`; assign `UX-22` as the next primary task using this backlog entry as the current scope authority unless the user reprioritizes.
* Thread-wrap completion directive (2026-03-14, `UX-22` wrap): `UX-22` is now implemented in core VMS `0.2.24.189` in `vms/includes/cpt/event-plans.php`; Event Plans admin list now shows a visible sortable `Event Date` column rendered from canonical Event Plan date/start-time meta, and sorting by that header orders plans by show chronology instead of post creation date. Verification on 2026-03-14: `php -l includes/cpt/event-plans.php` PASS, and a temporary Chromium Playwright live-admin probe PASS confirmed the `Event Date` header renders and `orderby=vms_event_date` sorts visible rows correctly in both ascending and descending order. Bug + Backlog Priority Scan Result for the next task: no open `BUG-*` items remain, and the top open non-bug backlog candidate is now `SCHED-04`, followed by `CSS-01`; assign `SCHED-04` as the next primary task using this backlog entry as the current scope authority unless the user reprioritizes.
* Thread-wrap completion directive (2026-03-14, `SCHED-04` wrap): `SCHED-04` is now implemented in core VMS `0.2.24.190` in `vms/includes/admin/settings-page.php`; Calendar settings vendor-type rows now replace industry-specific Schedule labels (`Band`, `Food Truck`, and talent-like aliases) with neutral role labels (`Primary Vendor`, `Secondary Vendor`) while preserving the stored vendor-type slugs, saved settings maps, and existing Calendar/List behavior. Verification on 2026-03-14: `php -l includes/admin/settings-page.php` PASS, `php -l includes/core/registry/constants.php` PASS, `php -l vendor-management-system.php` PASS, and grep verification confirmed the neutral labels plus version markers are wired into the canonical settings screen/build files. Bug + Backlog Priority Scan Result for the next task: no open `BUG-*` items remain, and the top open non-bug backlog candidate is now `CSS-01`, followed by `CSS-02`; assign `CSS-01` as the next primary task using this backlog entry as the current scope authority unless the user reprioritizes.
* Thread-wrap completion directive (2026-03-15, `CSS-01` wrap): `CSS-01` is now implemented in core VMS `0.2.24.191` across `vms/includes/helpers.php`, `vms/includes/docs-public.php`, and `vms/includes/public/vendor-profiles.php`; canonical asset helper functions now centralize VMS CSS URL/version resolution, legacy fallback admin/public enqueue paths reuse the same style-stack helpers, and the docs/vendor-profile public routes now explicitly enqueue `vms-shared` + `vms-ui` before their scoped styles so base styling no longer drops on those pages. Verification on 2026-03-15: `php -l` PASS for the three touched PHP files plus canonical version markers (`vendor-management-system.php`, `includes/core/registry/constants.php`), and grep/source verification confirmed the scoped loaders now use the shared enqueue helpers. Bug + Backlog Priority Scan Result for the next task: no open `BUG-*` items remain, and the top open non-bug backlog candidate is now `CSS-02`; despite that raw order, assign `OPS-08` as the next primary task because the user explicitly reprioritized the next thread to the supplied Ops Console build spec.
* Thread-wrap reprioritization directive (2026-03-15, explicit user direction): next primary implementation task is `OPS-08` using `wp-content/plugins/vms-doc-archive/vms-build-specs/Ops Console Private Club/vms-ops-build-spec-0.1.36-scan-access-buffer.md`. This user-directed Ops Console pass supersedes backlog-order candidate `CSS-02` and logged `OPS-07` for the next thread. After `OPS-08`, re-scan `bugs.txt` + `backlog.txt` before committing another Section 12 task.
* Thread-wrap completion directive (2026-03-15, `OPS-08` wrap): `OPS-08` is now implemented in `vms-ops-console-premium` `0.1.36` across WordPress-time helpers, ticket event/check-in buffer logic, Teams setup-status modeling/UI, WP Users Ops setup visibility, REST permission diagnostics, guided Teams/Settings tours, PWA setup-warning/event-label runtime behavior, and build metadata/package output. Verification on 2026-03-15: `php -l` PASS on touched Ops Console PHP files, `node --check` PASS on `admin/assets/admin.js` and `pwa/assets/js/app.js`, Chromium Playwright PASS for `plugins/vms/tests/e2e/ops08-scan-buffer-setup-warnings.spec.ts`, and release package built as `wp-content/plugins/vms-ops-console-premium-0.1.36.zip`; local WebKit coverage remains blocked by a missing Playwright WebKit binary in this environment.
* Thread-wrap reprioritization directive (2026-03-15, explicit user direction): next primary implementation task is `OPS-09` using `wp-content/plugins/vms-doc-archive/vms-build-specs/Ops Console Private Club/vms-ops-build-spec-0.1.37-verified-member-lookup.md`. This user-directed Ops Console follow-up supersedes raw backlog-order candidate `CSS-02` and keeps deferred `OPS-10` per-event buffer override work logged but out-of-scope unless the user reprioritizes again.
* Thread-wrap completion directive (2026-03-15, `OPS-09` wrap): `OPS-09` is now implemented in `vms-ops-console-premium` `0.1.37` across the capability registry, member lookup/member data helpers, REST routes, admin/PWA lookup surfaces, and focused Chromium coverage (`plugins/vms/tests/e2e/ops09-verified-member-lookup.spec.ts`). Verification on 2026-03-15: `php -l` PASS on touched Ops Console PHP files, `node --check` PASS on touched JS/spec files, Chromium Playwright PASS for the `OPS-09` spec, and package built as `wp-content/plugins/vms-ops-console-premium-0.1.37.zip`; local WebKit rerun remains blocked by a missing Playwright WebKit binary. Bug + Backlog Priority Scan Result for the next task: no open `BUG-*` items remain, and the top open non-bug backlog candidate is still `CSS-02`; despite that raw order, assign `COMP-01` as the next primary task because the user explicitly reprioritized the next thread to the supplied attendance-bonus compensation build spec.
* Thread-wrap reprioritization directive (2026-03-15, explicit user direction): next primary implementation task is `COMP-01` using `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Vendor Compensation/VMS_BUILD_SPEC_ATTENDANCE_BONUS_COMPENSATION_2026-03-15.md`. This user-directed core VMS compensation pass supersedes raw backlog-order candidate `CSS-02` and keeps logged `OPS-07`, `OPS-10`, and `VEND-14` backlogged unless the user reprioritizes again.
* Thread-wrap pause directive (2026-03-23, explicit user direction): pause further ticketing work after the current local `TICK-17` publish-gate pass. Keep `TICK-17` open with the remaining CSV/UI backlog items logged for later, and keep newly logged `BUG-26` inventory semantics work deferred out of the next thread unless the user explicitly reprioritizes ticketing again.
* Thread-wrap reprioritization directive (2026-03-23, explicit user direction): despite open ticketing items `BUG-26`, `TICK-17`, `TICK-ADDON-VERIFY-01`, and `TICK-VERIFY-STOREAPI-01`, next primary implementation task is `CSS-02`. Because no canonical `CSS-02` build spec currently exists in `wp-content/plugins/vms/docs/build_specs/`, the next implementation thread must first obtain a focused build spec or an explicit scope waiver before code changes.
* Thread-wrap reprioritization directive (2026-03-29, explicit user direction): next high-priority implementation task is `TICK-18` Event Plan Ticketing v2 admin workflow polish in core `vms` (drag/drop ordering for tickets + add-ons, default-collapsed detail bodies for at-a-glance scanning, and per-ticket image parity with add-ons). This supersedes the 2026-03-24 handoff assignment to the archived ticket-sales-resolver build spec and partially reopens ticketing work after the 2026-03-23 pause, but only for this Event Plan admin workflow stream. Scope authority is `wp-content/plugins/vms/docs/build_specs/VMS_TICK-18_Event_Plan_Ticketing_v2_admin_workflow_polish_2026-03-29.md`. Keep `BUG-26`, `BUG-27`, `TICK-17`, `TICK-ADDON-VERIFY-01`, and `TICK-VERIFY-STOREAPI-01` open unless the user explicitly reprioritizes them again.
* Thread-wrap reprioritization directive (2026-03-31, explicit user direction): despite open ticketing items `BUG-26`, `BUG-27`, `TICK-17`, `TICK-ADDON-VERIFY-01`, and `TICK-VERIFY-STOREAPI-01`, the next high-priority implementation task is now `MA-07` Meta Ads Builder stabilization in standalone plugin `vms-meta-ads`, starting with `BUG-28` template save/name persistence and then continuing through operator-reported builder/admin regressions inside that plugin only. Scope authority is `wp-content/plugins/vms/docs/build_specs/VMS_MA-07_Meta_Ads_Builder_stabilization_2026-03-31.md`. Keep the separate all-VMS admin notice-placement request logged as backlog only unless the user explicitly reprioritizes it into active implementation.
* Thread-wrap reprioritization directive (2026-04-01, explicit user direction): despite open ticketing items `BUG-26`, `BUG-27`, `TICK-17`, `TICK-18`, `TICK-ADDON-VERIFY-01`, and `TICK-VERIFY-STOREAPI-01`, and despite the prior handoff candidate `VEND-15`, the next high-priority implementation task is now `TIM-01` Ticket Integrity Monitor in core `vms`. Scope authority is `wp-content/plugins/vms/docs/build_specs/VMS_TIM-01_Ticket_Integrity_Monitor_2026-04-01.md`, which points to the user-supplied archived spec `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Build Spec — Ticket Integrity Monitor/vms-ticket-integrity-monitor-build-spec.md`. Keep `VEND-15` backlogged and do not reopen unrelated vendor-portal, Meta Ads, or ticket purchase UI work unless the user explicitly reprioritizes it or the monitor work exposes a hard blocker.
* Thread-wrap completion directive (2026-04-02, post-`TIM-01` / `BUG-26` / `BUG-27` wrap): `TIM-01` is now resolved, `BUG-26` and `BUG-27` are now closed, and no open `BUG-*` entries remain in `vms/docs/bugs.txt`. After the bug/backlog rescan, the next primary implementation task is `TICK-ADDON-VERIFY-01` using `wp-content/plugins/vms/docs/build_specs/VMS_TICKETING_verified_ticket_enforcement_ui_touchups_2026-03-20.md`. Keep `TICK-17`, `TICK-18`, `TICK-VERIFY-STOREAPI-01`, and `VEND-15` open/backlogged unless the user explicitly reprioritizes them.
* Thread-wrap reprioritization directive (2026-04-02, explicit user direction): despite the current top open purchase-integrity follow-up `TICK-ADDON-VERIFY-01`, the next primary implementation task is now `ADD-01` Availability & Date Dispatch in core `vms`. Scope authority is `wp-content/vms-dev/docs/build_specs/VMS_ADD-01_Availability_Date_Dispatch_2026-04-02.md`, which promotes the archived spec `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS - ADD — Availability & Date Dispatch/ADD Build Spec` into the canonical kickoff set. Keep `TICK-ADDON-VERIFY-01`, `TICK-17`, `TICK-18`, `TICK-VERIFY-STOREAPI-01`, `VEND-14`, and `VEND-15` open/backlogged unless the user explicitly reprioritizes them again.
* Thread-wrap reprioritization directive (2026-04-03, explicit user direction): despite no open `BUG-*` entries in `wp-content/vms-dev/docs/bugs.txt` and despite the current raw top non-bug backlog candidate `ADD-01`, the next primary implementation task is now `TIM-02` False-positive Ticket Integrity / sold-out add-on classification in core `vms`. Scope authority is `wp-content/vms-dev/docs/build_specs/VMS_TIM-02_False_Positive_Ticket_Integrity_2026-04-03.md`, which promotes the archived spec `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS — Ticket Integrity Monitor/vms-build-spec-false-positive-ticket-integrity-2026-04-03.md` into the canonical kickoff set. Keep `ADD-01`, `TICK-ADDON-VERIFY-01`, `TICK-17`, `TICK-VERIFY-STOREAPI-01`, `VEND-14`, and `VEND-15` open/backlogged unless the user explicitly reprioritizes them again.

---

## 2) Ticketing / Cart Logic / Entitlements

* [x] **TICK-01** Cart validation: allow addons that require tickets even if tickets were added earlier in session — deterministic hardening shipped across `vms/includes/integrations/ticketing-rules-v2.php`, `vms/assets/vms-ticketing-front.js`, and `vms/includes/core/cli/stale-check.php`; canonical validation on 2026-02-16: `wp vms stale-check --bugs=TICK-01` => PASS=1/WARN=0/MANUAL=0 with runtime fixture passing (`same-session GA hint applies when cart GA is absent and is cleared once cart GA is observed`). Manual live cart sanity test (GA then required add-ons in same session) deferred/backlogged for follow-up.
* [x] **TICK-02 (TEST)** Re-test entitlement auto-add across fresh + cached sessions for determinism — live-site retest resumed on 2026-03-07 after ticket-enabled fixture `unwound` became available; anonymous/private sessions exposed a front-end regression where classic public pages incorrectly entered Safe Mode and hid reserved add-ons. First follow-up restored visibility, and second follow-up repaired classic-mode mounting so reserved add-ons render back inside the unified ticket window; both landed in `vms/assets/vms-ticketing-front.js` with fixture regression coverage in `vms/tests/e2e/bug09-ticket-ui-entitlement-stepper.spec.ts`. Operator-verified closed on 2026-03-07.
* [x] **TICK-03 (TEST)** Confirm ticket template behavior does not clone into unrelated event plans unless intended — deferred until live-site testing phase, then operator-verified closed on 2026-03-07 after template apply/save and Preview -> Commit on one Event Plan left unrelated plans and products unchanged.

* [x] **TICK-04** Entitlements UI: full-width layout + richer descriptions with “More info” expand/collapse — shipped in `0.2.24.35` across admin/public Ticketing v2 (`vms/includes/integrations/ticketing-phase-b.php`, `vms/includes/integrations/ticketing-rules-v2.php`, `vms/assets/admin-ticketing.js`, `vms/assets/css/vms-admin.css`, `vms/assets/css/vms-ticketing-front.css`, `vms/assets/css/vms-entitlements-public.css`).
* [x] **TICK-05** Ticket modal summary: include entitlements in subtotal near “Get Tickets”; increase legibility of Quantity/Total/buttons (older-audience friendly, not oversized) — shipped in `0.2.24.35` (`vms/assets/vms-ticketing-front.js` and `vms/assets/css/vms-ticketing-front.css`). Path hygiene follow-up on 2026-03-01 converted legacy `vms/assets/js/vms-ticketing-front.js` into a compatibility shim that forwards to the canonical bundle.
* [x] **TICK-06** RSVP panel width/spacing parity: make RSVP window as wide as ticket window and add spacing between RSVP and ticket panels — shipped via `vms/assets/css/vms-ticketing-front.css` in the `0.2.24.149` follow-up thread; desktop RSVP card now expands to the same widened breakpoint as the ticket card, and the paid ticket form gets extra top spacing when it follows an RSVP panel.
* [x] **TICK-07** Entitlements UI polish: selected-row highlight needs more visible surround/padding around image area in live ticket context — reopened on 2026-03-07 after live follow-up showed no-image rows were still collapsing into the image-first grid; follow-up patch added explicit no-image row layout plus stronger compact-row inset in `vms/assets/css/vms-entitlements-public.css` and `vms/includes/integrations/ticketing-rules-v2.php`, then operator-verified closed on 2026-03-07.
* [x] **TICK-08** Ticket discount input UI flow beautification: reorganize fields into a clear step-by-step form with predictable grouping/order (instead of scattered/random placement), stronger visual hierarchy, and plain-language helper text so first-time operators can complete setup without technical interpretation — shipped in `vms-commerce-discounts` `0.1.3+` across `includes/class-vms-discounts-ui.php`, `assets/admin/discounts-admin.css`, and `assets/admin/discounts-admin.js`; rule cards now use numbered steps, stronger hierarchy, conditional inputs, and live summary/status feedback. Operator follow-up on 2026-03-07: overall redesign accepted, but desktop field alignment remains slightly uneven in some steps; treat as residual polish, not current blocking work.
* [x] **TICK-09** Ticket discount product targeting UX: replace manual `Specific Product IDs (CSV)` entry with a searchable multi-select product picker (name/SKU visibility, add/remove controls, bulk-friendly selection) while keeping raw JSON available only in collapsed `Advanced` mode — shipped in `vms-commerce-discounts` `0.1.3` across the same UI/admin assets plus AJAX search wiring in `includes/class-vms-discounts-loader.php`, `includes/class-vms-discounts-admin.php`, and `includes/class-vms-discounts-settings.php`; the main form now uses searchable product add/remove controls while preserving hidden CSV compatibility for saves/import/imported rules.
* [x] **TICK-10** Entitlement add-to-cart regression: when qualifying tickets are already in the cart, customers must be able to add eligible entitlements in that same cart/session without being forced to add tickets again — operator-verified closed on 2026-03-07: same-session add-on-only follow-up passed with qualifying tickets already in cart, and standard entitlement caps/limits remained intact.
* [x] **TICK-11** Emergency Ticket UI v2 rebuild: superseded/retired in backlog tracking on 2026-03-07. The original rebuild stream from `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Ticketing/VMS-Ticket-UI-v2-Rebuild-Build-Spec-2026-03-04.md` was overtaken by `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Ticketing/VMS_BUILD_SPEC_atomic_ticket_add_to_cart_0.2.24.139.md` and the subsequent resolved ticketing follow-ups (`BUG-20`, `BUG-21`, `TICK-12`), so no separate open `TICK-11` execution remains.
* [x] **TICK-12** Ticket UI v2 mobile sticky action simplification: keep sticky behavior but render a compact button-first bar (no itemized list) so mobile viewport remains usable; paired with bug fix stream `BUG-21` and confirmed resolved by user follow-up (2026-03-06).
* [x] **TICK-13** Ticket default sales-window behavior: when creating tickets in an Event Plan, default `Sales start` to current time (`now`) and default `Sales end` to the Event Plan show date/time so operators do not need to hand-enter standard window values each time — shipped in the `0.2.24.149` follow-up across `vms/includes/integrations/ticketing-phase-b.php` and `vms/includes/cpt/event-plans.php`; operator-verified closed on 2026-03-07 after confirming the autofill is working.
* [x] **TICK-14** Enabled ticket self-heal / sync repair — implemented on 2026-03-08 in `vms/includes/integrations/ticketing-phase-b.php`: Ticketing v2 now includes `enabled` in per-ticket sync hashes, Preview treats enabled mapped `draft`/`private`/hidden ticket products as repairable `update` actions instead of `skip`, enabled `create`/`adopt`/`update` commit paths republish and restore visibility on mapped Woo ticket products, and preview repair notes now collapse into one operator-facing sentence. Verification on 2026-03-08: `php -l wp-content/plugins/vms/includes/integrations/ticketing-phase-b.php` PASS and `bash tests/bin/run-smoke.sh` PASS (`tests/e2e/smoke-admin.spec.ts`). Manual broken-plan Preview -> Commit click-through remains deferred.
* [x] **DISC-01** VMS Commerce Discounts Square-compatible cart adjustments — implemented in active plugin `vms-commerce-discounts` and live-validated on 2026-03-13 by explicit user report. Current source uses item-level cart adjustments plus a Square-native bridge instead of negative Woo fees (`vms-commerce-discounts/includes/class-vms-discounts-cart.php`, `vms-commerce-discounts/includes/class-vms-discounts-square-bridge.php`, `vms-commerce-discounts/includes/class-vms-discounts-order.php`), and plugin build note `vms-commerce-discounts/vms-build.txt` `Version: 0.2.1` records the Square checkout seam/native-discount hardening. Do not reopen unless a new Square/discount checkout regression is reproduced.
* [x] **TICK-15** Ticket UI layout polish + verification link + unlock scroll + Woo product clarity — initial 2026-03-08 implementation already landed the verification link, unlock scroll/highlight, ticket availability toggle, dynamic verification-program sourcing, and clean Woo product title/event columns across `vms/assets/vms-ticketing-front.js`, `vms/includes/admin/settings-page.php`, `vms/includes/integrations/ticketing-rules-v2.php`, `vms/includes/integrations/ticketing.php`, `vms/assets/admin-ticketing.js`, `vms/includes/core/registry/vms-keys-map.php`, `vms/includes/integrations/ticketing-phase-b.php`, and `vms/includes/admin/woo-product-event-columns.php`. Final reopened-blocker closeout on 2026-03-13 updated ticket CSS source modules `vms/assets/css/ticketing-front/20-footer-cta.css`, `vms/assets/css/ticketing-front/30-ticket-polish.css`, and `vms/assets/css/ticketing-front/70-ticket-ui-v2.css`, regenerated `vms/assets/css/vms-ticketing-front.css`, and refined `vms/assets/css/vms-entitlements-public.css` so the Tickets section reads as a separate block, subtotal/CTA spacing is restored, touched live-ticket selectors are consolidated with `:is(.single-tribe_events, .tribe-events-single)` instead of layering more duplicate overrides, ticket right stacks stay right-aligned, and mobile image-row entitlements move price/qty onto a right-aligned second row instead of crushing copy. Added regression coverage: `vms/tests/e2e/tick15-ticket-ui-layout.spec.ts`. Verification on 2026-03-13: `npm run build:css:ticketing-front` PASS, Chromium Playwright PASS for `tests/e2e/tick15-ticket-ui-layout.spec.ts`, Chromium Playwright PASS for `tests/e2e/bug15-ticket-addon-safari-handoff.spec.ts`, and admin smoke PASS (`tests/e2e/smoke-admin.spec.ts`). Playwright WebKit could not run in this workspace because the WebKit browser executable is not installed locally; manual iPhone/Safari spot-check remains advisable, but the reopened source/layout blockers are now closed.
* [x] **TICK-16** Ticket template sales-end failsafe: completed in core VMS `0.2.24.188` on 2026-03-14 across `vms/includes/integrations/ticketing-phase-b.php`, `vms/assets/admin-ticketing.js`, `vms/includes/cpt/event-plans.php`, `vms/assets/css/vms-admin.css`, and `vms/tests/e2e/tick16-ticket-template-sales-end-failsafe.spec.ts`. Template options now carry Sales-end guardrail metadata, manual/default template apply pauses for explicit confirmation when any ticket `Sales end` is before the current show date/time, operators can apply as-saved or apply-and-reset stale Sales end values atomically, and loaded Ticketing configs now surface a warning + one-click reset path when stale Sales end values remain before the show date/time. Verification on 2026-03-14: `php -l` PASS for touched PHP files, `node --check assets/admin-ticketing.js` PASS, and Chromium Playwright PASS for `tests/e2e/tick16-ticket-template-sales-end-failsafe.spec.ts`.
* [ ] **TICK-17** Ticket Purchase UI publish-gate repair: use `wp-content/plugins/vms/docs/build_specs/VMS_TICKETING_ticket_purchase_ui_publish_gate_2026-03-22.md` to convert the operator CSV into canonical implementation authority and repair the customer ticket purchase surface across ticket blocks, qualified-ticket states, email verification entry, add-on layout/threshold behavior, subtotal separation, sticky CTA behavior, and the Chrome/Safari desktop/mobile matrix so the next plugin zip can be uploaded without breaking live event purchase flows. Partial source pass through `0.2.24.280` repaired the live server-controls mount, qualified-ticket shell/verification flow, add-on placement and gating, subtotal/CTA rail split, and most local publish-blocking UI behavior, but the user explicitly paused further ticketing work on 2026-03-23 after the local test pass. Deferred remainder now includes the still-off amount alignment called out by the user (subtotal values, ticket prices, and add-on prices not yet lining up cleanly with their intended rails/steppers), distinct visual treatment between ticket cards and add-on cards, rendering public ticket titles from the operator-entered Event Plan ticket field only (no prepended date/time in the customer-facing title), and any remaining unchecked CSV acceptance items that still need true mobile/live-test validation before `TICK-17` can close honestly.
* [x] **TICK-18** Event Plan Ticketing v2 admin workflow polish: make Ticketing v2 ticket rows and add-on / entitlement rows drag/drop sortable in the Event Plan editor, collapse row details by default so the section reads as an at-a-glance list until a row is being edited, and add per-ticket image controls for every ticket type so tickets can use explicit artwork like add-ons instead of only falling back to the Event Plan featured image. Logged 2026-03-29 by explicit user direction as the next high-priority task. Scope authority: `wp-content/plugins/vms/docs/build_specs/VMS_TICK-18_Event_Plan_Ticketing_v2_admin_workflow_polish_2026-03-29.md`. Scope guardrails: keep this inside core `vms` Event Plan Ticketing v2 admin/editor flow; preserve existing Preview -> Commit semantics, save/sync safety, and live purchase behavior unless this scope explicitly requires a deterministic order/image update; reuse the existing add-on image picker / storage pattern and current GA image-policy concepts wherever viable instead of inventing a second image workflow. Ordering must persist through saved config and render deterministically anywhere Ticketing v2 consumes the Event Plan ticket/add-on config.
* [x] **TIM-01** Ticket Integrity Monitor: implemented and expanded through the follow-up TIM passes and Woo inventory root-cause audit work in core `vms` (`0.2.24.360` through `0.2.24.365`) across the monitor UI, scan runner, mutation audit, repair runner, inventory forensics, and Woo verification logic. Explicit user follow-up now treats the original `TIM-01` task as resolved; reopen only if a new Ticket Integrity regression/spec is logged.
* [x] **VERIFY-01** Member Verification Form My Account rendering fix — source-validated on 2026-03-08 in `vms/includes/integrations/ticketing-verifications.php`: the My Account dashboard path echoes `vms_ticketing_verification_render_panel(0)` directly with no outer `wp_kses_post()` wrapper, so the plugin-rendered `<form>`, `<select>`, file-upload input, checkbox, and hidden fields render correctly on My Account. Renderer internals, field names, nonce/upload handling, and admin review flow remain unchanged.

---

## 3) Event Plan Editor UX (Mobile-first) + Compensation UI

* [x] **UX-01** Compensation tiles: show every available option (Venue default for day, Vendor default, Holiday default, etc) — complete and verified on 2026-02-16 (source pass: defaults + packages + context refresh hooks confirmed)
* [x] **UX-02** Separate “known defaults” (Venue/Vendor/Holiday) across top; “variable” options below — complete and verified on 2026-02-16 (source pass: distinct Defaults and Packages rows confirmed)
* [x] **UX-03** Reduce copy/wordiness; make choices glanceable (less text, clearer hierarchy) — complete and verified on 2026-02-16 (source pass: concise instructions/subtitles confirmed)
* [x] **UX-04** Always show Holiday tile (disabled when none) to prevent layout jump — complete and verified on 2026-02-16 (source pass: holiday tile always rendered + disabled state confirmed)
* [x] **UX-06** Event Plan title behavior: remove/stop auto-adding date to titles — complete and verified on 2026-02-16 (source pass: auto-title JS/save/Ready/fallback now use band-only title generation while preserving custom operator-entered titles)
* [x] **UX-07** Event Plan editor: remove redundant horizontal divider lines where they no longer add structure
* [x] **UX-08** Event Plan editor: remove “Media” section from the Event Plan Details form
* [x] **UX-09** Event Plan editor: move Secondary Vendors directly below Primary Vendor and above Staff
* [x] **UX-10** Compensation acknowledgments: group all warning windows together below “Lock Draft Pay for This Event”
* [x] **UX-11** Continue improving Event Plan editor into a denser horizontal/dashboard layout (reduce wasted single-column space)
* [x] **UX-12** Remove numeric input spinner controls (up/down steppers) across VMS admin/public forms to prevent accidental value changes from cursor/wheel interactions
* [x] **UX-13** Compensation tiles/cards: add deterministic color scale from lowest-to-highest (or highest-to-lowest) pay to improve at-a-glance comparison — complete and operator-verified on 2026-02-16 after follow-up tuning; scale now applies deterministically across both compensation option tiles and primary compensation structure tiles, with midpoint band differentiation (e.g., $300 no longer shares the same top color band as $500 when higher guarantees exist).
* [x] **UX-14** Event Plan editor: make Cancellation section collapsible and default-collapsed
* [x] **UX-15** Event Plan editor: move Cancellation section directly above Status; show current Status directly above workflow action buttons
* [x] **UX-16** Event Plan editor: style “Mark Cancelled” as a clear danger action with explicit confirmation prompt
* [x] **UX-17** Event Plan editor: shift Start/End controls to TEC-style dropdown time picker presentation for cleaner, unified date/time selection
* [x] **UX-18** Event Plan editor: fix collapsed “Changed” false positives for Cancellation + Secondary Vendors when fields remain at default empty selections
* [x] **UX-19** Event Plan form structure hardening: keep legacy/import ticketing integration as its own collapsible section, move Ticketing/Ticketing v2 into its own top-level section above Advanced Controls, enforce normal-column order above Guest List / Discounts / Advanced Controls, remove redundant nested headings/collapse chrome, retire the duplicate `Agenda / Event Description` field in favor of canonical post content, and fix false-positive `Changed` badges on untouched Secondary Vendors — operator-verified closed on 2026-03-07.
* [x] **UX-20** Event Plan metabox order persistence: fixed 2026-03-08 by removing the Event Plan load-time DOM reshuffle that re-appended metaboxes into a scripted order and by seeding a first-load default metabox order through the WordPress `meta-box-order_vms_event_plan` path so users can drag/reorder freely and keep their saved layout across refresh.
* [x] **UX-21** Event Plan lock-pay first-save safety — completed 2026-03-13 in core VMS per `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Event Plans/VMS_BUILD_SPEC_Event_Plan_Lock_Pay_Save_Safety_2026-03-13.md`: `Lock Draft Pay for This Event` is now non-destructive for Event Plan basics because `_vms_event_date`, `_vms_start_time`, `_vms_end_time`, and `_vms_venue_id` only write when explicitly posted, the compensation area disables lock-pay until saved basics exist and adds a nearby `Save Basic Details` action/helper, server-side lock attempts are blocked with a clear required-basics notice (while preserving entered values on first save), the Event Plan editor tour now explains save-first/pay-lock sequencing, and focused Chromium coverage landed in `vms/tests/e2e/ux21-lock-pay-first-save-safety.spec.ts`. Verification on 2026-03-13: `php -l` PASS for touched core files, escalated Chromium Playwright PASS for `tests/e2e/ux21-lock-pay-first-save-safety.spec.ts`, and `tests/e2e/smoke-admin.spec.ts` PASS.
* [x] **UX-22** Event Plans list date visibility/sortability — completed in core VMS `0.2.24.189` on 2026-03-14 in `vms/includes/cpt/event-plans.php`: Event Plans admin list now renders a visible `Event Date` column from canonical `_vms_event_date` plus start-time context, and sortable-column handling now orders the list by event/show chronology instead of post creation date while leaving the default list order unchanged. Verification on 2026-03-14: `php -l includes/cpt/event-plans.php` PASS, and a temporary Chromium Playwright live-admin probe PASS confirmed the column renders and `orderby=vms_event_date` sorts visible rows correctly in both directions.
* [x] **COMP-01** Attendance Bonus Compensation: treated complete on 2026-03-22 after explicit user confirmation plus current source scan showing `attendance_bonus` across core registries, helper/payables, venue defaults, comp packages, and Event Plan CSV normalization in core VMS.

---

## 4) Schedule Views (Calendar + List parity)

* [x] **SCHED-01** Fix Single Venue Calendar UI (month padding, day-of-week row, align with All Venues styling) — marked complete on 2026-02-16 per operator confirmation
* [x] **SCHED-02** Show holidays in Schedule/Calendar views — marked complete on 2026-02-16 per operator confirmation
* [x] **SCHED-03 (RULE)** Enforce feature parity: any schedule feature added to Calendar must also be added to List (and vice versa) — marked complete on 2026-02-16 per operator confirmation
* [x] **SCHED-04** Calendar settings terminology neutrality — completed in core VMS `0.2.24.190` on 2026-03-14 in `vms/includes/admin/settings-page.php`: Calendar settings vendor-type rows now alias band/talent-like labels to `Primary Vendor` and food-truck-like labels to `Secondary Vendor`, preserving stored vendor-type slugs and existing schedule behavior while removing industry-specific wording from the settings UI.

---

## 5) CSS / Frontend Stability

* [x] **CSS-01** Restore missing/admin public CSS loading lost during refactor — completed in core VMS `0.2.24.191` on 2026-03-15 by centralizing asset URL/version/style-stack helpers in `vms/includes/helpers.php` and rewiring `vms/includes/docs-public.php` plus `vms/includes/public/vendor-profiles.php` to explicitly enqueue the shared public CSS foundation before their scoped styles. Verification on 2026-03-15: `php -l` PASS for the touched PHP files and canonical version markers; manual admin/public browser regression testing remains advisable but is no longer blocking closure of the source fix.
* [ ] **CSS-02** Continue removing inline styles and relocating to correct CSS files as templates are touched — 2026-02-16 passes moved Venue Calendar inline CSS/inline style attributes from `vms/includes/admin/venue-calendar.php` into `vms/assets/css/vms-admin.css`, moved Public Venue Calendar shortcode inline styles from `vms/includes/public/venue-calendar-shortcode.php` into `vms/assets/css/vms-ui.css`, moved Holidays admin page inline style attributes from `vms/includes/admin/holidays.php` into `vms/assets/css/vms-admin.css`, moved Staff Tax Sidebar metabox inline style attributes from `vms/includes/admin/staff-tax-sidebar.php` into `vms/assets/css/vms-admin.css`, moved Staff List Columns tax-missing inline style attribute from `vms/includes/admin/staff-list-columns.php` into `vms/assets/css/vms-admin.css`, completed Section 12 grouped admin pass (Group A: `vms/includes/admin/season-dates.php`, `vms/includes/admin/vendor-user-link.php`, `vms/includes/admin/tax-bypass.php`, `vms/includes/admin/vendor-comp-packages.php`, `vms/includes/admin/venue-duplicate-templates.php`, `vms/includes/admin/tax-profile-admin-metabox.php`; Group B: `vms/admin/docs-page.php`, `vms/includes/admin/vendor-list-columns.php`, `vms/includes/admin/vendor-list-ui.php`, `vms/includes/admin/settings-page.php`, `vms/includes/admin/vendors/tax-columns.php`, `vms/includes/admin/vendors/tax-export-csv.php`, `vms/includes/admin/venue-comp-defaults.php`, `vms/includes/admin/integrity-calendar-reconcile.php`) by moving remaining inline styles into `vms/assets/css/vms-admin.css`, and completed the portal/CPT follow-up pass on canonical files `vms/includes/portal/vendor-portal.php`, `vms/includes/portal/vendor-tax-profile.php`, `vms/includes/portal/staff-portal.php`, `vms/includes/cpt/venues.php`, and `vms/includes/cpt/ratings.php` by moving inline styles into `vms/assets/css/vms-portal.css`, `vms/assets/css/vms-admin.css`, and `vms/assets/css/vms-ui.css`; next canonical follow-up candidates are `vms/includes/helpers.php`, `vms/includes/vendor-applications.php`, and `vms/includes/admin/admin-notices.php` (non-canonical duplicate `* 2.php` files intentionally excluded; comment-only legacy snippets in schedule files remain non-runtime)
* [x] **CSS-03** Standardize notice/alert design system site-wide (critical/error, warning, success/info) with consistent colors, hierarchy, and urgency cues
* [ ] **CSS-04** `sr-image-reel` plugin image rendering consistency: enforce configured crop/aspect behavior across all pages where reels render (some routes currently show uncropped full portrait images instead of settings-defined crop) — [OUT-OF-SCOPE — LOGGED] iceboxed by user on 2026-02-16 while prioritizing VMS-core tasks
* [x] **CSS-05** Vendor Portal Availability calendar: restore deterministic past-day shading so past dates visually match intended read-only state — source-pass hardening applied on 2026-02-16 in `vms/assets/css/vms-portal.css` (explicit `td.vms-av-past` visual overrides for desktop/mobile) with same-day follow-up fix in `vms/includes/portal/vendor-portal.php` (initialize `$today_date` inside `vms_vendor_portal_render_availability()` so `vms-av-past` class is emitted deterministically, including inactive/out-of-season cells); manual portal UI verification deferred to consolidated PROC-03 pass
* [ ] **CSS-06** Increase admin notice spacing only on strict target pages (Meta Ads Builder, Add-ons, Docs, Budget Calculator, Season Dates, Staffing Templates, Staffing Rollups, Teams) with uniform bottom spacing; reopen noted 2026-02-24 after broad pass over-applied spacing on non-target pages and still produced non-uniform gaps; backburnered per user direction on 2026-02-24 (revisit later)
* [ ] **CSS-07** Audit and remove any remaining visible `UI rev:` labels on admin pages — Season Dates runtime marker removed on 2026-02-24 in `vms/includes/admin/season-dates.php`; follow-up scan remains open
* [ ] **CSS-08** Placeholder text legibility pass: make input placeholder styling visibly distinct from user-entered values (lighter contrast/clear placeholder state) across VMS admin/public form surfaces while preserving accessibility/readability.
* [ ] **CSS-09** Ticketing front stylesheet consolidation: reduce duplicated `single-tribe_events` / `tribe-events-single` selector layers in `vms/assets/css/vms-ticketing-front.css` before further ticket-layout polish; operator follow-up on 2026-03-08 flagged 125+ repeated `single-tribe_events` instances as a signal that ticket styling has become stacked-edit-on-top-of-edit and should be consolidated instead of patched indefinitely.
* [ ] **CSS-10** VMS admin shell version-row border continuity: restore the missing left and right borders on the shared VMS admin version/header row so the page chrome frames cleanly on both sides instead of looking open/broken around the version marker. Logged 2026-04-02 from operator screenshot review on `page=vms-vendor-command-center`; scope is shared VMS admin shell/header CSS only, not page-specific content cards.

---

## 6) Vendors / Performers (Schema + Editor UX + Exports)

* [x] **VEND-01** Audit all modules for master vendor schema alignment (no duplicated schemas or meta drift) — source audit + alignment pass completed on 2026-02-16 across canonical runtime paths: fixed duplicate `tax_attention_to` field collision in `vms/includes/core/registry/vendor-schema.php`, moved schema tax/profile keys to registry-backed lookups, expanded `vms/includes/core/registry/meta-keys.php` with canonical vendor tax/default-comp/meta entries, and updated vendor admin/save readers in `vms/includes/admin/tax-profile-admin-metabox.php`, `vms/includes/admin/vendor-details.php`, `vms/includes/cpt/vendors.php`, `vms/includes/admin/tax-bypass.php`, and `vms/includes/helpers.php` to use registry-backed vendor keys with safe fallbacks; alignment checks now report no unmapped schema meta keys and no unknown `vms_meta_key('vendor', ...)` fields in canonical include paths (non-canonical duplicate `* 2.php` files excluded by boot-graph rule)
* [x] **VEND-02** Vendor Edit: remove unused “Tax Profile” metabox — treated as historical duplicate-input cleanup and closed per user clarification on 2026-02-16; canonical vendor edit keeps one Tax Profile Admin input metabox (`vms/includes/admin/tax-profile-admin-metabox.php`) and separate Tax Status side metabox (`vms/includes/admin/vendors/tax-metabox.php`)
* [x] **VEND-03** Vendor Edit: separate contact info from pay structure UI — canonical separation pass completed on 2026-02-16 by moving `fee_min`/`fee_max`/`min_show_rate` UI+save handling out of `vms/includes/cpt/vendors.php` (Vendor Details now contact/location only) into `vms/includes/admin/vendor-details.php` (pay/defaults metabox), preserving existing meta keys and save behavior; vendor tax input capability remains unchanged in `vms/includes/admin/tax-profile-admin-metabox.php` and `vms/includes/admin/vendors/tax-metabox.php`; manual vendor-edit UI verification deferred to consolidated PROC-03 pass
* [x] **VEND-04** Add per-venue default rate (currently global only) — canonical pass completed on 2026-02-16 by adding per-venue vendor Event Plan default override storage (`_vms_vendor_default_comp_by_venue` via `vms_meta_key('vendor','default_comp_by_venue')`), admin edit UI/save handling in `vms/includes/admin/vendor-details.php`, and venue-aware runtime resolution in `vms/includes/helpers.php` so Event Plan vendor defaults and “Apply Band Defaults” use venue-specific values when configured and otherwise fall back to global vendor defaults; existing global defaults and tax input flows remain unchanged; manual vendor/event-plan UI verification deferred to consolidated PROC-03 pass
* [x] **VEND-05** Optional per-day-of-week default rates per venue — canonical pass completed on 2026-02-16 by adding vendor-specific `venue + day-of-week` Event Plan default override storage (`_vms_vendor_default_comp_by_venue_dow` via `vms_meta_key('vendor','default_comp_by_venue_dow')`), admin edit UI/save handling in `vms/includes/admin/vendor-details.php`, and runtime resolution in `vms/includes/helpers.php` so vendor defaults now resolve in deterministic order global -> per-venue -> per-venue/day (when event date is known); compensation options tile labeling and “Apply Band Defaults” now consume the day-aware resolver; existing venue defaults, global vendor defaults, and tax flows remain unchanged; manual vendor/event-plan UI verification deferred to consolidated PROC-03 pass
* [x] **VEND-06** Resolve overlap between Event Plan Defaults vs Vendor Details (merge or clarify) — clarify pass completed on 2026-02-16 in canonical vendor edit UI: renamed metabox label to `Pay Structure + Event Plan Defaults`, split pay box copy into explicit “Vendor Pay Preferences (Reference)” vs “Event Plan Defaults ("Apply Band Defaults")” sections in `vms/includes/admin/vendor-details.php`, and added contact-box pointer copy in `vms/includes/cpt/vendors.php`; no save-path/meta behavior changes; manual vendor-edit UI verification deferred to consolidated PROC-03 pass
* [x] **VEND-07** Vendor availability calendar: show Tentative for Draft/Ready, Booked for Published — canonical status-mapping pass completed on 2026-02-16 in `vms/includes/portal/vendor-portal.php` by centralizing availability derivation from Event Plan status (`draft|ready => tentative`, `published => booked`) and wiring that mapper into both the vendor Availability calendar busy-state resolver and the All Vendors read-only labels; legacy defensive behavior for non-canonical/unknown statuses is preserved, and vendor tax input capability remains unchanged (`vms/includes/admin/tax-profile-admin-metabox.php`, `vms/includes/admin/vendors/tax-metabox.php`); manual portal UI verification deferred to consolidated PROC-03 pass
* [ ] **VEND-08** Vendor conflict detection: Red Flag + integrity scan + optional vendor email + “Vendor verified” suppress
* [ ] **VEND-09** Vendor export presets (Tax1099.com, QuickBooks, others) with mapping templates + validation + safe export
* [x] **VEND-10** Event Plan vendor picker: inline “Add Tax Bypass” interface must be present and actionable when selecting vendor (re-add if missing)
* [ ] **VEND-11** Tax-exempt controls (manual): support both per-vendor manual tax-exempt status and a global tax-exempt mode that suppresses tax-profile alarms system-wide (for operators who never hire 1099 contractors)
* [ ] **VEND-12** Tax-exempt controls (automatic): global configurable reporting-threshold setting for auto tax-exempt behavior (default/fallback hardcoded to $600); if a vendor is not expected to exceed threshold, suppress tax-profile alarms
* [ ] **VEND-13** Vendor Import operator-packaging follow-up per `wp-content/plugins/vms-doc-archive/vms-build-specs/VMS Vendor Import/vendor-import-viability-audit.md`: treat the current importer as a vendor bootstrap / maintenance importer (not a full-fidelity vendor migration tool) and add the operator-facing packaging it still lacks: `Download Blank CSV Template`, shared VMS guided-tour anchors/registration for `vms-data-tools-vendor-import`, and doc-copy cleanup that removes stale “Map fields if prompted” wording in favor of header-alias + template + Preview -> Commit guidance.
* [ ] **VEND-14** Vendor Availability Admin Board: per `wp-content/plugins/vms-doc-archive/vms-build-specs/Vendor Availability Admin Board/VMS_BUILD_SPEC_VAV-01_vendor-availability-board.md`, add `VMS -> Vendor Availability` with month/list views, vendor-type/status/name/venue/active filters, drill-down detail, guided help, and a shared canonical availability resolver/reason model reused by the vendor portal, Event Plan vendor picker, and future staff availability board. Logged 2026-03-13 from a user-supplied build spec; work it in where it fits without changing current Section 12 priority.
* [ ] **VEND-15** Vendor Portal multi-business applications: allow a logged-in vendor user to launch an `Add a business` flow from the Vendor Portal, reuse the canonical vendor application form/page for the new submission, store the submitting WP user on the application, and on later approval link the resulting vendor back to that same user through canonical vendor-user-links without auto-approving or bypassing the review queue. Scope authority: `wp-content/plugins/vms/docs/build_specs/VMS_VEND-15_Vendor_Portal_multi_business_applications_2026-03-31.md`. Logged 2026-03-31 by explicit user reprioritization.

---

## 7) Staffing + Payroll

* [x] **STAFF-01** Labor-per-event staffing roles (bartender, sound tech, cleanup) as distinct category — canonical Phase A pass completed on 2026-02-16: added structured staffing schema/tables + migration (`vendor_core_v3`) and constants (`vms/includes/db/migrations.php`, `vms/includes/core/registry/constants.php`, `vms/includes/core/plugin.php`), core role/template/slot/assignment/rollup/audit services with rebuild support (`vms/includes/core/staffing.php`, `vms/includes/core/load.php`), admin Staff Roles/Templates/Rollups surfaces (`vms/includes/admin/staffing.php`, `vms/includes/admin/load.php`), Event Plan staffing editor + structured save path + create-time template seeding (`vms/includes/cpt/event-plans.php`, `vms/includes/admin/schedule.php`), and Dashboard “Staffing Readiness (Next N Events)” panel wiring (`vms/includes/admin/dashboard.php`, `vms/includes/rest-dashboard.php`, `vms/assets/admin-dashboard.js`, `vms/assets/css/vms-admin.css`); manual UI verification remains deferred to consolidated `PROC-03` per user direction
* [ ] **STAFF-02** Track vendor/staff payments per event (paid status, amount, method) with future QuickBooks sync path
* [ ] **STAFF-03** Staff responsibility checklist auto-assigned per event (repeatable to-do list)
* [ ] **STAFF-04** Import historical work logs (vendor/staff) once hours tracking exists

* [ ] **STAFF-05** Task system (role-based + individual): recurring tasks (daily/weekly/monthly/custom), due dates, completion log, optional per-event linkage, and dashboard “overdue/missing” surfacing.
* [ ] **STAFF-06** Staff role pay tiers similar to vendor pay: hourly and/or daily/per-event rates, optional per-venue + per-day-of-week defaults, and override support.

---

## 8) Holidays + Season Dates

* [ ] **HOL-01** Data Tool: Upload Holidays CSV importer (`vms_holidays`) with Preview → Commit, validation, safe merge/overwrite, audit trail
* [ ] **HOL-02** Holidays as global/multi-venue (one holiday can apply to multiple venues)
* [ ] **SEAS-01** Season Dates: add pattern/day-of-week constraint option (e.g., Fri–Sat only)

---

## 9) Dashboard / Finance / Obligations

* [x] **FIN-01 (TEST)** Upcoming Bills: verify Published-only default + Draft/Ready toggle works correctly
* [x] **FIN-02** Due Dates / Compliance system: Payee storage + admin CRUD — complete and verified on 2026-02-16 (source pass: payee option storage + add/edit/archive admin flows confirmed)
* [x] **FIN-03** Due Dates / Compliance system: Obligations storage with schedules (monthly/quarterly/annual/one-time) — complete and verified on 2026-02-16 (source pass: obligation CRUD + cadence generation engine confirmed)
* [x] **FIN-04** Dashboard: due soon windows + overdue highlighting (mobile-first) — complete and verified on 2026-02-16 (source pass: dashboard due panel response + overdue/due-soon summary + visual attention states confirmed)
* [x] **FIN-05** Completion tracking: append-only audit log, reversible history, optional proof link/notes, guardrails — complete and verified on 2026-02-16 (source pass: append-only complete/uncomplete log actions, dashboard completion captures optional note+proof URL, and Recent Completion Activity surfaces proof links with a guarded Reopen action)
* [x] **FIN-06** Obligations list page: filters + “Mark complete” actions — complete and verified on 2026-02-16 (source pass: deterministic due-instance filters/status ordering added via shared core builder; Due Dates obligations list now exposes direct append-only Mark complete/Reopen actions; REST obligations list endpoint added for parity). Manual operator click-through verification (filters + mark complete/reopen UX pass) deferred/backlogged for follow-up.
* [x] **FIN-07** Dashboard redesign “QuickBooks-like” summary cards + financial section — complete and verified on 2026-02-16 (source pass: dashboard now includes a Financial Snapshot panel with deterministic at-a-glance bill/obligation totals + alert counts sourced from existing bills/due builders; existing inclusion filters and Due Dates mark-complete actions preserved)
* [x] **FIN-08** Budget Calculator: annual goals + progress + past revenue + live forecast from event plan changes — complete and verified on 2026-02-16 (source pass: calculator now includes annual-goal inputs, deterministic financial-context Event Plan inclusion for live annual forecast, past ticket-revenue context from explicit ticket-stats snapshots, and per-plan forecast breakdown with no automated money actions). Manual operator click-through QA of FIN-08 forecast/progress UX and mixed status/ticket-stats scenarios deferred/backlogged for follow-up.

---

## 10) Integrations + Legacy Compatibility

* [x] **INT-01** Event Plans can create a TEC event (not only link existing)
* [x] **INT-02** Locate and confirm ticket discount logic; ensure it still applies or re-implement hook — compatibility hardening shipped on 2026-02-24 in legacy `vmsx-ticket-discounts` `0.1.3` by aligning qualifier/amenity/ticket-key defaults to active `_sr_*` conventions and adding VMS marker-aware fallbacks (`_vms_product_role`) so discount qualification remains valid for current Ticketing flows. Active runtime plugin is now `vms-commerce-discounts`.
* [ ] **INT-03** Related Events widget: convert to plugin
* [ ] **INT-04** Related Events plugin: show one event at a time + Get Tickets button + motion/scroll effect
* [ ] **INT-05** VMS ↔ TEC status mapping rules: Draft Event Plan => TEC Draft + vendor availability Tentative; Ready => TEC Draft/Pending (not public); Published => TEC Published + vendor availability Booked; Cancelled => choose one canonical behavior (keep TEC Published with cancellation banner OR move TEC to Draft/Private and remove from public)
* [x] **INT-06** Rename plugin display name `Ticket Discounts Add-on (VMSX)` to a `VMS...`-prefixed name so VMS-family plugins stay grouped together in wp-admin Plugins list — completed 2026-02-25 in legacy `vmsx-ticket-discounts/vmsx-ticket-discounts.php` (`Plugin Name: VMS Ticket Discounts Add-on`). Current active plugin is `vms-commerce-discounts` (`Plugin Name: VMS Commerce Discounts`).

---

## 16) Marketing / Social Sharing

* [x] **SOCIAL-01** Social Sharing Phase 0/1 foundation (non-purchase-safe): provider registry/stubs, custom social queue/audit/tables, template engine + UTM, admin shells (Overview/Settings/Accounts/Venue Mapping/Templates/Queue/Logs), and Event Plan Promotion panel manual tools + queue/schedule/retry/cancel actions — source-validated on 2026-02-16 (`php -l` pass for new module files, `node --check` pass for social admin JS). Follow-up stabilization pass on 2026-02-16 added operator QA fixture `vms/tests/e2e/social01-sharing-stabilization.spec.ts` (copy/share + queue/schedule/retry/cancel flow coverage); live run currently SKIP due environment mismatch (`vms-social-sharing` menu and Promotion panel not exposed on the current target), so operator click-through remains blocked pending deployment/capability parity.
* [x] **SOCIAL-02** Social Sharing Phase 1 continuation: restore deployment/capability parity on the operator target (Social Sharing submenu + Event Plan Promotion panel visible to test account), then execute and record non-skip `social01-sharing-stabilization.spec.ts` pass and reconcile any queue/action regressions before Phase 2 provider OAuth/posting work. 2026-02-16 probe evidence initially showed parity mismatch on target (menu/panel/action denial), then runtime queue-form nesting issue was corrected and deployment advanced to `0.2.23.90`; final non-skip operator QA evidence recorded with live run PASS (`npx playwright test tests/e2e/social01-sharing-stabilization.spec.ts --project=chromium --reporter=line` => `1 passed`) on 2026-02-16.

---

## 11) Admin IA

* [ ] **MENU-01** Reorganize/reorder VMS admin menu (e.g., Season Dates under Settings, consolidation)
* [x] **MENU-02** Primary top-nav tab quick menus: add popup/hover menu per primary tab that visually extends the active tab style (matching color family, tab-connected shape, subtle shadow, restrained motion) to reduce click depth — completed 2026-03-02 in `vms/includes/admin-ui/nav.php` + shared admin UI styles/JS (quick-menu hover + focus access retained alongside static pill submenu row).
* [x] **MENU-03** Left VMS submenu consolidation: align/remove duplicate legacy left-nav entries so admin left menu mirrors the primary-tab information architecture and avoids split navigation paths — completed 2026-03-02 in `vms/includes/admin-ui/nav.php` via compact IA pass (`vms_admin_ui_compact_left_menu`).
* [x] **MENU-04** Marketing submenu ordering polish: place Social Sharing first/leftmost in Marketing & Social top-nav submenu and hub cards — applied 2026-02-24 in `vms/includes/admin-ui/nav.php`
* [ ] **MENU-05** VMS Settings page layout clarity: split major settings groups into clearly separated card/panel containers (headings retained) so sections no longer visually blend together on long pages.
* [ ] **MENU-06** Data Tools visual parity: keep `vms-data-tools` as a separate plugin but render its admin surfaces with the shared VMS shell/theme styling so Data Tools UI looks and behaves like native VMS pages (logged per user direction on 2026-03-02).
* [ ] **MENU-07** Mobile menu behavior update: remove static submenu pill rows on mobile layouts and use popup submenu interaction only (logged 2026-03-04; queued behind emergency ticketing repair unless reprioritized).

---

## 12) Public Opportunity Systems (Future / Logged)

* [ ] **PUB-01** Staff job board + account-required applications + PDF-only resumes with strict guardrails
* [ ] **PUB-02** Events up for bid for vendors (Opportunity + Submission model)
* [ ] **PUB-03** Market Events module (multi-vendor capacity, category caps, apply/approve/waitlist)
* [ ] **PUB-04** Guest List / Comp Admission system (auditable, check-inable, no SKU)
* [ ] **PUB-05** Pass Claims (Forecast-First, Claim-Only) implementation: Source/Batch/Token/Claim/Reservation model + Ops Console check-in parity (Option A attendee-cache integration) + attribution reports + preview/commit safety, per `DEBUGGING/vms-doc-archive/vms-build-specs/Guest List & Comp Admission/VMS_BuildSpec_PassClaims_ForecastFirst_2026-02-28.md` (added 2026-03-01). Progress update 2026-03-01: admin Sources/Batches/Passes/Reports, public claim route/form, reservation creation (`gl:<id>`), route fallback fix, zero-date expiry guard, pass `Void/Restore`, token CSV export, Ops attendee parity for `vms_event_plan` source, deeper reports parity (Per Source + Per Batch + Source/Event + Per Event with checked-in and unique-vs-repeat metrics), CSV export for each report view, and pass-batch-specific door check-in windows (`same_day` vs `24h` vs `48h`, with admin override mode behavior) enforced on `gl:` scans are in. Door-flow polish update 2026-03-02: Ops Ticket Console now exposes `Check In All Remaining` for `gl:` references (uses existing `qty` check-in support, cache-aware remaining count labels, and guest-list progress feedback). Additional polish update 2026-03-02: Guest List scan guidance now renders inline cache-state hints and all-remaining check-in can auto-refresh attendee cache once when `gl:` progress is missing; cache updates also backfill missing Guest List rows from check-in responses. Micro-copy polish update 2026-03-02: streamlined `gl:` button states and scan-hint text for faster door readability. Keep open for any further operator-requested door UX polish.

---

## 13) Release Hygiene / Process

* [ ] **PROC-01** Maintain `vms-build.txt` in plugin root for every patch pass
* [ ] **PROC-02** Enforce bombproof rules: no silent failures; reversible bulk actions; explicit confirmation for money/legal/irreversible changes
* [x] **PROC-03 (TEST)** Run one consolidated manual UI regression sweep (admin + public) after current implementation batching; per-task manual UI click-through is deferred by user direction (2026-02-16). Safe non-purchase live preflight executed on 2026-02-16 via Playwright smoke-admin (`tests/e2e/smoke-admin.spec.ts`) with PASS=1 (admin login + VMS dashboard accessibility). Additional non-purchase automation pass executed on 2026-02-16 via `tests/e2e/proc03-non-purchase-smoke.spec.ts` with PASS=2 (admin non-purchase surfaces: Dashboard/Schedule/Event Plans/Vendors/Staff/Venues plus dashboard panel hydration checks for `financial,today,week,staffing,bills,due`; public portal probes: `/vendor-portal/` 200, `/staff-portal/` 200, `/vendor-tax-profile/` 404). Stabilization-gate rerun executed on 2026-02-28 (America/Chicago): `smoke-admin.spec.ts` PASS=1, `proc03-non-purchase-smoke.spec.ts` PASS=2 (same portal probes: 200/200/404), and ticketing-critical augmentation `bug08-ticket-flow-mobile-actions.spec.ts` + `bug09-ticket-ui-entitlement-stepper.spec.ts` => PASS=3/SKIP=1 (BUG-08 skipped because no ticket-enabled event/modal was available in this environment snapshot). Additional admin mutation-sensitive check `bug13-link-existing-flow.spec.ts` executed with SKIP=1 (no eligible fixture path completed in this snapshot). Manual operator closure recorded from `vms/docs/PROC03_MANUAL_CLICKTHROUGH_CHECKLIST_2026-03-01.txt` on 2026-02-28 with overall Step 20 marked PASS; follow-up UX styling note logged as `CSS-08`.

---

## 14) Event Plan Status Workflow

* [x] **STATUS-01** Implement and expose true “Cancelled” Event Plan status in workflow/UI (currently not fully implemented)
* [x] **STATUS-02** Cancellation orchestration: policy-driven cancel flow that coordinates notifications, provider-native ticket sales stop, and Woo-native refund queue/execution with audit trail (without replacing TEC/Woo engines) — canonical stale-check validation completed on 2026-02-16 with seeded cancelled-plan run (`wp vms stale-check --bugs=CAN-01` => PASS=1/WARN=0/MANUAL=0; cancelled plans checked=1; zero envelope/state/review/step-shape/summary-state/step-total/run-audit/retry-audit mismatches)

---

## 15) Non-VMS Site / Commerce

* [ ] **SITE-01** SerenadeRange.com (non-VMS core): evaluate and implement customer referral/affiliate program via WooCommerce plugin flow when budget/timing allows

---

## 17) Meta Ads + Dashboard UX

* [x] **MA-01** Meta Ads Builder: Templates panel now defaults collapsed on `page=vms-ma-ads-builder` with explicit expand/collapse affordance and preserved template actions; applied 2026-02-24 in `vms-meta-ads/includes/admin/screens/ads-builder.php`, `vms-meta-ads/assets/ads-builder.js`, `vms-meta-ads/assets/admin.css`, and `vms-meta-ads/assets/guidance.js` (validated by user).
* [x] **DASH-01** VMS Dashboard: removed the top four summary cards entirely (they were perceived as pointless/non-valuable above the live panels), including related card styling; applied 2026-02-24 in `vms/includes/admin/menu.php` and `vms/assets/css/vms-admin-ui.css`
* [ ] **DASH-02** VMS tour drift remediation (`auto-update` report at 2026-02-28 21:37 America/Chicago): restore required missing anchors on `vms-dashboard-top` (`dashboard_welcome`, `dashboard_health`, `dashboard_quick_actions`, `dashboard_start_venue`), bind to canonical tour ID, and add a deterministic guard so required-anchor scans stop reporting drift for this context.
* [x] **MA-02** Meta Ads Builder template-save layout polish: move `Save Settings as Template` below template-name input in Step G (button no longer above the related form field) — applied 2026-02-24 in `vms-meta-ads/includes/admin/screens/ads-builder.php`
* [x] **MA-03** Meta Ads Builder template management: add delete action for saved templates (confirm + REST delete + list refresh) — applied 2026-02-24 in `vms-meta-ads/includes/admin/screens/ads-builder.php` and `vms-meta-ads/assets/ads-builder.js`
* [x] **MA-04** Meta Ads Builder skip-ahead guardrail: replace easy-to-miss lock toast with centered blocking modal when users try to jump ahead before Step A completion — applied 2026-02-24 in `vms-meta-ads/assets/guidance.js`, `vms-meta-ads/assets/ads-builder.js`, and `vms-meta-ads/includes/admin/screens/ads-builder.php`
* [x] **MA-05** Meta Ads Settings guided-tour collision follow-up: placement hardening applied and viewport probe revalidated 2026-02-24 with no overlap on targeted controls across desktop (1366x900), laptop (1024x768), tablet (834x1112), and mobile (390x844); changes in `vms-meta-ads/includes/tours/registry.php` and `vms-meta-ads/assets/tours.js`
* [ ] **MA-06** UI convention: keep form action buttons to the right of, or below, their related form inputs; avoid above-input action placement unless explicitly requested
* [ ] **MA-07** Meta Ads Builder stabilization: audit and repair the current `vms-meta-ads` admin builder workflow now that the local plugin matches the live/server copy, starting with `BUG-28` template-save/name persistence so saved templates always retain and display the exact operator-entered name, then continue through the remaining operator-reported builder regressions within the standalone `vms-meta-ads` plugin only. Scope authority: `wp-content/plugins/vms/docs/build_specs/VMS_MA-07_Meta_Ads_Builder_stabilization_2026-03-31.md`.

---

## 18) Ops Console / Day-to-Day Operations (Priority)

* [x] **OPS-01** Scan ID window/mobile fit: reduce scanner camera viewport height in the Scan ID modal so action controls (especially Cancel) remain visible on common phone screens without extra scrolling; preserve scan reliability and existing scanner behavior — completed 2026-02-24 in `vms-ops-console-premium/pwa/assets/css/app.css` (scan card height clamp + ID-mode viewport reduction), user-validated (“Much better!”).
* [ ] **OPS-02** Alert Staff workflow: add/confirm a tracked “Alert Staff” daily-ops flow (clear trigger action, recipient routing/presets, visible delivery status/confirmation, and audit trail) so this is explicitly prioritized for day-to-day operations hardening — source pass applied 2026-02-24 in `vms-ops-console-premium/pwa/assets/js/app.js`, `vms-ops-console-premium/pwa/assets/css/app.css`, `vms-ops-console-premium/includes/alerts/alerts-core.php`, `vms-ops-console-premium/includes/rest/routes.php`, and `vms-ops-console-premium/vms-ops-console-premium.php`; pending user validation.
* [x] **OPS-03** Teams scaling: support Staff Role-driven Ops capability profiles with one-click sync to linked users (to avoid per-user setup for larger crews while keeping per-user overrides available) — initial source pass applied 2026-02-24 in `vms-ops-console-premium/admin/teams.php`, `vms-ops-console-premium/admin/assets/admin.css`, and `vms-ops-console-premium/vms-ops-console-premium.php`; follow-up hardening on 2026-03-12 repaired the stale Staff Role/Portal User wiring in core VMS (`vms/includes/admin/staff-list-columns.php`, `vms/includes/cpt/staff.php`), added exact per-user Ops capability overrides via `vms_ops_cap_overrides` plus `user_has_cap` enforcement in `vms-ops-console-premium/includes/capabilities.php`, made Teams sync/readouts use canonical linked staff + role lookups, exposed linked Staff / Roles context in the Teams grid, and switched the Teams save submit path to a compact JSON payload in `vms-ops-console-premium/admin/assets/admin.js` so larger user grids no longer risk silent `max_input_vars` truncation. Syntax validation PASS (`php -l`, `node --check`); live admin click-through remains advisable, but operator accepted the wrap and this item is now considered implemented.
* [ ] **OPS-04** Ticket + ID console density: make Ticket Console and ID Console panels collapsible with per-user remembered open/closed state to reduce scroll during live operation; source pass applied 2026-02-24 in `vms-ops-console-premium/vms-ops-console-premium.php`, `vms-ops-console-premium/pwa/assets/js/app.js`, and `vms-ops-console-premium/pwa/assets/css/app.css`; pending user validation.

* [ ] **OPS-05** Venue selection QoL (global rule): anywhere a venue is chosen in VMS/Data Tools, replace raw venue-ID text entry with a venue select (searchable when needed); if only one active venue exists, auto-default and hide the selector. Explicit user directive logged 2026-03-02 after Vendor Invites preview review.
* [ ] **OPS-06** ID Console hardening: per `wp-content/plugins/vms-doc-archive/vms-build-specs/Ops Console Private Club/vms-ops-console-premium-0.1.33-build-spec-id-age-403.md`, add a large always-visible age card/banner for all scan outcomes, make the not-found / `Add Member` path capability-aware, harden service-worker shell caching so stale nonces do not cause connected-use `403` loops, add client-side nonce/session recovery with human-readable auth/permission errors, and keep the REST capability checks strict. Source-validation update 2026-03-14: current `vms-ops-console-premium` `0.1.35` already includes the age card, capability-aware not-found flow, network-first shell service worker, and categorized auth/permission messaging (`vms-ops-console-premium.php`, `pwa/assets/js/app.js`, `pwa/assets/css/app.css`, `pwa/sw.js`, `includes/rest/routes.php`); syntax checks PASS (`php -l`, `node --check`). Automated QA update 2026-03-14: added `plugins/vms/tests/e2e/ops06-id-console-guardrails.spec.ts` to mount the real Ops PWA shell against mocked REST responses and assert adult/under-21/unknown age states, scan-only vs manager not-found behavior, and invalid-nonce recovery messaging; Chromium PASS (`node ./node_modules/@playwright/test/cli.js test tests/e2e/ops06-id-console-guardrails.spec.ts --project=chromium`). Live runtime update 2026-03-14: authenticated loopback validation against Local (`serenaderange.local:10004`) confirmed the real shell loads for the logged-in user, bootstrap returns venues/presets, presence start succeeds, manual-member create succeeds, adult scan succeeds, under-21 scan succeeds, not-found scan returns `manual_creation_required` + prefill, and create-then-rescan recovery succeeds. Keep open until an actual operator/device pass confirms camera-based scan UX, scan-only-user gating, and stale-session messaging in the real browser UI; local WebKit rerun is currently blocked by a missing Playwright WebKit binary in this environment.
* [ ] **OPS-07** ID Scans menu visibility: expose a visible admin/navigation path to the ID Scans / ID Console screen because the user reported on 2026-03-14 that they still cannot find it in any menu. [OUT-OF-SCOPE — LOGGED]
* [x] **OPS-08** Post-show scan buffer + Teams setup red-flag hardening: completed on 2026-03-15 in `vms-ops-console-premium` `0.1.36` across `vms-ops-console-premium/includes/db-schema.php`, `vms-ops-console-premium/includes/ticket-console/events.php`, `vms-ops-console-premium/includes/ticket-console/checkin.php`, `vms-ops-console-premium/includes/rest/routes.php`, `vms-ops-console-premium/includes/setup-status.php`, `vms-ops-console-premium/admin/teams.php`, `vms-ops-console-premium/admin/settings.php`, `vms-ops-console-premium/admin/assets/admin.css`, `vms-ops-console-premium/admin/assets/admin.js`, `vms-ops-console-premium/pwa/assets/js/app.js`, `vms-ops-console-premium/pwa/assets/css/app.css`, `vms-ops-console-premium/vms-ops-console-premium.php`, and `vms-ops-console-premium/vms-build.txt`. Ops now keeps ended events available for a configurable post-show scan buffer using WordPress site time, Teams exposes setup-status badges/checklists/actions plus summary counts, WP Users shows Ops setup state, scanner permission denials carry clearer Teams/setup guidance, and guided help covers the updated Teams/Settings workflows. Verification on 2026-03-15: `php -l` PASS on touched Ops Console PHP files, `node --check` PASS on touched JS, Chromium Playwright PASS for `plugins/vms/tests/e2e/ops08-scan-buffer-setup-warnings.spec.ts`; local WebKit rerun remains blocked by a missing Playwright WebKit binary.
* [x] **OPS-09** Verified Member Lookup — completed 2026-03-15 in `vms-ops-console-premium` `0.1.37` across the capability registry, member lookup/member data helpers, REST routes, admin settings/teams surfaces, PWA lookup flow, and focused Chromium coverage (`plugins/vms/tests/e2e/ops09-verified-member-lookup.spec.ts`). Standalone bartender-safe member lookup now supports search/result/detail verification, DOB/confidence gating, service-decision logging, optional profile photos, settings/policy copy, and guided help without weakening the existing ID-scan flow. Verification on 2026-03-15: `php -l` PASS on touched Ops Console PHP files, `node --check` PASS on touched JS/spec files, Chromium Playwright PASS for the `OPS-09` spec, and package built as `wp-content/plugins/vms-ops-console-premium-0.1.37.zip`; local WebKit rerun remains blocked by a missing Playwright WebKit binary in this environment.
* [ ] **OPS-10** Per-event Ops access buffer override: allow a user-configurable login/scan buffer on an individual event so operators can temporarily reopen or extend access for a specific event (for example, yesterday’s event) without changing the global Ops post-show buffer default or the canonical event date/time. Logged 2026-03-15 from operator follow-up after `OPS-08`; keep this separate from `OPS-09` unless the user explicitly folds it in.

---

## 19) VMS Status Notices (Top Priority Enhancement)

* [x] **NOTICE-01** Build Status Notices system per `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Status Notices/vms-status-notices-build-spec.md` (targeted front/admin notices with non-technical operator controls, intensity modes, client-side final targeting for cache-safe behavior, and debug visibility). Baseline implementation shipped 2026-03-01: CPT + module boot + custom admin list/edit screen + template presets (including iOS Safari ticketing warning) + save/duplicate/toggle/trash actions + list bulk actions (Enable/Disable/Trash) + front/admin runtime evaluator/renderer/debug panel + simulator scaffold across `vms/includes/modules/status-notices/*`, `vms/assets/js/vms-notices-front.js`, `vms/assets/css/vms-notices-front.css`, `vms/assets/js/vms-status-notices-admin.js`, and `vms/assets/css/vms-status-notices-admin.css`; follow-up hardening switched status banners to module-specific query arg `vms_status_notice_result` in `admin-ui.php` to avoid generic-arg stripping. Automated QA on 2026-03-01: `notice01-status-notices-smoke.spec.ts` PASS and `notice01-status-notices-front-match.spec.ts` PASS (rerun after bulk-actions update: PASS=2). Completion update 2026-03-02: replaced raw targeting textareas with non-technical add/remove controls + search-and-add object picker (with secure admin AJAX lookup), expanded simulator to evaluate role-targeting conditions, and finished status-notice spec-parity UX closeout.
* [ ] **NOTICE-02** VMS admin notice placement normalization: render admin notices for VMS pages above the page title instead of below it, while still keeping them inside the page body and below the VMS submenu/shell. Reconfirmed 2026-04-02 from operator screenshot review after notices were observed rendering below the `Vendor Command Center` title again. Scope: shared VMS admin page chrome and notice render placement only; do not silently restyle unrelated notice content or fold this into unrelated plugin work unless the user explicitly reprioritizes it.

---

## 20) Event Plan CSV Import (Completed)

* [x] **EPCSV-01** Build Event Plan CSV Import per `DEBUGGING/vms-doc-archive/vms-build-specs/Event Plan CSV Import/VMS_BUILD_SPEC_Event_Plan_CSV_Import.md`: baseline implementation shipped 2026-03-01 in `vms/includes/services/event-plan-import/event-plan-import-engine.php`, `vms/includes/admin/data-tools/page-event-plan-import.php`, and `vms/includes/admin/data-tools/actions-event-plan-import.php` with Data Tools surface (`VMS -> Data Tools -> Import Event Plans (CSV)`), Preview -> Commit workflow, VMS-only upsert keyed by `_vms_import_event_key`, venue/vendor resolution + optional vendor auto-create, primary/secondary vendor + compensation mapping, default Ticketing v2 template auto-apply when config is missing, strict no TEC/Woo writes during import, report CSV download, import audit history, and `Revert last import` snapshot restore for updated plans. Completion update 2026-03-02: shipped selective commit scope controls (all-vs-selected with row checkboxes + audit summary fields) and finalized Section 11/14 modernization closeout in `vms/includes/cpt/event-plans.php` with legacy ticket field hard-cut (no legacy reads/writes) plus one-time admin cleanup migration that removes legacy ticket meta when v2 config exists or default template auto-apply succeeds. Follow-up update 2026-03-07: clarified that the v2 importer lives in core VMS because it owns canonical `vms_event_plan` writes and the VMS admin Data Tools surface; raw-title apostrophe matching now preserves `'`/`’` lookup parity for venues/vendors and avoids false missing-match recreate attempts, and deprecated `vms-data-tools/includes/admin/page-events-import.php` is now explicitly annotated as legacy with the v2 path.

---

## 21) VMS Data Tools Square Sync

* [x] **SQUARE-01** Implemented `DEBUGGING/vms-doc-archive/vms-build-specs/VMS Data Tools Square Sync/VMS-BuildSpec-DataTools-Square-POS-Actuals.md` in `vms-data-tools`: provider-neutral Square POS actuals now expose normalized windowed totals + buckets API, Event Plan snapshot action, bucket mapping UI, and auditable snapshot meta with explicit error logging in `vms-data-tools/includes/integrations/square/vms-square-sync.php`. Wrap hardening on 2026-03-07 made selector refresh reporting-category-aware while keeping legacy category-ID mappings compatible, persisted local window/business-date/source audit fields in snapshot payloads, and bumped `vms-data-tools` to `0.3.0`. Manual live Square sync / Event Plan snapshot click-through QA was deferred/backlogged by user direction; current closure is source-verified (`php -l` PASS on touched Data Tools PHP files).

---

## 22) Vendor Invite Orchestrator

* [x] **VIO-01** Implemented `DEBUGGING/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/vms-data-tools_vendor-invite-orchestrator_build-spec_2026-03-02.md` in `vms-data-tools`: Vendor Invite Orchestrator foundation now includes single-use vendor claim tokens/endpoint, Event Plan-based open spots engine, MailPoet bridge (subscriber fields + tags), Preview -> Commit bulk invite flow, vendor preferred-language handling, and auditable invite/claim/admin-claim-link logs with explicit admin-visible errors and safe commit gating. Wrap hardening on 2026-03-07 aligned MailPoet verification to real vendor types/languages, blocked commits when MailPoet delivery is unavailable, excluded intent rows missing vendor type, and honored the on-invite subscriber-creation setting. Guided-help follow-up on 2026-03-07 added shared VMS tour anchors/registration to Vendor Invites and codified the protocol rule that new operator-facing modules must ship with at least one registered guided tour plus screen anchors unless the deferral is explicitly logged.
* [x] **VIO-02** Implemented `DEBUGGING/vms-doc-archive/vms-build-specs/Vendor Invite Orchestrator/Vendor Invite Orchestrator — Next Phase Spec.md`: Open Dates preview now evaluates opportunities inside the Event Plan loop instead of gating on `secondary_vendor_type`, Vendor Portal now exposes an `Opportunities` tab with `I’m Interested` submissions backed by `{$wpdb->prefix}vms_vendor_opportunity_submissions`, and Vendor Invites admin now includes an `Interest Queue` tab that can assign accepted vendors back onto Event Plans through the canonical secondary-vendor storage path. Wrap notes: Data Tools now owns the opportunity submission workflow/service layer, core VMS portal gained extension hooks so the add-on can register the new front-end tab without forking the portal renderer, and `vms_event_plan_set_secondary_vendors()` was added in core so admin queue assignments reuse the same secondary-vendor meta shape as the Event Plan editor.
* [x] **VIO-03** Invite log retention controls — completed on 2026-03-13 in `vms-data-tools`: `wp_vms_vendor_invite_log` now carries explicit retention lifecycle columns, new table `wp_vms_vendor_invite_retention_audit` records archive/restore/purge snapshots, `includes/vendor-invites/retention.php` adds preview-token storage plus archive/restore/purge commit helpers, `Invite Logs` now defaults to `Active` with preview -> commit retention controls and a `Retention` column, the old `Delete Failed Log` row action is removed, and the legacy delete helper now fails loudly with a retention-controls message instead of silently deleting rows. Guided tour coverage was updated in `includes/vendor-invites/tours.php`, admin styling was extended in `includes/admin/vendor-invites.css`, `vms-data-tools` was bumped to `0.4.2`, and non-destructive Chromium coverage was added in `vms/tests/e2e/vio03-invite-log-retention.spec.ts`. Verification on 2026-03-13: `php -l` PASS on touched Data Tools PHP files, Chromium Playwright PASS for `tests/e2e/vio03-invite-log-retention.spec.ts` and `tests/e2e/smoke-admin.spec.ts`, and a direct local DB probe confirmed the new retention columns plus `wp_vms_vendor_invite_retention_audit`.

---

* [ ] **TICK-VERIFY-STOREAPI-01** Woo block cart/checkout hard-stop parity: source pass applied 2026-03-20 in `vms/includes/integrations/ticketing-rules-v2.php` and `vms/assets/vms-ticketing-front.js` to mirror VMS ticket/add-on blockers into Store API cart errors, block-checkout order-submit hard stops, and Store API add-to-cart validation. Cart-mutation freeze follow-up applied 2026-03-20: Store API cart-errors are now checkout-only, while cart/checkout blocker UI refreshes through the VMS cart-context endpoint so removing qualifying GA lines no longer freezes the block cart. UX follow-up applied in `0.2.24.272`: blocked checkout pages now route Retry back to cart, blocker copy no longer implies the button will auto-fix the cart, native Woo blocker notices are preferred over duplicate VMS fallback banners, and the event-page add-on helper now warns when a selected add-on gets auto-removed or reduced after ticket counts change. Keep open until live user validation confirms add-on-only carts and unverified restricted-ticket carts can no longer complete through Woo block cart/checkout flows and that the updated blocker UX behaves correctly on the target payment stack.

- 2026-03-20: v0.2.24.273 follow-up: protect server-controls event UI from legacy rehydrate path; force hard refresh on cart/checkout bfcache returns; hard-navigate cart remove links to avoid stale add-on-only cart state.

[2026-03-21 00:30] 0.2.24.274: Removed duplicate inline server-controls controller and hardened ticket quantity detection/remove flow. Re-test required for event-page recognition + last-item cart remove.

[2026-03-21 00:58] 0.2.24.275: Rolled front-end event controller back to 0.2.24.267 baseline and reintroduced only minimal cart/checkout blocker UI helpers. Explicitly removed custom cart remove interception to stop quantity multiplication regression.

[2026-03-21 00:00] Ticketing follow-up: 0.2.24.275 rollback was incomplete because it restored the front JS but not the matching PHP inline server-controls controller from 0.2.24.267. Build 0.2.24.276 restores that controller and adds an owner guard so the footer bundle does not retake the event page.

---

## 19) Cancellation Resolution / Event Credits

* [x] **CAN-EC-01** Event Credit foundation for cancelled events: refunds remain available, but operators can optionally issue a one-use Event Credit when a customer chooses credit instead of refund. Implemented in `0.2.24.564` with private `vms_event_credit` records, Woo fixed-cart coupon generation, Event Plan cancellation-panel issuance buttons, customer email option, duplicate-credit guard by original event/order, expiration metadata, coupon redemption status sync, and eligibility restriction to future event products.
* [ ] **CAN-EC-02** Customer self-service cancellation resolution portal: allow ticket holders to choose refund vs Event Credit from a secure link rather than requiring operator-side manual selection.
* [ ] **CAN-EC-03** Event Credit reporting: add dashboard/reporting rollups for issued, emailed, redeemed, expired, voided, and outstanding credit liability.
* [ ] **CAN-EC-04** Event Credit redemption polish: add clearer customer-facing invalid-coupon copy, operator-configurable transferability/email restriction, and optional per-event eligibility controls.

* [x] **CAN-EC-01A** Event Credit fixed-cart coupon guardrail repair: implemented/reconciled in `0.2.24.565` after live Codex testing showed a fixed-cart Event Credit coupon could be accepted on an unrelated cart. Added cart-level/coupon-validity checks so Event Credit coupons only validate when the cart contains an eligible future event product.

---

## 23) Agreements / Proposals / Booking Terms

* [ ] **AGREE-01** Core booking terms foundation: add shared core data model for cancellation policy profiles, Event Plan booking-term snapshots, clause status/bypass records, deposit terms, rider status, proposal acknowledgement audit records, and no-show/nonperformance review records. Canonical planning authority: `docs/12-agreement-contract-roadmap.md`.
* [ ] **AGREE-02** Cancellation & Event Viability policy profiles: support safety/weather/force-majeure, venue operational conditions, event viability thresholds, vendor no-show/nonperformance, and mutual written agreement categories. Business viability must be named directly as Event Viability, not hidden inside force majeure.
* [ ] **AGREE-03** Proposal-visible plain-English terms walkthrough: show key terms before vendor acceptance, including compensation, deposit, production provided, cancellation/viability, reschedule preference, nonperformance/no-show, rider rules, guest list/comps, and promo expectations. Acknowledgements live in the proposal rather than cluttering the final agreement.
* [ ] **AGREE-04** Proposal acknowledgement receipt: next to each acknowledgement, visibly print recorded details back to the vendor, including timestamp/timezone, vendor/contact identity, IP address, proposal version, and acknowledgement ID. Accepted proposal exports should include a full acknowledgement receipt page.
* [ ] **AGREE-05** Clause bypass / alternate terms guardrails: allow event viability/cancellation clauses to be included, modified, bypassed, replaced, or marked not applicable, but require permission checks, reason capture, timestamp, approver, internal warnings, event snapshot, and version history. Do not silently remove clauses.
* [~] **AGREE-06** Deposit support in core compensation: 0.2.24.567 added event-level deposit amount, status, treatment, due date, paid date, notes, canonical meta keys, compensation hash coverage, and Locked Pay snapshot capture. Remaining follow-up: cancellation/no-show treatment language, vendor defaults if desired, and proposal/agreement rendering.
* [ ] **AGREE-07** Vendor portal rider uploads: add separate Technical Rider and Hospitality Rider upload lanes with statuses of no rider, requested, uploaded, under review, approved, approved with exceptions, rejected, and superseded. Vendor-facing copy must state uploads are requests unless accepted by the venue in writing.
* [ ] **AGREE-08** No-show / nonperformance review: do not use a default flat penalty fee. Support documented direct costs, reasonable cover costs, payment withholding where applicable, future booking consequences, operator documentation, and formal notice workflow.
* [ ] **AGREE-09** Premium agreement add-on: build `vmsx-agreements` after the core facts exist. It should generate agreement packets/PDFs, store signed-copy metadata, keep agreement version history, and provide a manual DocuSign handoff before any full e-sign API integration.

Private post-event feedback module MVP shipped in 0.2.24.604 and Email Follow-Ups integration shipped in 0.2.24.605: post-event thank-you emails can include an event-specific private feedback link, feedback submissions are logged back into Email Follow-Ups, and Event Feedback admin links to the post-event email preview. Future enhancements: deeper attendee tokenization/status dashboards, long-term trend reports, curated anonymous vendor summaries, and season/vendor scorecards.
