# VMS 0.2.24.554 Test Plan — Guest Pass Theme + Mojibake Hotfix

## Scope
- Guest Pass claim pages render through the active WordPress theme header/footer.
- Guest Pass claim page styling is loaded from a CSS file, not inline styles.
- Guest Pass batch preview/generated UI no longer uses dash glyphs that can render as mojibake.
- Empty preview values display as “Not set”.

## Smoke Tests
1. Open **VMS → Guest Passes → Batches**.
2. Create or preview a Guest Pass batch.
3. Confirm the preview summary does not show `â€`, `â€”`, or similar mojibake.
4. Commit the batch.
5. Confirm the generated batch line reads normally.
6. Open one claim URL in an incognito window.
7. Confirm the page uses the site header/footer/theme shell.
8. Confirm the claim form is still readable on desktop and mobile widths.
9. Claim a pass and confirm the gate pass confirmation still appears.

## Regression Checks
- Existing Guest Pass links still resolve.
- VMS admin tabs still load.
- No PHP fatal on Guest Passes page.
