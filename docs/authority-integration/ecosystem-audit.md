# Remaining ecosystem source audit — 2026-09-06

## Scope and evidence standard

Read-only audit of Wave 4 at commit `1e53fa3e4afc4301ff9c5b912df1a4bfc83f7444`. Core source root (C): `/Users/treyconey/Local Sites/serenade-range-local-test-site/app/bvm-local-checkpoints/wave4-20260906/bvm`. Installed companion source root (P): `/Users/treyconey/Local Sites/serenade-range-local-test-site/app/public/wp-content/plugins`. Paths below expand against those exact roots. Installed sources were read, never loaded into WordPress or changed. This is an architecture/source audit, not proof of activation, configured delivery, actual operational use, end-to-end correctness, or production readiness.

Required preflight ran in the canonical original worktree. It reported the expected original dirty-tree warning, with the protected stash present and diff check clean. User instructions explicitly prohibit editing that original dirt; none was edited. C had clean status and the exact accepted HEAD. Workflow and remediation ledger were inspected as contextual evidence; historical PASS statements were not treated as current runtime verification. A worktree inventory contained no ecosystem-audit worktree. This document is the isolated read-only audit output; integration coordinator owns any durable documentation commit. No branches, worktrees, stashes, sources, databases, or runtime state were modified by this audit worker.

“ACTIVE / CANONICAL” below describes a wired canonical source path. Installed activation and usage remain unverified. “CONNECTED BUT UX-POOR” is a source-supported integration assessment with a UX hypothesis requiring later browser review. No component is called retired solely because it was not observed running. No source was deleted. Reconciled Weather, Commerce, Bridge, Sponsorships, Calendar, and Data Tools implementations are deliberately excluded from re-audit.

## Component classification and ranked backlog

| Component | Classification | Concrete source evidence | Remaining work / uncertainty |
|---|---|---|---|
| Agreements 0.3.48 | ACTIVE / CANONICAL; LEGACY COMPATIBILITY hooks | P:`vms-agreements/vms-agreements.php:48-53` registers CPT/default-template upgrades; `includes/vendor-portal.php:6-12` registers canonical portal integrations and older dashboard hooks. `vmsa_register_vendor_portal_tab()`, `vmsa_render_vendor_portal_custom_tab()`, `vmsa_get_vendor_portal_packets()`, `vmsa_partition_vendor_portal_packets()` implement real packet/status/history flows. Companion tests `bvm-public-runtime-compatibility.php` and `vendor-portal-routing-regression.php` exist. | P1: verify event/person linking and agreement status summaries against integrated candidate; preserve history/identity access. P2: ECC should show status/action links, not full packet editor. Signing and user access were not executed. |
| Events Slider 1.0.10 | ACTIVE / CANONICAL; LEGACY COMPATIBILITY shortcode | P:`vms-events-slider/vms-events-slider.php`: `vms_events_slider_core_function()` canonical-name resolver; `vms_events_slider_should_skip_event()` uses cancellation/reschedule visibility; `vms_events_slider_on_post_meta_change()` and save/status hooks bust cache; `vms_events_slider_shortcode()` registered under current and legacy shortcode constants at 1061-1062. | P1: source compatibility exists; add targeted public rendering/current-event-visibility/cache checks in disposable acceptance. Placement/page usage unknown. |
| Fill Dates 0.1.8 | ACTIVE / CANONICAL; CONNECTED BUT UX-POOR | P:`vms-fill-dates/vms-fill-dates.php`: `vms_fd_boot()`, navigation cluster/shell filters. `includes/helpers.php`: `vms_fd_vendor_availability()`, `vms_fd_build_busy_map()`, `vms_fd_upsert_secondary_vendor_ids()`, `vms_fd_set_primary_vendor()`, `vms_fd_plan_review_state()`. | P1: preserve canonical vendor-availability parity and booking review. P2: clarify Fill Dates versus Availability / Date Dispatch handoff; they are different discovery and invitation jobs, not proven duplicate implementations. No live bookings tested. |
| Investor Portal 0.2.3 installed | CONNECTED BUT UX-POOR; architectural debt | P:`vms-investor-portal/includes/class-vms-investor-metrics-provider.php`: `VMS_Investor_Metrics_Provider::get_event_metrics()` marks Data Tools labor overhead and band payout as `auto_actual`; `load_data_tools_reporting_support()` at 1053 loads admin implementation paths even from `WP_PLUGIN_DIR`; adjacent loaders also include core goals/ECC/staffing/admissions implementations. | P0 before future Investor promotion: apply/review separately tested financial branch source patch in isolated source, consume supported reporting/financial authority contracts, keep reported/forecast/committed/actual unavailable distinctions. Installed copy remains unchanged and cannot be described as already fixed. Financial coordinator owns patch validation. |
| Meta Ads 0.1.107 | ACTIVE / CANONICAL; CONNECTED BUT UX-POOR | P:`vms-meta-ads/includes/bootstrap.php`: `VMS_Meta_Ads::init()`; `includes/admin/event-plan-metabox.php`, `includes/services-event-plans.php`, `includes/services-ad-builds.php`, REST ad-build/event-plan controllers, shared marketing assets, Meta client/endpoint and diagnostics modules. | P1: separate configured budget, performance readback, and settled spend in financial provenance; source breadth does not prove live API readiness. P2: ECC marketing summary/deep link. Do not confuse this paid-ad builder with stub organic Social Sharing providers. No external APIs called. |
| Ops Console 0.1.68 | ACTIVE / CANONICAL; LEGACY COMPATIBILITY routing | P:`vms-ops-console-premium/vms-ops-console-premium.php`: `vms_ops_plugin_boot()`, `vms_ops_template_router()`, `vms_ops_render_pwa_shell()`, `vms_ops_redirect_legacy_console()`. `includes/ticket-console/{events,eligibility,checkin}.php`, private-club member/visit/audit modules, REST routes and scanner/checkin-window tests. | P1: test staffing-facing crew roles and current ticket/admission truth with integrated candidate; cross-component Season Pass interception is high-impact. ECC should hand off scanning to this dedicated surface. PWA install, actual device behavior, and role usage unverified. |
| Refer-a-Friend 0.2.6 | ACTIVE / CANONICAL; possible DUPLICATE installed directory | P:`vms-refer-a-friend/vms-refer-a-friend.php` boots `VMS_RAF_Plugin`; `includes/class-vms-raf-plugin.php` registers referral capture/frontend actions, own admin, and BVM admin registry. `includes/class-vms-raf-db.php` owns storage. P also contains `vms-refer-a-friend-0.2.0/`. | P1: attribution/reward/refund source contracts and current activation inventory before promotion; no reward/business actions. P2: determine whether old directory is inactive archive; directory coexistence alone does not prove double activation or duplicate behavior. Keep both untouched. |
| Season Passes 0.1.0 | ACTIVE / CANONICAL | P:`vms-season-passes/vms-season-passes.php`: dependency/version gate, `vms_season_passes_plugins_loaded()`. `includes/scanner.php`: `vms_season_passes_register_scanner_hooks()` intercepts `rest_dispatch_request`; `vms_season_passes_intercept_ops_ticket_checkin()`, `vms_season_passes_handle_ops_ticket_checkin()`; repository/DB/Woo/public/admin files present. | P1: admission entitlement, duplicate scan, cancellation/refund/revocation, event window, and Ops route compatibility tests. Do not count pass check-ins as paid tickets or allocate pass-sale revenue without explicit allocation authority. Runtime use unknown. |
| Checkout Policies 0.1.8 | ACTIVE / CANONICAL; LEGACY COMPATIBILITY resolver | P:`vmsx-checkout-policies/vmsx-checkout-policies.php`: `VMSX_Checkout_Policies` hooks classic checkout validation and Store API/block rendering; `validate_store_api_policy_acceptance()`, `store_order_acceptance_for_store_api()`, `filter_square_wallet_pages()`, fallback Woo settings and core-function resolver. | P1: retain classic/block/wallet acceptance ordering and order metadata checks in contained checkout tests. Financial integration must not bypass consent gates. No order/payment created. |
| Outreach 1.0.0 | ACTIVE / CANONICAL companion; SUPERSEDED embedded ownership | P:`backstage-outreach/includes/integration-bvm.php`: tab/nav filters, `backstage_outreach_public_invite_router()`, claim preflight/context/eligibility/validation/payload filters, `backstage_outreach_record_claim_success()`. Separate contact/suppression/storage and admissions outreach files exist. C:`tests/backstage-outreach-*` and accepted outreach docs retain recovery/stabilization coverage. | P1: verify companion is sole outreach owner and BVM claim extension hooks remain compatible; assert suppression and claim token/idempotence without mail or external communication. No re-embedding into BVM. Live legacy ownership was not executed. |
| Safety core and Safety Pro 0.1.0 | INCOMPLETE / DISCONNECTED | C:`includes/bootstrap.php:37-39` explicitly says `includes/safety/*` is a source-only prototype, not bootstrapped and excluded from public release. C safety CPT/caps/private-files/admin/audit source exists. P:`vms-safety-pro/includes/generators.php`: `vms_safety_pro_tab_prereq()` requires `vms_safety_admin_url` and `vms_doc`; generator hooks target `vms_safety_*`, while this core is absent from boot. | P2: decide whether to productize this separate document flow or supersede it with Agreements; first establish dependency/version/canonical naming contracts. Do not label complete/dormant simply because generator forms exist. No legal/document sufficiency conclusion. |
| Payouts scaffold | INCOMPLETE; named runtime module UNKNOWN | C:`includes/core/registry/constants.php:169` has commented `VMS_CAP_MANAGE_PAYOUTS`; C:`includes/admin/continuity-binder.php:72` payout checklist says to record status externally “until payout tracking is implemented.” Search of tracked C source found no payout module file. Active `payables.php` is an export calculation service. | P0 product wording: no final/paid accounting assertions inferred from export data. P2: explicit payments ledger/product decision required before payout implementation. A separate dormant payouts tree was not located; do not invent or delete one. |
| Availability / Date Dispatch | ACTIVE / CANONICAL, configurable | C:`includes/modules/load.php`: `bvmgr_core_module_toggle_enabled()` maps `availability_date_dispatch_enabled` (default true), and `bvmgr_load_modules()` includes module when enabled. Module boot loads DB/helpers/admin/public/email. | P1: idempotent invitations, vendor availability update, assignment-review integration. P2: one discoverable handoff from Fill Dates and vendor portal; settings/actual usage unknown. Mail delivery untested. |
| Staff Tasks | ACTIVE / CANONICAL; lifecycle integration watchpoint | C:`includes/modules/staff-tasks/staff-tasks.php`: `bvmgr_staff_tasks_module_boot()`. `generator.php`: `bvmgr_tasks_resolve_assignment_for_instance()` routes `scheduled_role`; `bvmgr_tasks_generate_for_event()`, queued/nightly generation; line 1049 subscribes `vms_staffing_event_saved` to `bvmgr_tasks_resolve_assignments_for_event()`. `store.php` has open/done/skipped/canceled/superseded task states. | P1 integration: lifecycle mutations must continue emitting event hook; scheduled-role selection and task reassignment must exclude declined/canceled assignments and define proposed eligibility. Do not equate task completion with hours worked/paid labor. Hook continuity alone is not proof of correct lifecycle result. |
| Status Notices | ACTIVE / CANONICAL | C:`includes/modules/status-notices/status-notices.php`: `bvmgr_status_notices_module_boot()`; `front.php`: `bvmgr_status_notice_runtime_context_front()`, admin context, prepare/enqueue notice callbacks on frontend and admin. CPT/default/store/admin files exist. | P2: audit targeting precedence and avoid duplicating ECC event alerts with global notices. Actual configured notices unknown. |
| Email Follow-Ups | ACTIVE / CANONICAL, context-gated | C:`includes/modules/email-followups/email-followups.php` registers module and exits on public frontend before loading settings/templates/MailPoet/recipients/sender/scheduler. `scheduler.php`: `bvmgr_email_followups_schedule_cron()`, `bvmgr_email_followups_due_items()`, `bvmgr_email_followups_cron_run()`. | P1: duplicate-send/cron-context/consent and cancellation-reschedule suppression in disposable transport. P2: distinguish routine buyer reminders from Outreach acquisition and operational Event Communications. No email sent. |
| Event Communications | ACTIVE / CANONICAL | C:`includes/core/load.php` and `includes/admin/load.php` load core/admin communication files. Core: `bvmgr_event_communication_build_ledger()`, `persist_for_operation()`, `verify_audience()`, `mutate_recipient()`, `send_recipient()`, `send_bulk()`, `mark_manual_bulk()`, `bootstrap_apply()`. | P1: staffing lifecycle audience freshness when rescheduling/canceling; preserve operation snapshots and delivery audit. P2: ECC unresolved-recipient summary and deep link rather than dense message configuration. No communication executed. |
| Social Sharing | INCOMPLETE overall; active queue infrastructure | C:`includes/bootstrap.php:35` loads `social-share/load.php`; queue repo/runner, encryption, audit, templates, admin/Event Plan panel, mock/webhook/Meta/LinkedIn registry are wired. `class-provider-meta.php:52` and LinkedIn equivalent explicitly warn provider stub; `publish()`/error classifications return stub behavior. | P1 UX: do not represent Meta/LinkedIn organic publishing as ready; display capability readiness. P2: choose supported provider completion; queued webhook/mock infrastructure does not make the native providers complete. No publish/OAuth/network executed. |
| Goals / payables remnants | ACTIVE / CANONICAL computation; LEGACY COMPATIBILITY concepts | C:`includes/core/load.php` loads goals and payables; `bvmgr_payables_build_bills_for_export()` computes export bills, payment/tax blocking and terms; no settlement ledger. Integrated financial work changes interpretation; historical Wave4 values cannot automatically be called actual. | P0 handled by integration semantics; retain distinct forecast/direct-cost/receipt/report labels. Historical fixture absence must remain explicit validation debt until identified, never fabricated. Core authority work should not silently retire useful export service. |
| Vendor / Staff portals | ACTIVE / CANONICAL; CONNECTED BUT UX-POOR hypothesis | C:`includes/portal/load.php` loads only live vendor/staff portal surfaces. Staff: `bvmgr_staff_portal_shortcode()`, `bvmgr_staff_portal_assignment_status_label()`, `bvmgr_staff_portal_assignment_event_payload()`, document role/assignment visibility and shift glance. Vendor portal has profile/tech/tax companions; admission vendor-guest portal separate; Agreements extends portal through hooks. | P1: lifecycle read/actions/visibility parity and financial basis checks, especially future dates/proposed versus confirmed. P2: keep role-specific work and private documents here, summarize at ECC. No actual user session or access checks executed in this audit. |

## Other first-party components located

| Component | Classification and evidence | Follow-up |
|---|---|---|
| Express Bar 0.6.24 | ACTIVE / CANONICAL standalone; SUPERSEDED core embedding. C bootstrap explicitly identifies move to standalone. P:`vms-express-bar/includes/{bootstrap,public,cache,admin,helpers}.php`; compatibility/cache/admin hook tests present. C:`includes/public/express-bar.php` remains source inventory. | P2 confirm single runtime ownership/public cache and preserve legacy source until references proven unused. |
| Event Venue Map Modal 1.2.4 | ACTIVE / CANONICAL standalone source: P:`event-venue-map-modal/event-venue-map-modal.php`, `Event_Venue_Map_Modal::inject_map_card_into_event_content()`, `render_modal_markup()`, `should_inject_into_content()`. | P2 map/event placement and public accessibility; not a financial or staffing authority. Actual use unknown. |
| SR Events CSV Importer 0.7.4 | CONNECTED; potential DUPLICATE authority, not proven superseded. P:`sr-events-csv-importer/sr-events-csv-importer.php`, `SR_Events_CSV_Importer::cron_import()`, `import_events_from_csv()`, `import_tickets_from_csv()`, fire-pit checkout validation and quantity discount helper. | P1 determine enabled cron/options and ownership boundary with accepted import/ticket/discount providers using isolated copied state; no automatic migration/deletion. Risk is multiple writers, not naming alone. |
| SR Image Reel 0.2.0 | ACTIVE / CANONICAL standalone source: P:`sr-image-reel/sr-image-reel.php`, `sr_image_reel_shortcode()`, saved footer renderer/enqueue callbacks. | P3 presentation-only placement/assets/accessibility audit if retained. Usage unknown. |
| SR TEC Custom CSS 1.0.0 | ACTIVE / CANONICAL standalone source: P:`sr-tec-custom-css/sr-tec-custom-css.php`, `sr_tec_custom_css_should_load()`, `sr_tec_custom_css_enqueue()`. | P3 public style compatibility; avoid broad restyling during integration. |
| Serenade Facebook Live Embed 1.0.0 | CONNECTED source; runtime readiness UNKNOWN: P:`serenade-fb-live-embed/serenade-fb-live-embed.php`, `serenade_fb_live_get_latest_video_url()`, `serenade_fb_live_shortcode_cb()`. | P3 provider/credentials/cache readiness only in later explicitly contained audit; never expose tokens or call network here. |
| Admissions / ticket-integrity helpers / public calendar / event feedback / vendor applications / due dates / lineup and review | ACTIVE / CANONICAL core families: C modules admissions loader, `core/load.php`, `integrations/load.php`, `public/load.php` provide explicit entry points; named service files in those loaders. | P1 preserve ticket/admission authoritative current truth; P2 map event feedback, lineup, due dates and vendor onboarding to dedicated screens/ECC summaries. This audit inspected loader connectivity, not every callback in these broad core domains. |
| Legacy module loader and public shortcodes | LEGACY COMPATIBILITY: C:`includes/modules/loader.php` explicitly requires canonical `load.php`; C:`includes/public/legacy-shortcodes.php`. | Preserve compatibility until a separately authorized usage/deprecation review. |
| ZIP archives, deployment/rollback/temp directories, old RAF directory, vms/backstage-venue-manager naming copies | UNKNOWN or rollback/deployment artifact; not independent authoritative development components. Directory inventory is evidence of copies, not license to reconcile/delete them. | Coordinator preservation receipts govern; no archive extraction or mutation performed here. |

Third-party WooCommerce, TEC/Event Tickets, MailPoet, Rank Math, Turnstile, role editor, Plugin Check, and other externally authored installed plugins are dependencies, not reclassified first-party products. DRM Intake/Router are accepted associated components and were not re-audited as new backlog. No unobserved archived-only product was assumed retired.

## Recommended ordering and acceptance criteria

1. **P0 integrated authority acceptance:** prove planned/committed/actual labor, unavailable/error cases, manual versus transactional revenue, and same Event Plan/ECC basis. Keep installed Investor coupling listed until a source patch is separately promoted. Do not imply payouts or settlement exist.
2. **P1 lifecycle consumers:** Staff Tasks scheduled-role reassignment, portal visibility/actions, Ops crew context, and communication audiences. Prove proposed/confirmed/declined/canceled behavior with value and provenance, not only hook presence.
3. **P1 ticket/public commerce boundaries:** Season Pass/Ops scans, RAF attribution/refunds, Checkout Policies classic/block/wallet gates, Slider visibility/cache, and any independently enabled CSV importer writers. Use disposable transport and databases only.
4. **P1 communication and companion continuity:** Agreements identity/status, Outreach single ownership/suppression, Email Follow-Ups idempotence/context. Tests must never send or publish externally.
5. **P2 workflow consolidation:** establish one discoverable path for Fill Dates → invitation/availability → booking review, clear communication product names, ECC links to owned screens, and role-specific portal workflows.
6. **P2 incomplete product decision:** explicit decision to complete or supersede Safety, Social native providers, and payout tracking. No feature is deleted as part of this audit.
7. **P3 presentation inventory:** map/reel/CSS/live embed, actual shortcode placements, dependency readiness, and archival duplicate inventory with an explicitly authorized future cleanup scope.

## Limitations

No normal-local DB queries, activation reads, browser execution, API requests, source changes, email, payments, deployments, or packaging occurred. Source evidence supports connectivity and explicit incompleteness, but not active runtime use or flawless UX. The integrated candidate and Investor patch may improve some findings after this Wave4 source snapshot; coordinator should annotate those separately rather than overwrite the unchanged installed-source findings. No new automated tests were run for this documentation-only workstream. C clean status/HEAD was checked; the audit artifact is the only file this worker created.
