Tracker format (as of **2026-03-04**, America/Chicago)

---

## 1) Critical Bugs / Regressions

* [x] **BUG-37** Qualified/credential ticket duplicate-email/self-allowance blockers ignored multi-ticket allowance — resolved across `0.2.24.602` and `0.2.24.603` by allowing the same approved assignee email to validate multiple seats up to that account's effective event allowance while preserving prior-purchase/cart consumption and over-limit blocking.
* [x] **BUG-01** Event Plans stuck in “Draft” (status not advancing correctly) — server stale-check run on 2026-02-15 returned PASS after hardening deploy: Draft+TEC sampled=1, actionable mismatches=0, intentional drafts (wp_status=draft)=1, invalid TEC links=0
* [x] **BUG-02** Add Event Plan from Schedule: date transfer restored deterministically by routing Schedule Create/Add through `admin-post` add-handler (date+venue written before redirect); server stale-check run on 2026-02-16 returned PASS (PASS=1, WARN=0, MANUAL=0); Playwright fixture added (`tests/e2e/bug02-schedule-add-date-transfer.spec.ts`) and executed with SKIP on current BookLiveTalent snapshot because no schedulable Create/Add links were available
* [x] **BUG-03** Event Plan times revert after saving Draft (values not persisting) — stale-check server run on 2026-02-15 returned PASS and manual Draft-mode verification confirmed time persists
* [x] **BUG-04** Pay-difference acknowledgment checkboxes duplicated and do not persist
* [x] **BUG-05** Holiday pay not applying (holiday shows, pay rate does not) — deterministic holiday-aware default resolver wired across Event Plan render/save/AJAX paths (`vms_get_event_plan_effective_comp_default`); server stale-check run on 2026-02-16 returned PASS (PASS=1, WARN=0, MANUAL=0); Playwright fixture `tests/e2e/bug05-holiday-pay-application.spec.ts` executed against BookLiveTalent on 2026-02-16 returned PASS (1 passed)
* [x] **BUG-06** Venue publish validation false-negative (Saturday-only Weekly Open Day fails publish) — server stale-check run on 2026-02-16 returned PASS after hardening deploy: Season-aware publish validation guard present and Saturday-only fixture passed for bitmask + legacy-array `days_w` forms (PASS=1, WARN=0, MANUAL=0)
* [x] **BUG-07** Schedule default view with single venue shows blank until “All Venues” clicked — Schedule now resolves single-venue default from canonical VMS venue candidates (`vms_sch_get_schedule_venue_candidates` + `vms_sch_pick_single_venue_candidate`) and persists selected venue before render; stale-check BUG-07 guard upgraded from marker-only scan to deterministic helper-wiring + fixture verification; server stale-check run on 2026-02-16 returned PASS (PASS=1, WARN=0, MANUAL=0), and user confirmed Schedule renders correctly in a true single-venue environment without requiring an “All Venues” click; Playwright fixture added (`tests/e2e/bug07-schedule-single-venue-default-render.spec.ts`) and executed with SKIP on current BookLiveTalent snapshot because that environment has 2 venues (single-venue precondition not met)
* [x] **BUG-08** Ticket flow mobile: post-add buttons off-screen, cannot scroll to “save & continue” / “checkout” — front bundle now hydrates mobile modal reachability classes on `#tribe-tickets__modal-form` open (`vms-ticketing-modal-scroll`, `vms-ticketing-modal-form`, `vms-ticketing-modal-footer`) and CSS enforces mobile scroll host + sticky wrapped footer actions + summary height cap (`vms/assets/vms-ticketing-front.js`, `vms/assets/css/vms-ticketing-front.css`); stale-check BUG-08 guard added and returned PASS in local code probe; server stale-check run on 2026-02-16 returned PASS (PASS=1, WARN=0, MANUAL=0); Playwright fixture added (`tests/e2e/bug08-ticket-flow-mobile-actions.spec.ts`) and executed with SKIP on current BookLiveTalent snapshot because no ticket-enabled event/modal was available; user confirmed BUG-08 behavior is resolved
* [x] **BUG-09** Ticket UI: entitlement +/- broken until user manually types “1” — implementation + local verification completed on 2026-02-16 (stale-check local code probe PASS=1/WARN=0/MANUAL=0; Playwright fixture `tests/e2e/bug09-ticket-ui-entitlement-stepper.spec.ts` PASS=1 with live-event attempt + deterministic harness fallback); canonical server validation completed on 2026-02-16 with `wp vms stale-check --bugs=BUG-09` returning PASS (PASS=1, WARN=0, MANUAL=0).
* [x] **BUG-10** Stale Woo notice appears on unrelated pages after cart emptied (“Table #01 added…”) — hardening implemented on 2026-02-16: success-only Woo notice pruning + cart-empty stale-notice guard + front URL hygiene (`vms/includes/integrations/ticketing-rules-v2.php`, `vms/assets/vms-ticketing-front.js`), with formal stale-check BUG-10 probe + runtime notice fixture added (`vms/includes/core/cli/stale-check.php`); local verification passed (`npm run lint:php`, `php -l` on touched PHP files, `node --check vms/assets/vms-ticketing-front.js`); canonical server validation completed on 2026-02-16 with `wp vms stale-check --bugs=BUG-10` returning PASS (PASS=1, WARN=0, MANUAL=0), with probe note that runtime fixture was skipped in that CLI context because Woo session/notices were unavailable and static guard markers were evaluated.
* [x] **BUG-11** Event Plan featured image inheritance regression: editing plan can overwrite event image with vendor image — resolver is centralized (`vms_tec_resolve_featured_image_arg`) so plan image wins, linked TEC image is preserved, and vendor image is fallback-only when both plan+TEC images are missing (`vms/includes/cpt/event-plans.php`); stale-check BUG-11 guard + resolver fixture added and verified via local code probe on 2026-02-16 (PASS=1, WARN=0, MANUAL=0); canonical server validation completed on 2026-02-16 with `wp vms stale-check --bugs=BUG-11` returning PASS (PASS=1, WARN=0, MANUAL=0).
* [x] **BUG-12** Non-WP action buttons trigger browser “leave site” prompt; bypass prompt for guided actions and autosave Event Plan edits before continuing where possible
* [x] **BUG-13** Link existing TEC event can collapse advanced UI and/or wipe unsaved Event Plan fields; linking flow should preserve in-progress edits and guard advanced controls until a draft exists — fixed with stable-draft gating + unsaved field snapshot/restore across link-sensitive reload actions; validated by harness run on 2026-02-16 (`tests/e2e/bug13-link-existing-flow.spec.ts`, pass on BookLiveTalent test site)
* [x] **BUG-14** VMS-wide timezone/format drift: Meta Ads builder + service datetime parsing/rendering now enforce deterministic WP-timezone semantics (localized site date/time injected at enqueue, strict local datetime parsing in builder JS, WP date/time format labels in server copy/status paths, no implicit browser/UTC date query fallback); validated 2026-02-23 by lint (`php -l`, `node --check`) and Playwright probe PASS (`tests/e2e/bug14-meta-ads-timezone.spec.ts`, `1 passed`)
* [x] **BUG-15** Meta Ads Builder tour flow/placement regression: Step C (Creative Mode) popup could obstruct target controls and, on new ad runs, tour could collapse to a single popup (`1 of 1`) instead of full multi-step guidance; fixed 2026-02-23 by Step C anchor/placement hardening in `vms-meta-ads/includes/tours/registry.php` + mirrored `vms/vms-meta-ads/includes/tours/registry.php` and by removing locked-step skip collapse in `vms-meta-ads/assets/tours.js`; operator verified full multi-step tour behavior restored.
* [x] **BUG-16** Meta Ads Builder Export Copy Pack action appears non-functional: resolved 2026-02-24 with Step F/Step G export affordance and feedback polish (`vms-meta-ads/includes/admin/screens/ads-builder.php`, `vms-meta-ads/assets/ads-builder.js`, `vms-meta-ads/assets/admin.css`) so operators have explicit copy/download actions, visible output panel state, and clearer action feedback.
* [x] **BUG-17** Meta Ads Builder `Save Settings as Template` action appears non-functional on `page=vms-ma-ads-builder`: fixed 2026-02-23 by removing duplicate footer save button ID collision (`vms-ma-save-template-footer`) and binding all template-save triggers through shared selector wiring in `vms-meta-ads/assets/ads-builder.js`; operator verified behavior restored.
* [x] **BUG-18** Legacy `vmsx-ticket-discounts` activation/runtime fatal (deprecated plugin): root cause reproduced on 2026-02-25 as unguarded `set_price()` call on malformed cart row data (`Call to a member function set_price() on false` in `vmsx-ticket-discounts/includes/class-vmsx-td-engine.php`); source hardening applied in `0.1.4` with cart-item product guards + safe hook wrappers + bootstrap try/catch logging (`vmsx-ticket-discounts/vmsx-ticket-discounts.php`, `vmsx-ticket-discounts/includes/class-vmsx-td-engine.php`). Stabilization-gate disposition on 2026-02-28: closed as deprecated/retired path in this workspace (legacy plugin directory not present under active plugins list), with active runtime replacement confirmed as `vms-commerce-discounts` (`vms-commerce-discounts/vms-commerce-discounts.php`, `Version: 0.1.2`); future live discount verification remains tracked on the active replacement plugin.
* [x] **BUG-19** Mobile revenue blocker: `Get Tickets` CTA could become non-functional when front-end heuristic qty sync force-disabled CTA state on markup variants where mobile qty state reports late/inconsistently. Hotfix shipped 2026-03-01 in canonical `assets/vms-ticketing-front.js` by (1) broadening CTA selector coverage to include non-button `.tribe-tickets__tickets-buy` elements and (2) removing heuristic force-disable behavior so the helper only force-enables and never force-disables checkout CTA. Verification: `node --check` PASS on canonical bundle; Playwright `tests/e2e/bug09-ticket-ui-entitlement-stepper.spec.ts` PASS=3; `tests/e2e/bug08-ticket-flow-mobile-actions.spec.ts` SKIP=1 (ticket-enabled modal unavailable in this environment snapshot). Path hygiene follow-up on 2026-03-01 converted legacy `assets/js/vms-ticketing-front.js` into a compatibility shim that forwards to the canonical bundle.
* [x] **BUG-20** Ticket UI v2 Safari mixed-flow handoff regression: fixed in `0.2.24.137` by preserving V2 architecture while hardening lifecycle (pending payload fingerprint + single-flight guards, timeout/abort recovery, BFCache pageshow cleanup), converging event-page actions to a single cart-first CTA (`Add items to cart`), simplifying modal to cart-only action (`View Cart`), and removing hardcoded `GA` public copy in favor of qualifying-ticket-aware language (`vms/assets/vms-ticketing-front.js`, `vms/includes/integrations/ticketing-rules-v2.php`, `vms/assets/css/vms-ticketing-front.css`). Verification: syntax checks PASS (`php -l`, `node --check`), and Playwright regression run PASS on 2026-03-06 (`tests/e2e/bug09-ticket-ui-entitlement-stepper.spec.ts` + `tests/e2e/bug15-ticket-addon-safari-handoff.spec.ts`, `12 passed` Chromium).
* [x] **BUG-21** Ticket UI v2 mobile sticky CTA footprint regression: sticky add-to-cart area was too tall and blocked content on smaller screens; compact button-first sticky bar behavior confirmed resolved by user in follow-up (2026-03-06).
* [x] **BUG-22** Ticketing Phase B entitlement creation failure on new events: preview showed `ENTITLEMENT: CREATE "<name>" — Will create a new entitlement product.`, but commit sync failed with `vms_ticketing_v2_pick_entitlement_product_by_sku(): Argument #1 ($sku) must be of type string, null given` (reported call site: `includes/integrations/ticketing-phase-b.php` line 3785). Fixed in `0.2.24.148` by defining deterministic entitlement SKU before SKU-based adoption lookup inside `vms_ticketing_v2_upsert_entitlement_product()` and passing a guaranteed string SKU; syntax validation PASS (`php -l includes/integrations/ticketing-phase-b.php`).
* [x] **BUG-23** Ops Console single-venue auto-default remains unconfirmed: resolved on 2026-03-13 by narrowing the Ops runtime venue payload/default path to published non-template venues only in `vms-ops-console-premium/includes/venues.php` and `vms-ops-console-premium/includes/rest/routes.php`. Root cause: the existing single-venue UI logic was already correct, but the bootstrap payload still counted `draft`/`private`/`pending`/`future` venues as active, so a true one-published-venue environment could still receive a multi-venue payload and show the redundant selector. Local validation against a true one-active-venue condition: `Test Venue` was temporarily toggled from `publish` to `draft`, pre-fix probe reproduced the bug (`/wp-json/vms-ops/v1/bootstrap` still returned both venues and the header picker remained visible), post-fix probe returned only `Serenade Range` and both the header picker and blocking overlay selector stayed hidden, then `Test Venue` was restored to `publish`. Verification: `php -l vms-ops-console-premium/includes/venues.php`, `php -l vms-ops-console-premium/includes/rest/routes.php`, Playwright admin smoke PASS, targeted local `/vms-ops/` probe PASS before/after fix.
* [x] **BUG-24** Event Plan secondary vendor type does not appear to persist on the local site — resolved on 2026-03-13 by updating `vms/includes/integrations/ticketing-claims-admin.php` so auxiliary Event Claims lookup/filter GET forms submit directly to the canonical Event Plan edit URL without hidden `post` / `action` inputs; this prevents duplicate `action=edit` values from overriding WordPress’s `action=editpost` save path and restores `_vms_secondary_vendor_type` persistence. Local verification: real browser submit persisted the secondary vendor type and a cancellation-note probe after reload, and `php -l` passed for `vms/includes/integrations/ticketing-claims-admin.php` and `vms/includes/cpt/event-plans.php`.
* [x] **BUG-25** Tickets/entitlements not saving on newly created Event Plan — resolved on 2026-03-13 by canonicalizing the Ticketing v2 dirty-check comparison in `vms/assets/admin-ticketing.js` so Preview-vs-current comparisons ignore server-normalized hidden/defaulted fields that the editor cannot represent. This removes the false `Your Ticketing config has changed since the last Preview. Click “Preview sync” again before committing.` block on fresh Event Plans. Local comparison against live-expected behavior: a direct fresh-plan Preview -> Commit probe after the fix created the linked TEC event, GA ticket, and entitlement successfully (`preview_elapsed_ms=17`, `commit_elapsed_ms=75`), confirming no remaining newly created Event Plan server-side save failure once the stale client guard is cleared. Verification: `node --check vms/assets/admin-ticketing.js`, `php -l vms/includes/integrations/ticketing-phase-b.php`.
* [x] **BUG-26** Event Plan ticket quantity `0` acts like unlimited inventory instead of sold out/unavailable. Closed on 2026-04-02 by explicit user confirmation after the Ticket Integrity inventory repair/root-cause passes in core `vms`; this wrap records the resolution so the tracker and handoff stop treating it as open. Reopen only if `Qty available = 0` is reproduced again as a sellable/unmanaged ticket state.
* [x] **BUG-27** VMS Commerce Discounts incorrectly applies discount to comped/free tickets. Resolved on 2026-04-02 in standalone `vms-commerce-discounts` through builds `0.2.2` -> `0.2.4`: qualification now rejects true zero-dollar Woo cart lines, zero-priced quantities no longer count toward `ticket_qty` / `combo_qty` / `product_group_qty`, positive-priced math recalculates from the restored original unit price instead of Woo's already-mutated discounted subtotal, and tax-inclusive catalogs use the correct gross cart basis. Verification: `php -l` PASS on touched discount plugin files, local cart/config probe PASS, and explicit user cart regression testing against the `$18.18` / `$1.82` failures.
* [x] **BUG-28** Meta Ads Builder template-save/name mismatch on the live builder surface. Resolved on 2026-03-31 in standalone `vms-meta-ads` by normalizing template API rows to expose the saved DB field as both `template_name` and `name` in `vms-meta-ads/includes/services-templates.php`, and by making the builder dropdown label fallback honor either response key in `vms-meta-ads/assets/ads-builder.js`. Root cause: the template service stored/returned `template_name`, while the builder list renderer only looked for `item.name`, so saved templates fell back to generic `Template <id>` labels instead of the operator-entered name. Verification: `php -l vms-meta-ads/includes/services-templates.php`, `node --check vms-meta-ads/assets/ads-builder.js`.
* [x] **BUG-29** Meta Ads Builder `Create in Meta` fatal in `build_manual_targeting()`. Resolved on 2026-03-31 in standalone `vms-meta-ads` by defining `$build_id` in `vms-meta-ads/includes/meta-api/client.php` before passing it into `build_manual_targeting(..., int $build_id)`. Root cause: `preflight_create_paused()` called `build_manual_targeting()` with an undefined `$build_id`, so PHP promoted the missing value to `null` and threw a type error on the callee signature during the REST `meta-create` path. Verification: `php -l vms-meta-ads/includes/meta-api/client.php`.
* [x] **BUG-30** Meta Ads Builder `Create in Meta` ad set create fails with Meta error `Advantage Audience Flag Required` (`error_subcode: 1870227`). Resolved on 2026-03-31 in standalone `vms-meta-ads` by explicitly sending `targeting_automation.advantage_audience = 0` in the ad set targeting spec built by `vms-meta-ads/includes/meta-api/client.php`. Root cause: newer Meta ad set create rules now require the audience-automation flag to be explicitly enabled or disabled in the targeting spec, but the builder was sending only manual age/geo targeting with no `targeting_automation` field. Verification: `php -l vms-meta-ads/includes/meta-api/client.php`.
* [x] **BUG-31** Meta Ads Builder schedule timezone drift when the Meta ad account uses a different timezone than the WordPress site. Resolved on 2026-03-31 in standalone `vms-meta-ads` by adding explicit Meta ad account timezone settings/detection in `vms-meta-ads/includes/utils.php`, `vms-meta-ads/includes/admin/screens/settings.php`, and `vms-meta-ads/includes/meta-api/client.php`, then using that timezone when formatting outgoing Meta schedule timestamps and schedule preflight comparisons. Root cause: the create/go-live path treated stored builder datetimes as WordPress-site timezone only, even when the connected Meta account operated in a different timezone (for example PDT). Verification: `php -l vms-meta-ads/includes/utils.php`, `php -l vms-meta-ads/includes/admin/screens/settings.php`, `php -l vms-meta-ads/includes/meta-api/client.php`.
* [x] **BUG-32** Meta Ads Builder radius targeting can silently widen to `United States` because the builder sends a radius with no deterministic venue/location anchor. Resolved on 2026-03-31 in standalone `vms-meta-ads` by deriving Event Plan venue geo/address context in `vms-meta-ads/includes/services-event-plans.php`, surfacing that context to builder prefill in `vms-meta-ads/includes/services-prefill.php` and `vms-meta-ads/assets/ads-builder.js`, lifting the builder radius UI to the configured max in `vms-meta-ads/includes/admin/screens/ads-builder.php`, and changing Meta targeting creation in `vms-meta-ads/includes/meta-api/client.php` to use `geo_locations.custom_locations` with venue/address anchor data instead of falling back to country-wide `US`. Root cause: when the freeform locations box was blank, the API create path had no radius anchor and defaulted the ad set to `countries = ['US']`, so a `100` mile audience effectively became nationwide. Verification: `php -l vms-meta-ads/includes/services-event-plans.php`, `php -l vms-meta-ads/includes/services-prefill.php`, `php -l vms-meta-ads/includes/meta-api/client.php`, `node --check vms-meta-ads/assets/ads-builder.js`.
* [x] **BUG-33** Meta Ads Builder dark-post website destination can present as `Facebook Event` instead of `Website` on the Meta side even when a destination URL is supplied. Resolved on 2026-03-31 in standalone `vms-meta-ads` by hardening website-link creative creation in `vms-meta-ads/includes/meta-api/client.php`: dark-post creatives now always send top-level `link_url`, and ticket-intent CTA selection maps builder `BUY_TICKETS` and legacy `BUY_NOW` values to Meta’s website-safe `LEARN_MORE` CTA for the API payload. The builder/save layer in `vms-meta-ads/includes/services-ad-builds.php`, `vms-meta-ads/includes/admin/screens/ads-builder.php`, and `vms-meta-ads/assets/ads-builder.js` now also strips legacy `BUY_NOW` selections so they no longer surface as `Unknown (BUY_NOW)` in Meta. Root cause: the builder default CTA intent drifted away from the original dark-post website-link spec, making the payload more event-like than website-link-specific and allowing an unsupported/undesirable website CTA value to persist into saved drafts. Verification: `php -l vms-meta-ads/includes/meta-api/client.php`, `php -l vms-meta-ads/includes/services-ad-builds.php`, `php -l vms-meta-ads/includes/admin/screens/ads-builder.php`, `node --check vms-meta-ads/assets/ads-builder.js`.
* [x] **BUG-34** Meta Ads Builder Event Plan list can fatal on sites running Events Calendar Pro when venue location context calls `tribe_get_venue_object(..., ARRAY_A)`. Resolved on 2026-03-31 in standalone `vms-meta-ads` by replacing that decorated-object call in `vms-meta-ads/includes/services-event-plans.php` with direct TEC venue meta reads (`_VenueAddress`, `_VenueCity`, `_VenueStateProvince`, `_VenueState`, `_VenueZip`, `_VenueCountry`, `_VenueLat`, `_VenueLng`) plus existing VMS venue fallbacks. Root cause: the new radius-anchor work introduced a `tribe_get_venue_object(..., ARRAY_A)` call, and an Events Calendar Pro model filter on some installs is not compatible with that array-output path, causing the Meta Ads builder screen to fatal before listing Event Plans. Verification: `php -l vms-meta-ads/includes/services-event-plans.php`.
* [x] **BUG-35** Meta Ads Builder automatic radius anchor can mis-center to the wrong place because Step D auto-fills from a generic venue label or city/state instead of a precise venue address, ZIP, or exact coordinates. Resolved on 2026-03-31 in standalone `vms-meta-ads` by tightening venue-context export in `vms-meta-ads/includes/services-event-plans.php` and `vms-meta-ads/includes/services-prefill.php`, switching builder auto-fill in `vms-meta-ads/assets/ads-builder.js` and `vms-meta-ads/includes/admin/screens/ads-builder.php` to exact address/ZIP anchors only, and hardening the Meta create path in `vms-meta-ads/includes/meta-api/client.php` so blank-location fallback only uses a precise anchor and otherwise errors instead of letting Meta guess from a weak label. Root cause: the first radius-anchor pass still allowed auto-targeting to derive from `location_label` values like venue name or generic city/state, which Meta could geocode to an unrelated place. Verification: `php -l vms-meta-ads/includes/services-event-plans.php`, `php -l vms-meta-ads/includes/services-prefill.php`, `php -l vms-meta-ads/includes/admin/screens/ads-builder.php`, `php -l vms-meta-ads/includes/meta-api/client.php`, `node --check vms-meta-ads/assets/ads-builder.js`.
* [x] **BUG-36** Meta Ads Builder lacks a dedicated editable physical targeting-address field, forcing operators to rely on weak venue-name matching or overload the extra-locations box. Resolved on 2026-03-31 in standalone `vms-meta-ads` by adding a dedicated primary targeting address/ZIP field to Step D in `vms-meta-ads/includes/admin/screens/ads-builder.php`, wiring it through save/load/template flows in `vms-meta-ads/assets/ads-builder.js`, `vms-meta-ads/includes/services-ad-builds.php`, and `vms-meta-ads/includes/services-templates.php`, prefilling it from exact VMS/TEC venue address data in `vms-meta-ads/includes/services-event-plans.php`, and making Meta targeting creation in `vms-meta-ads/includes/meta-api/client.php` use that field as the primary radius anchor while rejecting weak labels. Root cause: the builder had no first-class field for the exact address Meta should center on, so the auto-targeting path kept falling back to ambiguous labels and the freeform extra-locations textarea. Verification: `php -l vms-meta-ads/includes/services-event-plans.php`, `php -l vms-meta-ads/includes/services-ad-builds.php`, `php -l vms-meta-ads/includes/services-templates.php`, `php -l vms-meta-ads/includes/admin/screens/ads-builder.php`, `php -l vms-meta-ads/includes/meta-api/client.php`, `node --check vms-meta-ads/assets/ads-builder.js`.

---

* [x] **BUG-38** Qualified buyer self-allowance capped by ticket max-qty — resolved in `0.2.24.603` by separating verified/profile/direct-grant allowance from public ticket max-qty when calculating how many qualified tickets the logged-in buyer can cover without extra guest-email rows.
