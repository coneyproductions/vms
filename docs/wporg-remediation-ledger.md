# WordPress.org Remediation Ledger

Update this ledger only from repository evidence. If evidence is incomplete or uncertain, use `audited` or `blocked` instead of guessing. Historical `PASS` sections, checklist labels, commit subjects, or conversation memory are not sufficient on their own to justify `verified`.

Allowed status values:

- `not started`
- `audited`
- `in progress`
- `implemented`
- `verified`
- `not applicable`
- `blocked`

Status semantics used in this ledger:

- `implemented`: code or test remediation was completed historically and repository evidence supports its existence, but it has not yet been freshly re-certified by the current final audit.
- `verified`: current-tree inspection plus all applicable focused verification commands were freshly run and passed in the current task.
- `audited`: the issue was investigated, but implementation or proof remains incomplete.
- `blocked`: the issue cannot safely proceed without a decision, missing source, or dependency.
- `not started`, `in progress`, and `not applicable` keep their ordinary plain-language meaning.

Original reviewer correspondence status:

- As of `2026-07-15`, the authoritative sanitized reviewer transcription exists locally at `docs/wporg-review-source.md`. Update this ledger from that source plus current repository evidence.

## Imported Evidence Baseline

Historical table rows preserve the counts, status language, and handoffs recorded at their verification dates. Any “current” language in the `WPORG-28`, `WPORG-28Q`, `WPORG-28R`, or `WPORG-28R-G10` rows is checkpoint-local and is superseded by the latest current addendum below.

| Issue ID | Family | Status | Current repository evidence | Focused tests on record | Verification state in this pass | Commit SHA on record | Remaining verification or follow-up | Most recent focused verification on record |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WPORG-17B` | `A` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-17B Result`.<br>Current mirror no longer contains `assets/admin/addons/manifest-addons.json`, `includes/admin/addons/class-vms-admin-addons.php`, or `includes/admin/addons/class-vms-addons-licensing.php`.<br>This correction pass also found no `Premium Add-ons`, `Freemius`, `freemius`, or `license key` matches under `includes/` or `assets/`. | No dedicated current focused test file was located; historical result section only. | Spot-checked current mirror tree only. No fresh closeout audit was rerun, so historical PASS does not retain `verified`. | none imported | Fresh mirror/live re-audit and reviewer-source closeout are still required before submission. | `2026-07-10` |
| `Review-2 Name/Slug Closeout` | `2` | `blocked` | Current repository now uses `Backstage Venue Manager` in `vendor-management-system.php` and `readme.txt`, `vendor-management-system.php` still declares text domain `backstage-venue-manager`, and the current public-release builder plus compatibility harness now produce and validate `backstage-venue-manager/` public artifacts while leaving the internal `VMS_PLUGIN_SLUG='vms'` compatibility identity intact.<br>The completed no-edit audit also found no remaining current mirror matches for `VMS – Venue Management System` or `vms-venue-management-system`, and current live header/readme use the same renamed display name and text domain.<br>Later closeout work under `WPORG-20A-S` and `WPORG-28Q` rebuilt clean packaged Plugin Check evidence, but `WPORG-28R` later reopened submission readiness after rule-family reconciliation found one new packaged warning plus multiple unmapped or still-blocking families.<br>Current repository evidence still does not prove WordPress.org-side slug reservation, corrected upload, or reviewer reply, and must not be read as proof that the directory slug already changed. | `tests/public-release-build-pipeline.php`<br>`tests/release-compatibility-harness.php`<br>`tests/runtime-stub-guards.php` | Current code/documentation implementation plus focused release-harness verification; the later `WPORG-20A-S`, `WPORG-28Q`, and `WPORG-28R` audits supplied fresh packaged Plugin Check reruns and the reopened residual reconciliation, but no external WordPress.org action was authorized or performed in any of those tasks. | none imported | Code-level rename and public package slug separation are now implemented, but explicit slug reservation, corrected upload through the "Add your plugin" page, the short reviewer reply, and resolution of `WPORG-28R` remain blocked external or later-gate actions pending authorization. | `2026-07-25` |
| `WPORG-18A` | `J` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-18A Result`.<br>Current `includes/core/registry/admin-menu.php` still defines the compatibility shim `vms_i18n_runtime()` and current `includes/core/registry/statuses.php` uses literal gettext call sites instead of wrapper-mediated runtime strings. | `tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | This correction pass spot-checked the current wrapper and literal call sites, but did not rerun parser or extraction gates. | none imported | Fresh parser, extraction, and reviewer-source verification are still required before any final resubmission audit. | `2026-07-10` |
| `WPORG-18B` | `J` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-18B Result`.<br>Current repository still contains the named i18n harnesses and the registry files audited in `WPORG-18A`, but this pass did not rebuild the full translator-comment inventory from scratch. | `tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | No fresh mirror extraction or live `wp plugin check ... --checks=i18n_usage` run was performed in this correction pass. | none imported | Translator-comment ordering, extraction, and mirror/live alignment still require a focused rerun before this can return to `verified`. | `2026-07-10` |
| `WPORG-18D` | `J` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-18D Result`.<br>The combined `WPORG-18` closeout proof still depends on the historical parser and extraction reruns documented there. | historical result section only | This correction pass did not rerun the final semantic translator-comment audit, parser gates, or extraction gates. | none imported | Future submission work still needs the actual reviewer source plus a fresh semantic closeout audit. | `2026-07-10` |
| `WPORG-19A` | `C` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-19A Result`.<br>Current repository still contains the shared nonce and request guard surfaces in `includes/runtime-guards.php`. | `tests/nonce-input-normalization.php`<br>`tests/admissions-rest-permissions.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | This correction pass did not rerun the nonce normalization suite or the release harnesses. | none imported | Object-level authorization follow-up remains tracked by `WPORG-19B`; both batches still need fresh final-audit certification. | `2026-07-10` |
| `WPORG-19B` | `C` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-19B Result`.<br>Current repository still contains `tests/authorization-boundary-hardening.php`, `tests/admissions-rest-permissions.php`, and the shared runtime guards used by the audited handlers. | `tests/authorization-boundary-hardening.php`<br>`tests/admissions-rest-permissions.php`<br>`tests/request-input-sanitization.php` | This correction pass did not rerun the authorization inventory or its focused tests. | none imported | Future audits should confirm no new direct-dispatch paths were added outside the historical inventory. | `2026-07-10` |
| `WPORG-20A` | `D` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-20A Result`.<br>Current repository still contains `includes/runtime-guards.php`, and this correction pass found no `FILTER_UNSAFE_RAW` matches under `includes/` or `tests/`. | `tests/request-input-sanitization.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | This correction pass spot-checked current helper inventory only. It did not rerun the historical sanitization or release verification commands. | none imported | Upload handling and decoded JSON validation still stay in `WPORG-20B` and `WPORG-20C`; the direct `$_SERVER` follow-up previously tracked in `WPORG-20A-S` is now closed under `verified`; the broader `WPORG-20` family still needs each remaining child batch to earn its own fresh final certification. | `2026-07-11` |
| `WPORG-20A-S` | `D` | `verified` | Current repository still routes the Event Plan performance request-time seed in mirror/live `includes/core/event-plan-performance.php` through `vms_request_server_value('REQUEST_TIME_FLOAT')`, preserving the existing request-ID hash, trace-log payloads, transient lock payloads, and request-local cache while failing closed on empty or non-scalar server inputs.<br>Fresh clean public-package build evidence on 2026-07-22 produced `backstage-venue-manager-1.0.0-public-release.zip` with SHA-256 `83f9661c3059f23a7921ff2e9db119f430ecf5c5594a83c74bcf9505dbc81f7a`, and the clean packaged Plugin Check reruns no longer reproduced the historical direct-server `REQUEST_TIME_FLOAT`, `REQUEST_URI`, `HTTP_USER_AGENT`, `REMOTE_ADDR`, or `HTTP_ACCEPT` hits in `includes/core/event-plan-performance.php`, `includes/core/slow-request-logger.php`, `includes/runtime-guards.php`, or `includes/integrations/ticketing-phase-b.php`.<br>The only fresh packaged `$_SERVER` rows that remain are the centralized `vms_request_server_value()` helper in `includes/runtime-guards.php` and the exact-case POST helpers in `includes/admin/season-dates.php`, `includes/modules/staff-tasks/admin-ui.php`, `includes/portal/staff-portal.php`, and `includes/portal/vendor-tax-profile.php`; those bounded residuals are already covered by the focused strict-POST and request-wrapper harnesses and are accepted for resubmission. | `tests/event-plan-performance-request-id-remediation.php`<br>`tests/vendor-tax-profile-strict-post-remediation.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/admin-request-method-wrapper-remediation.php`<br>`tests/slow-request-logger-request-input-characterization.php`<br>`tests/event-feedback-request-hash-characterization.php`<br>`tests/public-calendar-user-agent-view-characterization.php`<br>`tests/request-input-sanitization.php`<br>`tests/runtime-stub-guards.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/release-compatibility-harness.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php` | Fresh focused suites, continuity reruns, clean public packaging, and clean packaged Plugin Check reruns all passed locally on 2026-07-22. The normalized raw packaged findings were retained in `docs/plugin-check-1.0.0-2026-07-22-raw.txt`, and the historical `docs/plugin-check-1.0.0-raw.txt` remained unchanged. | current task commit `Close WPORG-20A-S direct server remediation` | Closed. If the reviewer reopens direct `$_SERVER` usage, start from the bounded `vms_request_server_value()` helper and the four exact-POST helper sites; the remaining packaged `$_REQUEST`/`$_POST` warnings in Event Plan Performance and Ticketing Phase B are outside this direct-server parent. | `2026-07-22` |
| `WPORG-20B` | `D`, `H` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-20B Result`.<br>Current repository still contains `vms_upload_read_file()` and `vms_validate_uploaded_file()` in `includes/runtime-guards.php`, plus current consumers in `includes/core/private-files.php`, `includes/admin/data-tools/actions-event-plan-import.php`, `includes/portal/vendor-portal.php`, and `includes/integrations/ticketing-verifications.php`. | `tests/upload-validation-guards.php` | This correction pass confirmed the helper and test files still exist, but did not rerun the focused upload validation suite or any broader release gates. | `d1cdfbd80b05c8254cdc413d0e1bbb821ca13492` | Future audits should confirm no new raw path persistence or trust-on-client-MIME paths were introduced, and mirror/live alignment was not freshly rediffed here. | `2026-07-11` |
| `WPORG-20C` | `D` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-20C Result`.<br>Current repository still contains `vms_json_top_level_token()` and `vms_json_decode_associative()` in `includes/runtime-guards.php`, with current consumers in `includes/rest/class-vms-rest-tours.php`, `includes/services/event-plan-import/event-plan-import-engine.php`, and `includes/integrations/ticketing-rules-v2.php`. | `tests/decoded-json-validation.php` | This correction pass confirmed the helper and test files still exist, but did not rerun decoded-JSON validation or route-specific payload tests. | none imported | Route-specific field validation still belongs to each handler and should be rechecked when payload shapes change. Residual raw `json_decode()` inventory outside the shared validator is tracked separately in `WPORG-20C-R`. | `2026-07-11` |
| `WPORG-20C-R` | `D` | `verified` | Current repository evidence now classifies every residual raw `json_decode()` consumer inside the mirror parent boundary.<br>`41ff8b9fdb0bf719172ecb59d75f7bd84bcdd937` hardens stored claim-assignment reads in `includes/integrations/ticketing-rules-v2.php` while preserving the direct-array branch and legacy seat-meta fallback, and current focused coverage still proves the mirror/live helper stays synchronized where required.<br>`93f0b32746c9b2c897e73a4e290bd868b952568b`, `7f92ae2985ca9ec0d9259c2cfdd04cedeeb5e965`, `a452d088cfaf251bbffb4c24beb7917d8aaa2292`, `c2040655b409f7027791358e4451ff517aa00834`, `42189c9f91b9269403adc1cd55cad1ed5b488014`, and `0a5c74fccc6339635f0887cf8b67b758a33739c8` close the remaining mutation-sensitive or compatibility-sensitive consumers in Social queue snapshots, Staff Tasks stored signature JSON, Staff Tasks checklist override JSON, Event Plan Review snapshot/changes JSON, and Tours user-state readers.<br>Intentional helper internals now reduce to `includes/runtime-guards.php`.<br>Validated specialized decoders remain in `includes/social-share/queue-runner.php`, `includes/integrations/ticketing-rules-v2.php`, `includes/modules/staff-tasks/generator.php`, `includes/modules/staff-tasks/store.php`, and `includes/core/event-plan-review.php`.<br>Accepted low-risk raw decodes now reduce to `includes/social-share/crypto.php` (plugin-authored encrypted payloads, fail-closed), `includes/social-share/queue-repo.php` (plugin-authored `meta_json`, annotation-only, no secrets/authorization/provider/routing dependency), `includes/integrations/ticketing-claims-framework.php` (admin log/source context only), `includes/core/staffing.php` (computed admin summary payloads; malformed fallback loses summary display only), and the bounded Tours compatibility readers in `includes/tours/class-vms-tours-storage.php` plus `includes/core/tours/class-vms-tours.php` (malformed state can only re-show tours or lose completion/progress visibility; malformed nested values may stringify as `Array` in an admin-only label).<br>No-action diagnostic or remote-error decodes remain only in `includes/ticketing/ticket-inventory-forensics.php` and `includes/integrations/square-ticket-mirror.php`. | `tests/decoded-json-validation.php`<br>`tests/ticketing-rules-v2-request-path-normalization.php`<br>`tests/social-share-queue-snapshot-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/event-plan-review-json-characterization.php`<br>`tests/tours-user-state-json-characterization.php`<br>`tests/request-input-sanitization.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh per-site continuity reruns passed in this closeout, including Tours characterization with matching mirror/live SHA-256 values for the shared Tours runtime files and retained Ticketing Rules mirror/live decoder parity where required.<br>The clean packaged Plugin Check evidence from `WPORG-20A-S` dated `2026-07-22` remains adequate for this parent, so no fresh packaged scan was required in this documentation-only closeout. | `41ff8b9fdb0bf719172ecb59d75f7bd84bcdd937`<br>`93f0b32746c9b2c897e73a4e290bd868b952568b`<br>`7f92ae2985ca9ec0d9259c2cfdd04cedeeb5e965`<br>`a452d088cfaf251bbffb4c24beb7917d8aaa2292`<br>`c2040655b409f7027791358e4451ff517aa00834`<br>`42189c9f91b9269403adc1cd55cad1ed5b488014`<br>`0a5c74fccc6339635f0887cf8b67b758a33739c8` | Closed under terminal status `verified`.<br>No unsafe residual direct consumer remains, no runtime child remains open, and the optional Tours admin-label cleanup is accepted as low risk rather than a release blocker.<br>Future payload-shape drift should reopen only the affected narrow child boundary with focused tests rather than this parent by default. | `2026-07-23` |
| `WPORG-21` | `H` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-21 Result`.<br>Current repository still reflects the upload-hardening helpers and tests carried by `WPORG-20B`, but no separate implementation artifact exists beyond that bookkeeping. | relies on `WPORG-20B` test evidence, especially `tests/upload-validation-guards.php` | This correction pass did not rerun the historical upload-finding reconciliation. | `d1cdfbd80b05c8254cdc413d0e1bbb821ca13492` via `WPORG-20B` | Treat this as historical bookkeeping unless a concrete upload regression reopens it. | `2026-07-11` |
| `Review-10 Upload APIs` | `10` | `verified` | Current mirror source now shows no live plugin-owned `move_uploaded_file()` use under `includes/` for this review family.<br>`includes/admin/data-tools/actions-event-plan-import.php` now uses its own scoped `wp_handle_upload()` path for Event Plan preview staging, and `includes/core/private-files.php` now uses `wp_handle_upload()` for the shared private-file broker without broadening caller MIME boundaries or introducing attachment/public-URL behavior.<br>This reviewer requirement is now independently closed from the earlier MIME-validation and private-file validation work tracked in `WPORG-20B` and `WPORG-21`. | `tests/event-plan-import-upload-api-remediation.php`<br>`tests/private-file-upload-api-remediation.php`<br>`tests/upload-validation-guards.php`<br>`tests/verification-proof-normalization.php` | Current source reconciliation plus focused upload-API verification reruns passed in this closeout. | multiple; see `Review-10A` and `Review-10B` below | Closed. No live plugin-owned raw HTTP-upload mover remains in mirror `includes/` for this review family. | `2026-07-18` |
| `Review-10A Event Plan CSV preview upload API` | `10` | `verified` | Current repository evidence shows `includes/admin/data-tools/actions-event-plan-import.php` replaced the historical raw Event Plan preview mover with a scoped `wp_handle_upload()` path while preserving the existing private staging directory, deterministic `<token>-source.csv` destination, preview/report storage keys, preview rollback, commit cleanup, notices, redirects, and filter cleanup behavior.<br>The current closeout also refreshed `tests/event-plan-import-upload-api-remediation.php` so it verifies the current Event Plan boundary without assuming the later shared private-file broker must remain forever unchanged. | `tests/event-plan-import-upload-api-remediation.php` | Current focused source/runtime verification rerun passed in this closeout. | `88a90cce7087e2d013493236b0280da6d1bd31ea` | Closed under `Review-10`. No separate follow-up remains for the Event Plan preview upload path. | `2026-07-18` |
| `Review-10B Shared private-file broker upload API` | `10` | `verified` | Current repository evidence shows `includes/core/private-files.php` replaced the historical raw shared private-file mover with `wp_handle_upload()`, and the current callers in `includes/core/private-files.php`, `includes/portal/vendor-portal.php`, and `includes/integrations/ticketing-verifications.php` now pass their exact existing MIME maps into the broker.<br>The current source and focused reruns confirm UUID storage keys, private buckets, caller MIME boundaries, original-filename metadata, related-post metadata, `0640`, registration, replacement cleanup, deletion, expiration, and verification-image bypass behavior remain intact. | `tests/private-file-upload-api-remediation.php`<br>`tests/upload-validation-guards.php`<br>`tests/verification-proof-normalization.php` | Current focused source/runtime verification reruns passed in this closeout. | `7441c7008d8bd38834f4f8cb50b84e61d6fabda2` | Closed under `Review-10`. No separate shared-broker implementation residual remains. | `2026-07-18` |
| `WPORG-22` | `B` | `implemented` | Historical evidence remains the `WPORG-22` result sections in `docs/WPORG_PREREVIEW_REMEDIATION.md`.<br>Current repository still contains the externalized asset owners and tests, including `assets/css/admin-ticket-integrity.css`, `assets/js/vms-event-plan-secondary-vendors.js`, `assets/js/vms-event-plan-staff.js`, `tests/ticket-integrity-inline-css-remediation.php`, `tests/ticketing-server-controls-inline-js-remediation.php`, `tests/vendor-portal-modal-inline-js-remediation.php`, and `tests/event-plan-shell-controller-inline-js-remediation.php`. | `tests/ticket-integrity-inline-css-remediation.php`<br>`tests/ticketing-server-controls-inline-js-remediation.php`<br>`tests/vendor-portal-modal-inline-js-remediation.php`<br>`tests/event-plan-shell-controller-inline-js-remediation.php` and related `tests/event-plan-*-inline-js-remediation.php` files | This correction pass confirmed current assets and focused test files exist, but did not rerun the slice-specific suites. | `8096682beaea18a91650f37e26675810f1a341ff` for `B4`; other sub-batches remain documented in their result sections | Future edits must preserve externalized asset ownership and avoid reintroducing executable inline controllers. Fresh mirror/live verification is still required for `verified`. | `2026-07-12` |
| `WPORG-22R` | `B` | `verified` | Current source inventory no longer finds executable inline runtime `<script>` / `<style>` emitters in the tracked `WPORG-22R` family after `WPORG-22R-M` removed the final two Staff Portal emitters from `includes/portal/staff-portal.php` and moved their behavior into `assets/js/vms-staff-portal.js`.<br>All known runtime implementation children `WPORG-22R-A`, `WPORG-22R-B`, `WPORG-22R-C`, `WPORG-22R-D`, `WPORG-22R-F`, `WPORG-22R-G`, `WPORG-22R-H`, `WPORG-22R-I`, `WPORG-22R-J`, `WPORG-22R-K`, `WPORG-22R-L`, and `WPORG-22R-M` are now verified, and stale-test reconciliation in `db9f19d7c14bb36c06f6467af04d5ac62af62566` refreshed the remaining proof without changing runtime, asset, live-tree, version, or packaging files. | `tests/staff-portal-inline-js-remediation.php`<br>`tests/ticket-integrity-inline-css-remediation.php`<br>`tests/ticketing-server-controls-inline-js-remediation.php`<br>`tests/vendor-portal-modal-inline-js-remediation.php`<br>`tests/event-plan-shell-controller-inline-js-remediation.php` and related `tests/event-plan-*-inline-js-remediation.php` files | Current formal closeout pass verified `php -l` on all 17 maintained test files, reran all 17 maintained tests plus all 22 required support/sentinel tests, confirmed the accepted unchanged `tests/add-dispatch-open-vendor-needs.php` failure, and reran the scoped repository-wide inline-asset scan. | `db9f19d7c14bb36c06f6467af04d5ac62af62566` | Closed under terminal status `verified`. No executable inline JS/CSS remains inside the tracked `WPORG-22R` runtime family, the unchanged ADD baseline defect remains outside this parent, and unrelated executable inline assets still remain elsewhere in the repository. | `2026-07-20` |
| `WPORG-22R-A` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable clipboard helper from `includes/admin/reference/keys-map.php` and load the same behavior from the page-scoped `assets/js/vms-reference-keys-map.js` asset.<br>The current source preserves the `manage_options` page guard, the `vms-reference-keys-map` page slug, the `#vms-copy-keys-map` button ID, the `#vms-keys-map-text` textarea ID, the textarea payload rendering path, and the existing focus/select, `document.execCommand('copy')`, label, and `1500ms` reset behavior via inert escaped data attributes. | `tests/reference-keys-map-inline-js-remediation.php` | Current focused source/runtime verification rerun passed in this closeout. | current task commit `Externalize Keys Map clipboard helper` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-B` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable Holidays bulk-selection helper from `includes/admin/holidays.php` and load the same behavior from the page-scoped `assets/js/vms-holidays-admin.js` asset.<br>The current source preserves the `manage_options` page guard, the `vms-holidays` page slug, the `#vms_holidays_select_all` controlling checkbox, the `.vms_holidays_row_cb` row selector, the immediate post-markup change-handler timing, and the exact checked-state propagation behavior without adding a disabled-row filter. | `tests/wporg-22r-holidays-tax-bypass-inline-js-remediation.php` | Current focused source/runtime verification rerun passed in this closeout. | current task commit `Externalize holidays and tax helpers` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-C` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable Event Plan Import commit-selection helper from `includes/admin/data-tools/page-event-plan-import.php` and load the same behavior from the page-scoped `assets/js/vms-event-plan-import.js` asset.<br>The current source preserves the hidden page slug `vms-import-event-plans`, the `manage_options` boundary, the page registration, the `#vms-epcsv-commit-form` ID, the `.vms-epcsv-row-check` selector, the selected/all scope controls, the selected-count element, the select-all and clear-all controls, the exact selected-required alert message through an inert escaped form attribute, the rows-payload error renderer, the `rows_json_storage_key` / legacy `rows_json_path` fallback, and the existing preview/report/redirect/cleanup lifecycle while synchronizing the live page and live Administrator shell to the current mirror contracts. | `tests/event-plan-import-inline-js-remediation.php`<br>`tests/event-plan-import-upload-api-remediation.php`<br>`tests/event-plan-import-rows-payload-output-remediation.php`<br>`tests/administrator-explicit-notice-output-remediation.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live page, shell, and asset synchronization proof. | current task commit `Externalize Event Plan Import commit helper` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-D` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable Staff Tasks admin-page helpers from `vms_tasks_render_tasks_page()` and `vms_tasks_render_checklist_templates_page()` in `includes/modules/staff-tasks/admin-ui.php` and load the same behavior from the page-scoped `assets/js/vms-tasks-admin-pages.js` asset.<br>The current source preserves the `vms_tasks_current_user_can_manage_all()` boundary, the exact `vms-tasks` and `vms-checklist-templates` page gates, the existing Tasks and Checklist Templates page registrations, the create-task form IDs/selectors, the checklist `data-scope` option attributes, the checklist scope/apply-mode rows, the same initial-state sync, and the same recurrence, scheduled-role, and checklist-option reset behavior while leaving the separate Event Plan metabox asset untouched. | `tests/wporg-22r-module-admin-helpers-inline-js-remediation.php`<br>`tests/event-plan-dead-editor-scripts-partial-removal.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live Staff Tasks PHP and JS synchronization proof. | current task commit `Externalize module admin helpers` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-F` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable compensation/defaults metabox helpers from `vms_render_comp_package_meta_box()` in `includes/admin/vendor-comp-packages.php` and `vms_render_vendor_defaults_metabox()` in `includes/admin/vendor-details.php` and load the same behavior from the screen-scoped `assets/js/vms-compensation-admin.js` asset.<br>The current source preserves the exact `post` / `post-new` screen assumptions for `vms_comp_package` and `vms_vendor`, the existing metabox registrations, nonce/save boundaries, field names, template `data-terms` bridge, selectors, initial-state sync, template-copy behavior, attendance preview calculations, and save-facing DOM while using only inert localized label/string config. | `tests/vendor-compensation-inline-js-remediation.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live PHP and JS synchronization proof. | current task commit `Externalize vendor compensation helpers` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-20` |
| `WPORG-22R-G` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable Tax Bypass required-field shim from `includes/admin/tax-bypass.php` and load the same behavior from the screen-scoped `assets/js/vms-tax-bypass-admin.js` asset.<br>The current source preserves the `manage_options` boundary, the supported post types `vms_vendor` and `vms_staff`, the `post` / `post-new` screen assumptions, the exact field selectors, and the exact `required` / `aria-required` removal behavior without altering save handlers, validation, nonce checks, or business rules. | `tests/wporg-22r-holidays-tax-bypass-inline-js-remediation.php` | Current focused source/runtime verification rerun passed in this closeout. | current task commit `Externalize holidays and tax helpers` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-H` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable ADD request-builder helper from `vms_add_dispatch_render_request_builder()` in `includes/modules/availability-date-dispatch/admin-ui.php` and load the same behavior from the page-scoped `assets/js/vms-add-dispatch-admin.js` asset.<br>The current source preserves the exact `vms-add-dispatch` page gate, the existing `manage_options` boundary, the request-builder form, hidden fields, recipient-row selectors, selected/eligible count nodes, existing `data-vms-add-*` row attributes, filter-driven hidden-field sync, eligibility labels/details, select-all, clear-all, and send-button state transitions while intentionally leaving the separate ADD menu-badge CSS/JS emitters and ADD public shell untouched. | `tests/wporg-22r-module-admin-helpers-inline-js-remediation.php`<br>`tests/add-dispatch-pill-output-remediation.php`<br>`tests/add-dispatch-public-shell-output-remediation.php`<br>`tests/event-plan-secondary-vendor-capacity-and-add.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live ADD PHP and JS synchronization proof. | current task commit `Externalize module admin helpers` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-I` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable ADD menu-badge CSS and JS emitters from `vms_add_dispatch_render_menu_badge_css()` and `vms_add_dispatch_render_menu_badge_js()` in `includes/modules/availability-date-dispatch/admin-ui.php` and load the same behavior from the shared `assets/css/vms-admin-menu.css` stylesheet plus the admin-only `assets/js/vms-admin-menu.js` asset.<br>The current source preserves the existing `manage_options` boundary, the positive-count gate, the exact `vms_add_dispatch_current_pending_count()` source, the exact top-level and submenu selectors, the awaiting-mod/pending-count badge markup, the `beforeend` insertion point, the DOM-ready timing branch, the no-target no-op path, and duplicate-badge prevention while using only inert localized pending-count config. | `tests/add-dispatch-menu-badge-inline-assets-remediation.php`<br>`tests/add-dispatch-pill-output-remediation.php`<br>`tests/add-dispatch-public-shell-output-remediation.php`<br>`tests/event-plan-secondary-vendor-capacity-and-add.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live ADD admin PHP, shared admin-menu CSS, and admin-menu JS synchronization proof. The known baseline `tests/add-dispatch-open-vendor-needs.php` failure remained unchanged. | current task commit `Externalize ADD menu badge assets` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-18` |
| `WPORG-22R-J` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the static inline standalone ADD public-shell `<style>` block from `vms_add_dispatch_render_public_shell()` in `includes/modules/availability-date-dispatch/public.php` and load the same behavior from the standalone `assets/css/vms-add-dispatch-public-shell.css` stylesheet.<br>The current source preserves the existing `template_redirect` router, rewrite tag/rule, standalone document lifecycle, `status_header(200)`, `nocache_headers()`, response allowlist sink, escaped title/link URLs, notices, action links, and request/response logic while adding only a versioned escaped same-origin stylesheet `<link>` in the existing `<head>`. | `tests/add-dispatch-public-shell-inline-css-remediation.php`<br>`tests/add-dispatch-public-shell-output-remediation.php`<br>`tests/add-dispatch-pill-output-remediation.php`<br>`tests/event-plan-secondary-vendor-capacity-and-add.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live PHP and CSS synchronization proof. The known baseline `tests/add-dispatch-open-vendor-needs.php` failure remained unchanged. | current task commit `Externalize ADD public shell styles` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-20` |
| `WPORG-22R-K` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable Staff qualifications metabox helper from `includes/cpt/staff.php` and load the same behavior from the screen-scoped `assets/js/vms-staff-cpt-admin.js` asset.<br>The current source preserves the exact `post` / `post-new` screen assumptions for `vms_staff`, the existing Staff qualifications metabox registration, nonce/save boundaries, qualification field names, `storage_kind` / proof-download fallback handling, add/remove selectors, last-row reset behavior, and save-facing DOM while using only inert localized label/status config. | `tests/staff-cpt-inline-js-remediation.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live PHP and JS synchronization proof. | current task commit `Externalize Staff CPT admin helper` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-20` |
| `WPORG-22R-L` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the executable Staff Roles qualification-builder helper plus the static/executable Staffing Templates emitters from `includes/admin/staffing.php` and load the same behavior from the screen/page-scoped `assets/js/vms-staffing-admin.js` asset plus the page-scoped `assets/css/vms-staffing-admin.css` stylesheet.<br>The current source preserves the exact `edit-tags` / `term` Staff Roles screen assumptions for taxonomy `vms_staff_role` under post type `vms_staff`, the exact `vms-staffing-templates` page slug and `manage_options` boundary, the qualification add/remove selectors and last-row reset behavior, the `#vms-tpl-slots` / `#vms-tpl-add-row` template editor selectors, the absolute/relative timing visibility toggles, the existing absolute-warning condition, the one-row minimum removal guard, and the save-facing DOM while handing the JS only inert `<template>` markup already escaped by PHP. | `tests/staffing-admin-inline-assets-remediation.php` | Current focused source/runtime verification rerun passed in this closeout, including mirror/live PHP, JS, and CSS synchronization proof. | current task commit `Externalize Staffing admin assets` | Closed under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-20` |
| `WPORG-22R-M` | `B` | `verified` | Current repository and synchronized live-tree runtime now remove the final two executable Staff Portal availability emitters from `vms_staff_portal_render_availability_manual()` in `includes/portal/staff-portal.php` and load the same behavior from the shortcode-scoped `assets/js/vms-staff-portal.js` asset.<br>The current source preserves the normal shortcode lifecycle in `vms_staff_portal_shortcode()`, the availability-tab-only enqueue gate, the `#vms-portal-root` DOM root, the `.vms-staff-av-form`, `.vms-av-autosave`, `.vms-staff-av-btn`, `.vms-av-hidden`, `.vms-av-badge-status`, `.vms-av-src`, `.vms-av-month`, and `.vms-av-counts` selectors, the exact Unset -> Available -> Unavailable cycle, the `vms_staff_save_manual_availability_day` AJAX action and payload, the `vms_staff_avail_ajax` nonce boundary, the existing optimistic/revert autosave behavior, and the same `beforeunload` warning while handing the JS only inert escaped `data-vms-staff-availability-*` values for `admin-ajax.php` and the existing nonce. | `tests/staff-portal-inline-js-remediation.php`<br>`tests/staff-portal-safe-html-output-remediation.php`<br>`tests/request-input-sanitization.php`<br>`tests/authorization-boundary-hardening.php` | Current focused source/runtime verification reruns passed in this closeout, including mirror/live Staff Portal PHP and JS synchronization proof plus a current-source scan confirming no remaining executable inline runtime in `includes/portal/staff-portal.php`. | current task commit `Externalize Staff Portal helpers` | Closed as the final known runtime implementation slice under parent `WPORG-22R`, now terminally verified by the stale-test reconciliation and formal closeout evidence. | `2026-07-20` |
| `WPORG-23` | `K` | `implemented` | Historical evidence remains `docs/WPORG_PREREVIEW_REMEDIATION.md` `WPORG-23 Result`.<br>Current repository still contains `vms_admin_ui_is_admin_notice_screen()` in `includes/admin-ui/context.php` and current call sites in `includes/admin/admin-notices.php`, `includes/runtime-guards.php`, and `includes/ticketing/ticket-integrity-payment-gateway-health.php`. | `tests/admin-notice-scope-remediation.php`<br>`tests/request-input-sanitization.php`<br>`tests/vendor-apply-admin-css-remediation.php`<br>`tests/ticket-integrity-inline-css-remediation.php` | This correction pass spot-checked the helper and current call sites, but did not rerun the focused notice-scope suite. | none imported | Notice placement polish remains outside `WPORG-23` and should not be conflated with this completed scope. Fresh final-audit certification is still required. | `2026-07-12` |
| `WPORG-24` | `E` | `verified` | Current repository evidence now shows the accepted remediation chain through package-owned `WPORG-24Q`, plus the documentation-only `WPORG-24R` closeout recorded in `docs/wporg-remediation-ledger.md` and `docs/WPORG_PREREVIEW_REMEDIATION.md`.<br>Current source and test files, current Git history, and the accepted closeout evidence now confirm that Social Sharing closed under `WPORG-24P` plus `WPORG-24Q`, the separate Settings entitlement image-sync family remains finite and safely escaped, every current Event Plan HTML family is already mapped to accepted tested children, and the remaining shared Administrator-shell raw `$captured_notices_html` / `$content_html` sinks are intentional page-owned architecture rather than unresolved parent defects. | see the `WPORG-24` breakdown below | This documentation-only closure pass reran the accepted parent closeout suites, re-audited the stale residual tracker in both remediation docs, and freshly verified the final parent classification without production or test changes. | multiple; see the `WPORG-24` breakdown below | Closed in `WPORG-24R`. Any future trusted printable-export-shell inventory, including `vms_safety_render_export_shell()` in `includes/safety/admin.php`, belongs to a separate non-blocking issue outside this parent. | `2026-07-18` |
| `WPORG-24R` | `E` | `verified` | Current repository evidence for this documentation-only child is limited to `docs/wporg-remediation-ledger.md` and `docs/WPORG_PREREVIEW_REMEDIATION.md` plus the current repository state they describe.<br>The closeout records that shared Administrator-shell raw `$captured_notices_html` / `$content_html` remain intentional page-owned renderer output outside the parent, Settings entitlement image-sync is accepted as finite and verified safe, Social Sharing closed under `WPORG-24P` plus `WPORG-24Q`, and every current Event Plan HTML family already maps to accepted tested children while remaining handlers are data-only JSON. | `tests/administrator-explicit-notice-output-remediation.php`<br>`tests/settings-integrity-scan-output-remediation.php`<br>`tests/settings-default-venue-alert-output-remediation.php`<br>`tests/social-event-panel-finite-renderer-output-remediation.php`<br>`tests/social-event-panel-lazy-load-output-characterization.php`<br>`tests/social-event-panel-lazy-load-js-characterization.php`<br>`tests/event-plan-comp-options-output-remediation.php`<br>`tests/event-plan-supporting-vendor-options-output-remediation.php`<br>`tests/event-plan-readiness-details-output-remediation.php`<br>`tests/event-plan-staff-output-remediation.php`<br>`tests/event-plan-secondary-vendors-lazy-load-output-remediation.php`<br>`tests/event-plan-secondary-vendors-save-output-remediation.php`<br>`tests/event-plan-ticket-ui-overrides-isolated.php`<br>`tests/event-plan-calendar-unpublished-suppress-save.php`<br>`tests/admin-notice-scope-remediation.php`<br>`tests/decoded-json-validation.php` | This documentation-only child reran the accepted parent closeout suites, reconciled both remediation docs, and made no production, test, asset, packaging, deployment, or stash changes. | current task commit `Close WPORG-24 residual tracker` | Parent closed. Any future trusted printable-export-shell inventory remains outside `WPORG-24` and does not block resubmission. | `2026-07-18` |
| `WPORG-25` | `I` | `verified` | Reviewer source dated `2026-07-01` requires every plugin-owned `ob_start()` to close in the same logical flow without hooks or bypassable paths.<br>Current repository audit now shows `67` `ob_start()` sites in `includes/`: `66` are same-function local fragment or template captures paired with `ob_get_clean()`, and the remaining request-global AJAX opener in `includes/integrations/load.php` is explicitly closed by the current AJAX owner family in `includes/integrations/ticketing.php` plus the approved fast helper in `includes/integrations/ticketing-phase-b.php`.<br>`I1` is now verified: the opener remains request-global under `DOING_AJAX`, all seven legacy ticketing callbacks terminate through `vms_ticketing_ajax_send_success()` / `vms_ticketing_ajax_send_error()`, all three public V2 cart or context callbacks terminate through `vms_ticketing_v2_ajax_send_success()` / `vms_ticketing_v2_ajax_send_error()`, all eleven Phase B callbacks terminate through those cleanup-only wrappers or `vms_ticketing_v2_ajax_send_json_success_fast()`, both customer-claims callbacks terminate through the cleanup-only wrappers, no direct-send responder remains beneath the opener, and no reachable nested plugin-owned buffer was found in the audited callback paths.<br>`I2` is now verified: no My Tickets or server-mount `template_redirect` registration remains, no My Tickets or server-mount `ob_start()` remains, My Tickets now uses native `tec_tickets_my_tickets_link_ticket_count_by_type`, reserved add-ons now use `tribe_template_before_include_html:tickets/v2/tickets/footer`, disabled ticket products now use native `tribe_tickets_get_tickets_query_args`, the automatic append path remains fallback-only when native footer placement did not succeed, shortcode/manual renderer contracts remain intact, server-side cancellation enforcement remains intact, and no inline-controller or runtime-sidecar dependency remains in PHP.<br>Mirror/live parity is exact for the AJAX opener wrappers, My Tickets native filter, native footer placement, native disabled-ticket suppression, cancellation filter, shortcode callback, and append fallback; accepted unrelated live drift remains in `vms_ticketing_v2_render_entitlements_block()` plus two live-only AJAX callback bodies and does not reopen this parent.<br>The remaining boolean ownership flag in `includes/integrations/load.php` is retained as a nonblocking theoretical compatibility note rather than a demonstrated current defect. | `tests/ticketing-output-buffer-lifecycle-characterization.php`<br>`tests/ticketing-v2-ajax-output-buffer-ownership.php`<br>`tests/ticketing-phase-b-ajax-output-buffer-ownership.php`<br>`tests/ticketing-claims-ajax-output-buffer-ownership.php`<br>`tests/ticketing-my-tickets-notice-source-filter-remediation.php`<br>`tests/ticketing-server-mount-native-footer-remediation.php`<br>`tests/ticketing-disabled-ticket-native-suppression-remediation.php`<br>`tests/ticketing-server-controls-inline-js-remediation.php`<br>`tests/ticket-claims-assignee-validation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php` | Fresh focused lifecycle suites and broader continuity checks were rerun in this documentation-only closeout, and the source audit now proves that every output-buffer lifecycle inside `WPORG-25` is either explicitly owned or replaced by a native rendering boundary.<br>No runtime, test, live-tree, asset, packaging, deployment, or stash state changed in this closeout. | `2f8d8e16bb5a6842f5e2aae60cb027aa1cb30d3e`<br>`b5de3cd39f528b39b430cf12feec1725262f7fdb`<br>`e65d62de12e16708a8c755d7ac8a89f265cf16cb`<br>`92bc9ff8e226c716a851a639662b0e6943933ee4`<br>`5458dad4e0e7e430bd2a8b0d961582decc44108c`<br>`3b23ec96b59be61fc06be68bd7faf00aed852352` | Closed under terminal status `verified`.<br>No open `WPORG-25` implementation residual remains.<br>`WPORG-28R` later reopened the packaged prereview parent because the final rule-family reconciliation found one new packaged warning plus multiple unmapped or still-blocking families, so external slug-reservation, corrected-upload, and reviewer-reply work remain blocked under `Review-2 Name/Slug Closeout`, `WPORG-28R`, and `Review-13 Final Actions`. | `2026-07-25` |
| `Review-8 Prefixing` | `8` | `not applicable` | The completed no-edit audit did not identify a concrete current unprefixed plugin function, class, or WordPress-facing declaration.<br>The one non-`VMS_` constant hit was `SAVEQUERIES` in `includes/core/event-plan-performance.php:594`, which is a WordPress core constant rather than a plugin declaration. | none | Current-tree spot checks only. No additional namespace migration work was authorized. | none imported | Reopen only if a concrete current unprefixed declaration is later found. No blind compatibility-breaking prefix migration should be inferred from this reviewer family. | `2026-07-15` |
| `Review-9 Static AJAX URLs` | `9` | `not applicable` | Current runtime request URLs use `admin_url('admin-ajax.php')` in the audited mirror call sites.<br>The hard-coded `/wp-admin/admin-ajax.php` strings in `includes/core/slow-request-logger.php:292-306` are diagnostic normalization keys rather than live browser request construction, and the mirror/live `includes/core/slow-request-logger.php` files currently match. | `tests/ticketing-server-controls-inline-js-remediation.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/event-plan-supporting-vendor-options-output-remediation.php`<br>`tests/event-plan-readiness-details-output-remediation.php`<br>`tests/event-plan-secondary-vendors-save-output-remediation.php` | Current-tree inspection only. No broad rerun of the related source-assertion suites was performed in this task. | none imported | Reopen only if a true hard-coded runtime endpoint is found outside diagnostic normalization or similar non-request-construction contexts. | `2026-07-15` |
| `WPORG-28` | `M` | `blocked` | Current repository still defines the public-core release line as `1.2.0`: mirror `vendor-management-system.php`, `readme.txt`, and `vms-build.txt` align on `1.2.0`, the sibling live tree intentionally remains `1.1.0` as a separate production-convergence boundary, and the fresh disposable `2026-08-07` public builds still produce a valid `backstage-venue-manager/` package with matching packaged header, stable tag, changelog, upgrade notice, and build metadata.<br>`WPORG-28R-A` through `WPORG-28R-F6` cleared the packaged `NEW_FINDING`, `UNMAPPED`, and alternative-function residuals, `WPORG-28R-G0` decomposed the remaining blocker families into the ordered `WPORG-28R-G1` through `WPORG-28R-G17` roadmap, `WPORG-28R-G1` through `WPORG-28R-G6-T5` then removed the full nonce/input family to zero, `WPORG-28R-G8-T1` through `WPORG-28R-G8-T5` removed the full staffing and Staff Tasks DB/SQL family exactly as planned, `WPORG-28R-G9` then removed the full admissions and claim-state DB/SQL family exactly as planned, the verified `WPORG-28R-G10` ADD/ticketing-claims checkpoint removed `132` DB/SQL rows, and coordinated Wave 1 later removed `134` more packaged DB/SQL rows across Phase B, vendor-user-links, social queue, and the dormant Safety package boundary.<br>The current packaged baseline therefore now stands at `175` errors, `345` warnings (`520` total), `14` unique rule codes, `KNOWN_ACCEPTED=0`, `KNOWN_NONBLOCKING=125`, `NEW_FINDING=0`, `UNMAPPED=0`, and `SUBMISSION_BLOCKER=395`. | `tests/add-dispatch-repository-sql-remediation.php`<br>`tests/add-dispatch-assignment-review.php`<br>`tests/availability-date-dispatch-request-state-remediation.php`<br>`tests/ticketing-claims-repository-sql-remediation.php`<br>`tests/ticket-claims-assignee-validation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh dirty public packaging, a packaged strict-json Plugin Check rerun, the focused `WPORG-28R-G10` ADD and ticketing-claims suites plus continuity and support reruns, and PHP lint across the changed mirror/live ticketing and availability files all passed locally on `2026-08-07` while the live `../../vms/` tree changed only inside the authorized `WPORG-28R-G10` target boundaries.<br>The metadata boundary remains verified, but submission readiness stays blocked because the package findings are not fully reconciled. | current task commit `Close WPORG-28R-G10 checkpoint` | Still blocked under `WPORG-28R`.<br>The public package is not currently ready for final artifact preparation, upload/submission, slug reservation, or reviewer communication.<br>Separate Outreach extraction, duplicate-core safety, and live production replacement or migration still remain outside this parent. | `2026-08-07` |
| `WPORG-28Q` | `M` | `audited` | Current repository evidence for this documentation-only child remains limited to the fresh build/scan reproducibility state it recorded plus the current repository state.<br>The closeout correctly recorded the `php scripts/build-public-release.php --output-dir <temp> --force` build, the `backstage-venue-manager-1.2.0-public-release.zip` identity, the extracted-package structure and exclusion audit, the successful `php tests/release-compatibility-harness.php`, `php tests/public-release-build-pipeline.php`, and `php tests/runtime-stub-guards.php` reruns, plus a packaged strict-json Plugin Check rerun against the extracted public package without any runtime or release-metadata change.<br>`WPORG-28R` later reproduced the same packaged totals, `WPORG-28R-G0` later proved that the remaining residuals required the ordered `G1` through `G17` blocker roadmap, `WPORG-28R-G6-T5` later closed the nonce/input family at zero, `WPORG-28R-G8-T1` through `WPORG-28R-G8-T5` later reduced the packaged baseline to `1045` total findings with `SUBMISSION_BLOCKER=916`, `WPORG-28R-G9` later reduced that same residual baseline to `790` total findings with `SUBMISSION_BLOCKER=661`, the coordinated Wave 1 checkpoint has now reduced it to `520` total findings with `SUBMISSION_BLOCKER=395`. | `tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php`<br>`tests/add-dispatch-repository-sql-remediation.php`<br>`tests/ticketing-claims-repository-sql-remediation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php` | This documentation-only child still stands as fresh build or scan evidence, but its readiness conclusion is superseded by `WPORG-28R` and the later `WPORG-28R-G6-T5`, `WPORG-28R-G8-T1`, `WPORG-28R-G8-T2`, `WPORG-28R-G8-T3`, `WPORG-28R-G8-T4`, `WPORG-28R-G8-T5`, `WPORG-28R-G9`, and the current `WPORG-28R-G10` checkpoint. | none; no commit authorized | Child no longer closes the packaged prereview parent on its own.<br>No upload, deployment, submission, reviewer reply, live replacement, or migration occurred. | `2026-08-07` |
| `WPORG-28R` | `M` | `blocked` | Current repository evidence for this parent now includes the original `2026-07-25` packaged reopening audit, the committed implementation children `WPORG-28R-A` through `WPORG-28R-F6`, the planning child `WPORG-28R-G0`, the verified nonce/input runtime chain `WPORG-28R-G1` through `WPORG-28R-G6-T5`, the verified DB/SQL parent `WPORG-28R-G8`, the verified DB/SQL children `WPORG-28R-G8-T1`, `WPORG-28R-G8-T2`, `WPORG-28R-G8-T3`, `WPORG-28R-G8-T4`, and `WPORG-28R-G8-T5`, the verified DB/SQL child `WPORG-28R-G9`, the coordinated Wave 1 checkpoint, and the follow-up test or documentation child `WPORG-28R-G4-T1`.<br>The original reopen pass rebuilt the public package and proved that submission readiness had to reopen at `309 / 1709 / 2018 / 34`; the later committed `A` through `F` children removed the packaged `NEW_FINDING`, `UNMAPPED`, and alternative-function residuals without changing the surviving blocker families; `WPORG-28R-G0` then mapped every current blocker row to the ordered `G1` through `G17` implementation sequence; `WPORG-28R-G1` through `WPORG-28R-G6-T5` removed the full packaged nonce/input family to zero; `WPORG-28R-G8-T1` through `WPORG-28R-G8-T5` then removed the full `233` staffing and Staff Tasks DB/SQL rows across `52` boundaries; `WPORG-28R-G9` then removed the full `255` admissions and claim-state DB/SQL rows across the tracked admissions boundaries; the verified G10 ADD/ticketing-claims checkpoint removed `132` DB/SQL rows and coordinated Wave 1 later removed another `134` packaged DB/SQL rows across Phase B, vendor-user-links, social queue, and the dormant Safety package boundary.<br>The current parent baseline therefore stands at `175` errors, `345` warnings, `520` total findings, `14` unique rule codes, `KNOWN_ACCEPTED=0`, `KNOWN_NONBLOCKING=125`, `NEW_FINDING=0`, `UNMAPPED=0`, and `SUBMISSION_BLOCKER=395`, with blocker-family totals DB/SQL `328`, nonce/input `0`, date/time `25`, and logging `42`. | `tests/add-dispatch-repository-sql-remediation.php`<br>`tests/add-dispatch-assignment-review.php`<br>`tests/availability-date-dispatch-request-state-remediation.php`<br>`tests/ticketing-claims-repository-sql-remediation.php`<br>`tests/ticket-claims-assignee-validation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh dirty public packaging, a fresh packaged strict-json Plugin Check rerun with empty stderr, the focused `WPORG-28R-G10` ADD and ticketing-claims suites plus continuity and support reruns, and PHP lint across the changed mirror/live ticketing and availability files all passed locally on `2026-08-07` while the synchronized live `../../vms/` tree changed only inside the authorized `WPORG-28R-G10` target boundaries.<br>The parent remains blocked only by the still-blocking packaged families, the completed G10 Phase B slice is verified, and the exact next handoff remains inside `WPORG-28R-G10`. | current task commit `Close WPORG-28R-G10 checkpoint` | Keep blocked after the current `WPORG-28R-G10` checkpoint.<br>Do not treat the current package as ready for final artifact preparation, upload/submission, slug reservation, corrected upload, or reviewer communication while this parent remains blocked.<br>The exact next G10 handoff is the Ticketing Rules V2 query-boundary slice in `includes/integrations/ticketing-rules-v2.php`. | `2026-08-07` |
| `WPORG-28R-A` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only suppression comment in `includes/integrations/ticketing-rules-v2.php`, the focused assertion update in `tests/ticketing-disabled-ticket-native-suppression-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live file at `../../vms/includes/integrations/ticketing-rules-v2.php`.<br>The child confirmed that the public and live ticketing files remain globally divergent, but the target callback, hook registration, and disabled-ID helper stayed semantically equivalent, so full-file parity was intentionally not attempted.<br>The packaged `WordPressVIPMinimum.Performance.WPQueryParams.PostNotIn_post__not_in` warning at `includes/integrations/ticketing-rules-v2.php:7476` dropped from `1` to `0`, no unrelated packaged code count increased, and no new rule code appeared. | `tests/ticketing-disabled-ticket-native-suppression-remediation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/ticketing-output-buffer-lifecycle-characterization.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, focused ticketing reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-25`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28ra-post2.hpJlqH/build --force --allow-dirty` after the exact `--force` attempt was blocked only by the builder's dirty-worktree policy. | current task commit `Document bounded disabled ticket exclusion` | Child closed under terminal status `verified`.<br>`WPORG-28R-B` later reconciled the Turnstile `OffloadedContent` occurrence; recommend the next `WPORG-28R` child for `WordPress.Security.EscapeOutput.ExceptionNotEscaped`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-25` |
| `WPORG-28R-B` | `M` | `verified` | Current repository evidence for this reconciliation child includes the unchanged mirror `includes/vendor-applications.php`, the unchanged packaged `readme.txt` disclosure block, the existing Turnstile-focused tests, the clean packaged strict-json repro stored in disposable `/tmp/wporg-28rb-pre.4RnyrA` artifacts before cleanup, and the unchanged read-only live file at `../../vms/includes/vendor-applications.php`.<br>The child proved the current packaged `PluginCheck.CodeAnalysis.EnqueuedResourceOffloading.OffloadedContent` occurrence is the Cloudflare Turnstile client URL `https://challenges.cloudflare.com/turnstile/v0/api.js` inside the fully configured `[vms_vendor_apply]` shortcode path, exactly matching the `fd67f51` Turnstile guard/disclosure boundary, with no later weakening of that boundary.<br>The packaged occurrence remains visible in Plugin Check, but the current official WordPress.org guidance and packaged disclosure evidence support classifying this exact service-backed row as `KNOWN_NONBLOCKING` rather than `UNMAPPED`. | `tests/vendor-apply-turnstile-contract-remediation.php`<br>`tests/vendor-apply-inline-js-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh clean packaged repro, local Plugin Check rule inspection, current official WordPress.org policy audit, focused Turnstile reruns, and broader regression gates all passed locally on `2026-07-25`.<br>No runtime, readme, or test file changed in this child because the existing service boundary and disclosure were already adequate. | current task commit `Reconcile Turnstile offloaded content` | Child closed under terminal status `verified` for project tracking only.<br>Final WordPress.org acceptance is not guaranteed because Plugin Check still emits the generic remote-resource error with no service-specific exception.<br>Recommend the next `WPORG-28R` child for `WordPress.Security.EscapeOutput.ExceptionNotEscaped`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-25` |
| `WPORG-28R-C1` | `M` | `verified` | Current repository evidence for this runtime child includes the two mirror-only `WordPress.Security.EscapeOutput.ExceptionNotEscaped` suppression comments in `includes/integrations/ticketing-rules-v2.php`, the focused boundary assertions in `tests/ticketing-exception-message-boundary-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live file at `../../vms/includes/integrations/ticketing-rules-v2.php`.<br>The child proved that the two packaged ticketing hits belong only to Store API exception messages that are already normalized to plain-text validation strings and consumed downstream as JSON/REST error payloads or `WP_Error` messages, so escaping at exception construction would corrupt the consumer contract instead of protecting a direct HTML sink.<br>The packaged `WordPress.Security.EscapeOutput.ExceptionNotEscaped` family dropped from `4` to `2`, the removed rows were exactly the ticketing-owned `includes/integrations/ticketing-rules-v2.php` occurrences, and `WPORG-28R-C2` later removed the remaining webhook occurrences. | `tests/ticketing-exception-message-boundary-remediation.php`<br>`tests/ticketing-v2-ajax-output-buffer-ownership.php`<br>`tests/ticket-checkout-safety-hardening.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh source/sink characterization, focused ticketing reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-25`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rc1-post2.3xeZFs/build --force --allow-dirty` after the exact `--force` attempt at `2026-07-26T00:20:19+00:00` UTC was blocked only by the builder's dirty-worktree policy. | current task commit `Document ticketing exception boundaries` | Child closed under terminal status `verified`.<br>`WPORG-28R-C2` later removed the remaining webhook `ExceptionNotEscaped` rows, and `WPORG-28R-E1` later removed the Ticket Integrity `SuppressFilters` row; recommend the next `WPORG-28R` child for the remaining vendor-taxonomy occurrence under `WPORG-28R-E2`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-25` |
| `WPORG-28R-C2` | `M` | `verified` | Current repository evidence for this runtime child includes the two mirror-only `WordPress.Security.EscapeOutput.ExceptionNotEscaped` suppression comments in `includes/social-share/providers/class-provider-webhook.php`, the focused boundary assertions in `tests/social-share-webhook-exception-boundary-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live file at `../../vms/includes/social-share/providers/class-provider-webhook.php`.<br>The child proved that the two remaining packaged hits belong only to internal webhook diagnostics that are caught by the queue runner, sanitized to plain text before storage and the account `last_error` side path, escaped at the admin and event-panel sinks, and JSON-encoded in audit details; no response body, request body, headers, tokens, secrets, or full endpoint text enter either exception message.<br>The packaged `WordPress.Security.EscapeOutput.ExceptionNotEscaped` family dropped from `2` to `0`, the removed rows were exactly the webhook `includes/social-share/providers/class-provider-webhook.php` occurrences, no unrelated packaged code count increased, and the rule code disappeared entirely from the post-edit package. | `tests/social-share-webhook-exception-boundary-remediation.php`<br>`tests/social-share-queue-snapshot-json-remediation.php`<br>`tests/social-event-panel-finite-renderer-output-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh source/sink characterization, installed WordPressCS rule inspection, focused webhook reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rc2-post.FoOEEU/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Document webhook exception boundaries` | Child closed under terminal status `verified`.<br>The full packaged `WordPress.Security.EscapeOutput.ExceptionNotEscaped` family is now reconciled through `WPORG-28R-C1` and `WPORG-28R-C2`, and `WPORG-28R-E1` later removed the Ticket Integrity `SuppressFilters` row; recommend the next `WPORG-28R` child for the remaining vendor-taxonomy occurrence under `WPORG-28R-E2`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-E1` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only `WordPressVIPMinimum.Performance.WPQueryParams.SuppressFilters_suppress_filters` suppression comment in `includes/ticketing/ticket-integrity-monitor.php`, the focused boundary assertions in `tests/ticket-integrity-query-filter-boundary-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, the unchanged read-only deferred file `includes/taxonomies/vendor-type.php`, and the unchanged read-only live file at `../../vms/includes/ticketing/ticket-integrity-monitor.php`.<br>The child proved that `vms_ticket_integrity_build_targets()` must retain a bounded canonical `WP_Query` over published `vms_event_plan` IDs for cron, stale-report refresh, and nonce-protected `manage_options` admin scans, because filters could hide canonical plans and create false-negative integrity gaps while the query already remains bounded by status, linked TEC event, date window, and batch pagination.<br>The packaged `WordPressVIPMinimum.Performance.WPQueryParams.SuppressFilters_suppress_filters` family dropped from `2` to `1`, the removed row was exactly the Ticket Integrity `includes/ticketing/ticket-integrity-monitor.php:787:17` occurrence, and the remaining packaged `includes/taxonomies/vendor-type.php:431:13` row stays open for `WPORG-28R-E2`. | `tests/ticket-integrity-query-filter-boundary-remediation.php`<br>`tests/ticket-integrity-scan-lock.php`<br>`tests/state-of-range-delivery-state.php`<br>`tests/state-of-range-upcoming-filter.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live and deferred-file checksum verification, core-query and VIP-rule inspection, focused Ticket Integrity reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28re1-post.zwAarA/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Document ticket integrity filter suppression` | Child closed under terminal status `verified`.<br>The full packaged `WordPressVIPMinimum.Performance.WPQueryParams.SuppressFilters_suppress_filters` family remains open because the separate vendor-taxonomy row is still outstanding; recommend the next `WPORG-28R` child for that remaining occurrence under `WPORG-28R-E2`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-E2` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only `WordPressVIPMinimum.Performance.WPQueryParams.SuppressFilters_suppress_filters` suppression comment in `includes/taxonomies/vendor-type.php`, the focused boundary assertions in `tests/vendor-type-query-filter-boundary-remediation.php`, the unchanged protected Ticket Integrity files, and the unchanged read-only live file at `../../vms/includes/taxonomies/vendor-type.php`.<br>The child proved that `vms_vendor_type_maybe_canonicalize_terms()` performs a one-time canonical vendor-type repair pass on `init` and that its event-plan lookup already uses `get_posts()`, which defaults `suppress_filters` to `true`, so the explicit argument was redundant but behaviorally unchanged while remaining useful documentation for the canonical migration boundary.<br>The packaged `WordPressVIPMinimum.Performance.WPQueryParams.SuppressFilters_suppress_filters` family dropped from `1` to `0`, the removed row was exactly the vendor-taxonomy `includes/taxonomies/vendor-type.php:431:13` occurrence, no unrelated packaged code count increased, and the full family is now reconciled through `WPORG-28R-E1` and `WPORG-28R-E2`. | `tests/vendor-type-query-filter-boundary-remediation.php`<br>`tests/event-plan-secondary-vendor-assignments.php`<br>`tests/event-plan-editor-vendor-preservation.php`<br>`tests/event-plan-calendar-resync-isolated.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live and protected-file checksum verification, core-query and VIP-rule inspection, focused vendor-taxonomy reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28re2-post.edcDEd/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Document vendor taxonomy filter suppression` | Child closed under terminal status `verified`.<br>The full packaged `WordPressVIPMinimum.Performance.WPQueryParams.SuppressFilters_suppress_filters` family is now reconciled through `WPORG-28R-E1` and `WPORG-28R-E2`; recommend the follow-up mapping child `WPORG-28R-F0` for the remaining alternative filesystem/helper-function codes.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F0` | `M` | `verified` | Current repository evidence for this documentation-only mapping child is limited to `docs/WPORG_PREREVIEW_REMEDIATION.md`, `docs/wporg-remediation-ledger.md`, the clean disposable package audit recorded in `/tmp/wporg-28rf0-pre.2JcEBj`, and read-only mirror/live/test inspection.<br>The child proved that current `UNMAPPED=39` is exactly one Plugin Check `set_time_limit()` override row plus `38` current WordPressCS alternative-function rows, spanning `13` packaged files and six ordered runtime children: `3` pure helper substitutions, `6` admin CSV/output-stream rows, `12` shared private-file/proof-image rows, `11` Event Plan import staged-file rows, `3` slow-request logger local-rotation rows, and `4` runtime bounded-stream rows.<br>The child also proved that no other rule code contributes to `UNMAPPED`, none of the `39` rows was already reconciled by `WPORG-28R-A`, `WPORG-28R-B`, `WPORG-28R-C1`, `WPORG-28R-C2`, `WPORG-28R-E1`, or `WPORG-28R-E2`, and the then-current parent baseline remained `303 / 1708 / 2011 / 31` with `KNOWN_NONBLOCKING=129`, `UNMAPPED=39`, and `SUBMISSION_BLOCKER=1843`. | `tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh clean public packaging, strict-json packaged Plugin Check reproduction, Plugin Check/WPCS ruleset inspection, read-only mirror/live checksum and semantic-boundary inspection, and the required regression-gate reruns all passed locally on `2026-07-26`.<br>No runtime, test, build, live-tree, or metadata file changed in this child. | current task commit `Map packaged alternative function findings` | Child closed under terminal status `verified` as a roadmap-only documentation slice.<br>`WPORG-28R-F1` later removed the mapped pure helper-substitution rows; recommend `WPORG-28R-F2` next for the remaining admin CSV/output-stream and timeout boundary before continuing `WPORG-28R-F3` through `WPORG-28R-F6`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F1` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only helper substitutions in `includes/core/slow-request-logger.php` and `includes/integrations/ticketing.php`, the new focused tests `tests/slow-request-logger-url-helper-remediation.php` and `tests/ticketing-text-helper-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live files at `../../vms/includes/core/slow-request-logger.php` and `../../vms/includes/integrations/ticketing.php`.<br>The child proved that `vms_slow_request_logger_parse_request_uri()` reads only the path/query components of a trimmed scalar `REQUEST_URI`, so `wp_parse_url()` is semantically equivalent for the two targeted component reads, and that `vms_ticketing_ajax_attach_noise()` only returns admin-facing AJAX debug text in `_vms_ajax_noise`, so `wp_strip_all_tags((string) $noise, false)` preserves the visible text and line-break contract while intentionally dropping script/style contents.<br>The packaged `WordPress.WP.AlternativeFunctions.parse_url_parse_url` family dropped from `2` to `0`, `WordPress.WP.AlternativeFunctions.strip_tags_strip_tags` dropped from `1` to `0`, the package moved from `303 / 1708 / 2011 / 31` to `300 / 1708 / 2008 / 29`, no unrelated packaged code count increased, and no new rule code appeared. | `tests/slow-request-logger-url-helper-remediation.php`<br>`tests/ticketing-text-helper-remediation.php`<br>`tests/slow-request-logger-request-input-characterization.php`<br>`tests/ticketing-output-buffer-lifecycle-characterization.php`<br>`tests/ticketing-v2-ajax-output-buffer-ownership.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh helper-equivalence characterization, public/live checksum verification, focused slow-request and ticketing reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rf1-post.Yet6nj/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Use WordPress URL and text helpers` | Child closed under terminal status `verified`.<br>The pure helper-substitution package residual is now gone; recommend `WPORG-28R-F2` next for the remaining admin CSV/output-stream and timeout boundary.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F2` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only line-specific suppressions in `includes/admin/settings-page.php`, `includes/admin/square-sync-protection.php`, `includes/modules/admissions/admin-ui.php`, and `includes/modules/admissions/pass-claims.php`, the new focused test `tests/admin-export-stream-boundary-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that all six packaged rows belong only to nonce-gated administrator `admin_post_*` CSV exports that stream directly to `php://output`, that installed WordPress `7.0.1` `WP_Filesystem` APIs still operate on filesystem paths and buffered contents rather than on the existing HTTP response stream, that WordPressCS `3.3.0` already allowlists `php://output` for `fopen()` but cannot infer the already-opened stream type when later seeing `fclose()`, and that Plugin Check `2.0.0` locally forbids `set_time_limit()` with no WordPress-native replacement.<br>The packaged `Squiz.PHP.DiscouragedFunctions.Discouraged` family dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.file_system_operations_fclose` dropped from `12` to `7`, the package moved from `300 / 1708 / 2008 / 29` to `295 / 1707 / 2002 / 28`, the exact removed rows were only the six F2-owned occurrences, and no unrelated packaged code count increased or new rule code appeared. | `tests/admin-export-stream-boundary-remediation.php`<br>`tests/settings-integrity-scan-output-remediation.php`<br>`tests/settings-default-venue-alert-output-remediation.php`<br>`tests/administrator-explicit-notice-output-remediation.php`<br>`tests/pass-claims-public-shell-output-remediation.php`<br>`tests/pass-claims-public-status-output-remediation.php`<br>`tests/pass-claims-public-claimed-card-output-remediation.php`<br>`tests/pass-claims-public-success-output-remediation.php`<br>`tests/pass-claims-public-form-output-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, Plugin Check/WPCS and WordPress filesystem inspection, focused export-boundary reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rf2-post.Yh296i/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Document bounded admin export streams` | Child closed under terminal status `verified`.<br>The bounded admin CSV/output-stream and timeout family is now reconciled; recommend `WPORG-28R-F3` next for the shared private-file, proof-image, and safety-download rows before continuing `WPORG-28R-F4` through `WPORG-28R-F6`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F3` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only `wp_delete_file()` substitutions and line-specific `readfile()` / `chmod()` suppressions in `includes/core/private-files.php`, `includes/safety/private-files.php`, `includes/integrations/ticketing-verifications.php`, and `includes/helpers/image-normalization.php`, the new focused test `tests/private-file-operations-boundary-remediation.php`, the updated focused regression harnesses `tests/private-file-upload-api-remediation.php` and `tests/verification-proof-normalization.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/` where they exist.<br>The child proved that all twelve packaged rows belong only to brokered private-file downloads, proof-image normalization, or bounded private-root cleanup, that `wp_is_writable()` is a direct semantic replacement for the two directory writability checks, that `wp_delete_file()` is a direct semantic replacement for the six existing private-root cleanup deletions because the current code already root-validates each path, and that the two streamed `readfile()` rows plus the two explicit `0640` permission rows remain justified native local-file operations with no behaviorally equivalent `WP_Filesystem` alternative.<br>The packaged `WordPress.WP.AlternativeFunctions.unlink_unlink` family dropped from `9` to `3`, `WordPress.WP.AlternativeFunctions.file_system_operations_is_writable` dropped from `3` to `1`, `WordPress.WP.AlternativeFunctions.file_system_operations_readfile` dropped from `3` to `1`, `WordPress.WP.AlternativeFunctions.file_system_operations_chmod` dropped from `3` to `1`, the package moved from `295 / 1707 / 2002 / 28` to `283 / 1707 / 1990 / 28`, the exact removed rows were only the twelve F3-owned occurrences, and no unrelated packaged code count increased or new rule code appeared. | `tests/private-file-operations-boundary-remediation.php`<br>`tests/private-file-upload-api-remediation.php`<br>`tests/verification-proof-normalization.php`<br>`tests/upload-validation-guards.php`<br>`tests/vendor-tax-profile-strict-post-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, WordPress core and WPCS rule inspection, focused private-file and proof-image reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rf3-post.j1oKau/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Normalize and document private-file operations` | Child closed under terminal status `verified`.<br>The shared private-file, proof-image, and safety-download family is now reconciled; recommend `WPORG-28R-F4` next for the shared Event Plan import staged-file rows before continuing `WPORG-28R-F5` through `WPORG-28R-F6`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F4` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only `wp_delete_file()` substitutions and line-specific `chmod()` / `readfile()` / `fopen()` / `fclose()` suppressions in `includes/admin/data-tools/actions-event-plan-import.php` and `includes/services/event-plan-import/event-plan-import-engine.php`, the new focused test `tests/event-plan-import-file-operations-boundary-remediation.php`, the updated focused regression harness `tests/event-plan-import-upload-api-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that all eleven packaged rows belong only to administrator-only Event Plan import preview staging, preview report downloads, sample CSV streaming, or bounded staged-file cleanup, that `wp_delete_file()` is a direct semantic replacement for the validated preview-upload rollback deletion and stored-file cleanup deletion, and that the remaining `chmod()`, `readfile()`, `fopen()`, and `fclose()` boundaries are justified native local-file or response-stream operations with no behaviorally equivalent `WP_Filesystem` alternative.<br>The packaged `WordPress.WP.AlternativeFunctions.unlink_unlink` family dropped from `3` to `1`, `WordPress.WP.AlternativeFunctions.file_system_operations_chmod` dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.file_system_operations_readfile` dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.file_system_operations_fopen` dropped from `3` to `1`, `WordPress.WP.AlternativeFunctions.file_system_operations_fclose` dropped from `7` to `2`, the package moved from `283 / 1707 / 1990 / 28` to `272 / 1707 / 1979 / 26`, the exact removed rows were the eleven F4-owned occurrences, and the only added strict-json row was the unchanged `WordPress.DB.SlowDBQuery.slow_db_query_meta_query` warning in the authorized engine file after its line number shifted from `2112` to `2116`. | `tests/event-plan-import-file-operations-boundary-remediation.php`<br>`tests/event-plan-import-upload-api-remediation.php`<br>`tests/event-plan-import-rows-payload-output-remediation.php`<br>`tests/event-plan-import-inline-js-remediation.php`<br>`tests/decoded-json-validation.php`<br>`tests/upload-validation-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, WordPress core and WPCS rule inspection, focused Event Plan import reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rf4-post.LmQZEH/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Normalize and document Event Plan import staging` | Child closed under terminal status `verified`.<br>The Event Plan import staged-file family is now reconciled; recommend `WPORG-28R-F5` next for the slow-request logger local-rotation rows before continuing to `WPORG-28R-F6`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F5` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only `wp_delete_file_from_directory()` and `wp_is_writable()` substitutions plus the line-specific `rename()` suppression in `includes/core/slow-request-logger.php`, the new focused test `tests/slow-request-logger-rotation-boundary-remediation.php`, the updated pure-PHP logger harness `tests/slow-request-logger-request-input-characterization.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterpart at `../../vms/includes/core/slow-request-logger.php`.<br>The child proved that all three packaged rows belong only to the slow-request logger's bounded same-directory rotation lifecycle, that `wp_is_writable()` is a direct semantic replacement for the directory writability gate, that `wp_delete_file_from_directory()` is a direct semantic replacement for the retained-generation cleanup while adding an explicit same-directory boundary, and that the same-directory native `rename()` remains the least-weak local promotion step because `WP_Filesystem::move()` may introduce credential expectations or copy/delete fallback semantics during request shutdown.<br>The packaged `WordPress.WP.AlternativeFunctions.unlink_unlink` family dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.rename_rename` dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.file_system_operations_is_writable` dropped from `1` to `0`, the package moved from `272 / 1707 / 1979 / 26` to `269 / 1707 / 1976 / 23`, the exact removed rows were only the three F5-owned occurrences, and the four F6 packaged rows remained byte-for-byte unchanged. | `tests/slow-request-logger-rotation-boundary-remediation.php`<br>`tests/slow-request-logger-request-input-characterization.php`<br>`tests/slow-request-logger-url-helper-remediation.php`<br>`tests/event-plan-performance-request-id-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, WordPress core and WPCS rule inspection, focused slow-request logger reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rf5-post.9MqqQd/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Normalize and document slow-request log rotation` | Child closed under terminal status `verified`.<br>The slow-request logger local-rotation family is now reconciled; recommend `WPORG-28R-F6` next for the runtime-guard bounded-stream rows before any fresh final packaged baseline or external WordPress.org action.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-F6` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only line-specific `fopen()` / `fread()` / `fclose()` suppressions in `includes/runtime-guards.php`, the new focused test `tests/runtime-guard-stream-boundary-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterpart at `../../vms/includes/runtime-guards.php`.<br>The child proved that all four packaged rows belong only to `vms_read_limited_stream()`'s bounded `php://input` request-body lifecycle, that the current caller hardcodes `php://input` through `vms_ticketing_v2_read_json_request_payload(int $max_bytes)`, that no request-controlled, user-controlled, database-controlled, constant-controlled, or filter-controlled value contributes to the stream URI, that the read remains bounded to `max_bytes + 1` bytes via `min(8192, $remaining)`, and that open/read failures still fail closed with an empty payload and no warning leakage.<br>The child also proved that `WP_Filesystem` and whole-body helper replacements are not behaviorally equivalent because this helper must preserve bootstrap-safe bounded local-stream reads, warning behavior, and explicit close semantics on the PHP request-body stream.<br>The packaged `WordPress.WP.AlternativeFunctions.file_system_operations_fopen` family dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.file_system_operations_fread` dropped from `1` to `0`, `WordPress.WP.AlternativeFunctions.file_system_operations_fclose` dropped from `2` to `0`, the package moved from `269 / 1707 / 1976 / 23` to `265 / 1707 / 1972 / 20`, the exact removed rows were only the four F6-owned occurrences, and no unrelated packaged code count increased or new rule code appeared. | `tests/runtime-guard-stream-boundary-remediation.php`<br>`tests/request-input-sanitization.php`<br>`tests/upload-validation-guards.php`<br>`tests/public-calendar-user-agent-view-characterization.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, WordPress core and WPCS rule inspection, focused runtime-guard reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-26`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rf6-post.vdryoe/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Document bounded runtime guard input stream` | Child closed under terminal status `verified`.<br>The final packaged alternative-function residual is now reconciled; no further ordered `WPORG-28R-F*` child remains, and the parent stays blocked only by the still-blocking packaged families.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-26` |
| `WPORG-28R-G0` | `M` | `verified` | Current repository evidence for this documentation-only planning child is limited to `docs/WPORG_PREREVIEW_REMEDIATION.md`, `docs/WPORG_PLUGIN_CHECK_TRIAGE_1.0.0.md`, `docs/wporg-remediation-ledger.md`, the fresh disposable `2026-07-26` package artifacts it described, and the current repository state.<br>The child rebuilt the public package, reran the packaged strict-json Plugin Check gate, reproduced the current `265 / 1707 / 1972 / 20` packaged baseline with `KNOWN_NONBLOCKING=129` and `SUBMISSION_BLOCKER=1843`, and reconciled every remaining blocker row into the ordered `WPORG-28R-G1` through `WPORG-28R-G17` roadmap.<br>The exact family sums now reconcile as nonce/input `694`, DB/SQL `1082`, date/time `25`, and logging `42`, with `WPORG-28R-G1` selected as the first implementation child. | `tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh packaging, the required non-mutating validation suites, the packaged strict-json rerun, and the full child-ownership reconciliation all passed locally on `2026-07-26`.<br>No runtime, test, builder, release-metadata, or live-tree file changed in this child. | current task commit `Decompose remaining prereview blocker families` | Child closed under terminal status `verified`.<br>The next implementation child is `WPORG-28R-G1`, and external slug reservation, corrected upload, and reviewer communication remain unauthorized while `WPORG-28R` stays blocked. | `2026-07-26` |
| `WPORG-28R-G1` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only read-only request helpers and narrow suppressions in `includes/modules/staff-tasks/admin-ui.php`, `includes/modules/email-followups/admin-ui.php`, `includes/admin/event-feedback.php`, and `includes/admin/season-dates.php`, the three updated focused source suites `tests/strict-post-gate-remediation.php`, `tests/authorization-boundary-hardening.php`, and `tests/administrator-explicit-notice-output-remediation.php`, the unchanged focused exact-POST suite `tests/admin-request-method-wrapper-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded admin-module read-only query state or already-capability-gated mutation handlers, that the exact literal `REQUEST_METHOD === 'POST'` boundaries in Season Dates and Staff Tasks must remain direct for the existing focused harnesses, that Email Follow-Ups structured settings arrays remain safely centralized under `vms_email_followups_sanitize_settings()`, and that Staff Tasks enum/date/order/settings inputs can be tightened with semantic unslashing and allowlisting without altering the existing mutation nonce or capability guards.<br>The packaged `WordPress.Security.NonceVerification.Recommended` family dropped from `372` to `334`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `134` to `119`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `100` to `94`, moving the package from `265 / 1707 / 1972 / 20` to `265 / 1648 / 1913 / 20`; the exact removed rows were the `59` G1-owned occurrences, no G1 row remained, and the only added strict-json rows were same-file line shifts for deferred DB and accepted `OutputNotEscaped` findings whose counts stayed unchanged. | `tests/strict-post-gate-remediation.php`<br>`tests/admin-request-method-wrapper-remediation.php`<br>`tests/administrator-explicit-notice-output-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, the required focused G1 reruns, broader regression gates, a new disposable public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-27`.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg1-post.ZjwkDI/build --force --allow-dirty` as the authorized dirty-worktree validation step, while the live `../../vms/` tree remained untouched. | current task commit `Reconcile admin module form boundaries` | Child closed under terminal status `verified`.<br>The first residual nonce/input child is now reconciled; recommend `WPORG-28R-G2` next for the remaining admin dashboard and secondary settings request boundaries while keeping the parent blocked.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-27` |
| `WPORG-28R-G2` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only request-helper normalizations and narrow occurrence-specific suppressions in `includes/admin/approvals-review-queue.php`, `includes/admin/continuity-binder.php`, `includes/admin/express-bar.php`, `includes/admin/integrity-calendar-reconcile.php`, `includes/admin/integrity-venue-reconcile.php`, `includes/admin/menu.php`, `includes/admin/reference/keys-map.php`, `includes/admin/schedule.php`, `includes/admin/settings-page.php`, `includes/admin/settings/class-vms-settings-tours.php`, `includes/admin/settings/notifications-user-profile.php`, `includes/admin/square-sync-protection.php`, `includes/admin/staff-tax-sidebar.php`, `includes/admin/staffing.php`, `includes/admin/tax-bypass-ajax.php`, `includes/admin/tax-profile-admin-metabox.php`, `includes/admin/vendor-booking-onboarding.php`, `includes/admin/vendor-command-center.php`, `includes/admin/vendor-comp-packages.php`, `includes/admin/vendor-details.php`, `includes/admin/vendor-staff-link.php`, `includes/admin/vendor-user-link.php`, `includes/admin/venue-calendar.php`, `includes/admin/venue-comp-defaults.php`, `includes/admin/venue-context.php`, and `includes/safety/admin.php`, the updated focused G2 harnesses under `tests/`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded admin dashboard and secondary-settings request state, that read-only GET notice/filter/view routing can safely move behind helper-backed unslashing without adding inappropriate nonces, that existing mutation capability and nonce boundaries remain intact across admin-post, AJAX, settings, and user-profile flows, that the user-profile notifications save path now explicitly verifies `update-user_<id>`, and that the retained structured settings arrays and raw `$_FILES` payloads stay under exact line-level suppressions because downstream sanitizers or upload handlers already own their normalization.<br>The packaged `WordPress.Security.NonceVerification.Missing` family dropped from `85` to `75`, `WordPress.Security.NonceVerification.Recommended` dropped from `334` to `258`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `119` to `91`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `94` to `68`, moving the package from `265 / 1648 / 1913 / 20` to `265 / 1508 / 1773 / 20`; the exact removed rows were the `140` G2-owned occurrences, no G2 row remained, no new rule code appeared, and the same-file deferred DB/date/logging plus accepted `OutputNotEscaped` counts stayed unchanged. | `tests/authorization-boundary-hardening.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/reference-keys-map-inline-js-remediation.php`<br>`tests/vendor-compensation-inline-js-remediation.php`<br>`tests/administrator-explicit-notice-output-remediation.php`<br>`tests/settings-integrity-scan-output-remediation.php`<br>`tests/settings-default-venue-alert-output-remediation.php`<br>`tests/schedule-invalid-bounds-output-remediation.php`<br>`tests/schedule-warning-notice-output-remediation.php`<br>`tests/schedule-unpublished-venue-notice-output-remediation.php`<br>`tests/staffing-admin-inline-assets-remediation.php`<br>`tests/admin-selector-redirect-uri-remediation.php`<br>`tests/private-file-upload-api-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, the required focused G2 reruns, broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-27` while the live `../../vms/` tree remained untouched. | current task commit `Reconcile admin dashboard request boundaries` | Child closed under terminal status `verified`.<br>The second residual nonce/input child is now reconciled; recommend `WPORG-28R-G3` next for the Event Plan editor and core request boundaries while keeping the parent blocked.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-27` |
| `WPORG-28R-G3` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only request-helper normalization and nonce-verified editor POST routing in `includes/cpt/event-plans.php`, the diagnostics-only request snapshots in `includes/core/event-plan-performance.php` and `includes/core/event-plan-save-profiler.php`, the fallback normalized POST helper in `includes/core/event-plan-review.php`, the workflow-status partial request read update, the two updated focused pure-PHP suites `tests/event-plan-performance-request-id-remediation.php` and `tests/event-plan-review-json-characterization.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded Event Plan editor save, render, profiler, review, and diagnostic request boundaries, that editor mutation paths can read through a centralized nonce-verified POST snapshot without changing existing save ordering, that read-only GET/admin request state can move behind helper-backed unslashing without adding inappropriate nonces, and that the retained helper-level suppressions stay limited to diagnostics-only or fallback-only request access while the live tree remains intentionally untouched.<br>The packaged `WordPress.Security.NonceVerification.Missing` family dropped from `75` to `52`, `WordPress.Security.NonceVerification.Recommended` dropped from `258` to `182`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `91` to `61`, `WordPress.Security.ValidatedSanitizedInput.InputNotValidated` dropped from `3` to `0`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `68` to `36`, moving the package from `265 / 1508 / 1773 / 20` to `265 / 1344 / 1609 / 19`; the exact removed rows were the `164` G3-owned occurrences, no G3 row remained, and the same-file deferred DB/logging plus accepted `OutputNotEscaped` counts stayed unchanged. | `tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/event-plan-performance-request-id-remediation.php`<br>`tests/event-plan-readiness-details-output-remediation.php`<br>`tests/decoded-json-validation.php`<br>`tests/event-plan-review-json-characterization.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public/live target characterization, the required focused G3 reruns, broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-28` while the live `../../vms/` tree remained untouched. | current task commit `Reconcile Event Plan editor request boundaries` | Child closed under terminal status `verified`.<br>The third residual nonce/input child is now reconciled; recommend `WPORG-28R-G4` next for the ancillary CPT save boundaries while keeping the parent blocked.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-28` |
| `WPORG-28R-G4` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only request-edge helper routing and narrow occurrence-specific suppressions in `includes/cpt/venues.php`, `includes/cpt/ratings.php`, `includes/cpt/vendors.php`, and `includes/cpt/staff.php`, the updated focused shared-helper suite `tests/authorization-boundary-hardening.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to ancillary CPT save, publish-gate, read-only notice, and shortcode request boundaries, that Venue and Vendor mutation paths can keep their existing nonce and capability ordering while moving scalar and structured request reads to the save edge, that Ratings can separate public submit POST state from read-only GET shortcode arguments without widening into DB work, and that Staff taxonomy saves can accept both the core edit and inline-edit request paths while keeping the retained raw taxonomy array access limited to one exact verified save boundary.<br>The packaged `WordPress.Security.NonceVerification.Missing` family dropped from `52` to `38`, `WordPress.Security.NonceVerification.Recommended` dropped from `182` to `172`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `61` to `54`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `36` to `28`, moving the package from `265 / 1344 / 1609 / 19` to `265 / 1305 / 1570 / 19`; the exact removed rows were the `39` G4-owned occurrences, no G4 row remained, no new rule code appeared, and the same-file deferred DB plus accepted `OutputNotEscaped` counts stayed unchanged. | `tests/staff-cpt-inline-js-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/vendor-tax-profile-strict-post-remediation.php`<br>`tests/request-input-sanitization.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public target characterization, the required focused G4 reruns, broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-28` while the live `../../vms/` tree remained untouched.<br>Follow-up `WPORG-28R-G4-T1` then aligned the obsolete Staff CPT parity source assertion so the corrected focused suite now passes while the live Staff CPT hash remains unchanged and live-tree convergence stays deferred. | current task commit `Align Staff CPT mirror parity harness` | Child closed under terminal status `verified`.<br>The fourth residual nonce/input child remains reconciled; recommend `WPORG-28R-G5` next for the public, portal, and vendor-application request boundaries while keeping the parent blocked.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-29` |
| `WPORG-28R-G4-T1` | `T` | `verified` | Current repository evidence for this test-only closeout includes the drift-aware Staff CPT parity assertions in `tests/staff-cpt-inline-js-remediation.php`, the unchanged mirror Staff runtime file `includes/cpt/staff.php`, the unchanged read-only live counterpart `../../vms/includes/cpt/staff.php`, and the G4 tracker-document addenda that record the aligned parity boundary.<br>The child proved that the pre-edit failure was limited to the obsolete full-file PHP equality assertion, that the live Staff CPT file remained on the untouched SHA-256 `148bd26964f6197bd7568f05e9fc4bd49edd59854999546150ac1d6f84d32432`, that the mirror Staff CPT file intentionally retains the G4-only request-boundary helpers and therefore diverges from live, and that the Staff JS asset still remains byte-identical across mirror/live. | `tests/staff-cpt-inline-js-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/vendor-tax-profile-strict-post-remediation.php`<br>`tests/request-input-sanitization.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | The corrected Staff CPT parity harness, the required focused suites, the broader regression gates, and the fresh packaged invariance proof all passed locally on `2026-07-29` without changing any runtime or live-tree source file. | current task commit `Align Staff CPT mirror parity harness` | Child closed under terminal status `verified`.<br>`WPORG-28R-G4` now closes fully with a green focused-suite set, and the exact next implementation child is now `WPORG-28R-G6` while the parent stays blocked.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-29` |
| `WPORG-28R-G5` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only request-helper normalization and nonce-scoped request-edge routing in `includes/portal/vendor-portal.php`, `includes/public/event-feedback.php`, `includes/vendor-applications.php`, `includes/portal/staff-portal.php`, `includes/portal/vendor-tax-profile.php`, `includes/public/express-bar.php`, `includes/public/calendar-ics.php`, `includes/modules/status-notices/front.php`, and `includes/core/vendor-application-confirmation.php`, the two updated focused source suites `tests/vendor-apply-turnstile-contract-remediation.php` and `tests/vendor-tax-profile-strict-post-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded public read-only navigation, vendor-application status/confirmation flows, authenticated portal views, authenticated portal mutations, and browser-driven public form submissions, that read-only GET state can move behind helper-backed unslashing without inappropriate nonce additions, that the existing portal, confirmation, upload, Turnstile, and vendor-application nonce/ownership boundaries remain intact when request sources are narrowed to their actual lifecycle, and that the final Staff certification upload field can read through the existing nonce-scoped helper without widening the mirror/live divergence boundary.<br>The packaged `WordPress.Security.NonceVerification.Missing` family dropped from `38` to `26`, `WordPress.Security.NonceVerification.Recommended` dropped from `172` to `126`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `54` to `25`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `28` to `12`, moving the package from `265 / 1305 / 1570 / 19` to `265 / 1202 / 1467 / 19`; the exact removed rows were the `103` G5-owned occurrences, no G5 row remained, no new rule code appeared, and the same-file deferred DB/date/logging plus accepted `OutputNotEscaped` and `OffloadedContent` counts stayed unchanged. | `tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/vendor-tax-profile-strict-post-remediation.php`<br>`tests/vendor-apply-turnstile-contract-remediation.php`<br>`tests/event-feedback-request-hash-characterization.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/vendor-application-confirmation-output-remediation.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/runtime-stub-guards.php` | Fresh public target characterization, the required focused G5 reruns, the broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-29` while the live `../../vms/` tree remained untouched.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg5-post2.IWfzrj/build --force --allow-dirty` as the authorized dirty-worktree validation step. | current task commit `Reconcile public and portal request boundaries` | Child closed under terminal status `verified`.<br>The fifth residual nonce/input child is now reconciled; recommend `WPORG-28R-G6` next for the ticketing, admissions, and availability request boundaries while keeping the parent blocked.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-29` |
| `WPORG-28R-G6-T1` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only read-only request-helper normalizations and narrow suppressions in `includes/integrations/ticketing-claims-admin.php`, `includes/integrations/ticketing-claims-customer.php`, `includes/integrations/ticketing-rules-v2.php`, `includes/integrations/ticketing-verifications.php`, and `includes/integrations/ticketing.php`, the two updated focused source suites `tests/ticket-claims-assignee-validation.php` and `tests/ticketing-rules-v2-request-path-normalization.php`, the two new focused request-state suites `tests/ticketing-claims-admin-request-state-remediation.php` and `tests/ticketing-verifications-request-state-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded read-only ticketing claims and verification request state, that claims-page notices, lookup/filter state, account-panel expansion, Woo request-path probes, verification panel state, proof-view IDs, and legacy ticketing admin asset scoping can move behind subsystem-local helper-backed scalar readers without widening any existing mutation path, and that the remaining same-file nonce/input rows in the five touched runtime files all belong to later `T2` or `T5` ownership instead of to this child.<br>The packaged `WordPress.Security.NonceVerification.Recommended` family dropped from `126` to `62`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `25` to `24`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `12` to `11`, while `WordPress.Security.NonceVerification.Missing` stayed at `26`, moving the package from `265 / 1202 / 1467 / 19` to `265 / 1136 / 1401 / 19`; the exact removed rows were the `66` G6-T1-owned occurrences, no G6-T1 row remained, no new rule code appeared, same-file residual nonce/input rows in the five touched files stayed bounded to `35` rows that all map to later `T2` or `T5` ownership, and the untouched live tree remained intentionally unsynchronized. | `tests/ticket-claims-assignee-validation.php`<br>`tests/ticketing-claims-ajax-output-buffer-ownership.php`<br>`tests/ticketing-rules-v2-request-path-normalization.php`<br>`tests/ticketing-v2-ajax-output-buffer-ownership.php`<br>`tests/ticketing-output-buffer-lifecycle-characterization.php`<br>`tests/verification-proof-normalization.php`<br>`tests/private-file-upload-api-remediation.php`<br>`tests/private-file-operations-boundary-remediation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/ticketing-claims-admin-request-state-remediation.php`<br>`tests/ticketing-verifications-request-state-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/request-input-sanitization.php` | Fresh public target characterization, the required focused G6-T1 reruns, the broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-30` while the live `../../vms/` tree remained untouched.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg6-t1-post.X3zo92 --force --allow-dirty`, then normalized the known WP-CLI deprecation banner away from `plugin-check.strict.json` while preserving the raw stdout at `/tmp/wporg-28rg6-t1-post.X3zo92/plugin-check.stdout.raw`. | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G6` remains open; do not treat this child as closing the whole G6 family.<br>The exact next implementation child is `WPORG-28R-G6-T2`, and no upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-30` |
| `WPORG-28R-G6-T2` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only mutation-boundary helpers and narrow suppressions in `includes/integrations/ticketing-claims-admin.php`, `includes/integrations/ticketing-claims-customer.php`, `includes/integrations/ticketing-verifications.php`, and `includes/integrations/ticketing.php`, the mirror-only front-end payload alignment in `assets/vms-ticketing-front.js`, the updated focused source suite `tests/ticketing-claims-mutation-request-remediation.php`, the two new focused mutation suites `tests/ticketing-verifications-mutation-request-remediation.php` and `tests/ticketing-search-request-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded ticketing claims and verification mutation/request-shape boundaries under `Normalize ticketing mutation request boundaries`, that existing admin-post, AJAX, redirect, and user-profile nonce ordering can remain intact while the remaining POST reads move behind subsystem-local helpers, that `existing_counts` plus the verification arrays now reject malformed top-level shapes before unslashing, that the front-end claims validator now submits `existing_counts[...]` array fields matching the tightened POST boundary, and that the deferred whole-`$_POST` `includes/integrations/ticketing-rules-v2.php` rows still belong to `WPORG-28R-G6-T5`.<br>The packaged `WordPress.Security.NonceVerification.Missing` family dropped from `26` to `8`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `24` to `14`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `11` to `6`, while `WordPress.Security.NonceVerification.Recommended` stayed at `62`, moving the package from `265 / 1136 / 1401 / 19` to `265 / 1103 / 1368 / 19`; the exact removed rows were the `33` G6-T2-owned occurrences, no G6-T2 row remained, no new rule code appeared, and no nonce/input row remained in the four touched PHP runtime files. | `tests/ticket-claims-assignee-validation.php`<br>`tests/ticketing-claims-ajax-output-buffer-ownership.php`<br>`tests/ticketing-claims-mutation-request-remediation.php`<br>`tests/ticketing-verifications-mutation-request-remediation.php`<br>`tests/ticketing-search-request-remediation.php`<br>`tests/ticketing-output-buffer-lifecycle-characterization.php`<br>`tests/verification-proof-normalization.php`<br>`tests/private-file-upload-api-remediation.php`<br>`tests/private-file-operations-boundary-remediation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/ticketing-claims-admin-request-state-remediation.php`<br>`tests/ticketing-verifications-request-state-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/request-input-sanitization.php` | Fresh public target characterization, the required focused G6-T2 reruns, the broader regression gates, syntax lint across the changed PHP and front-end files, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-30` while the live `../../vms/` tree remained untouched.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg6-t2-post-fix.YIVQdN --force --allow-dirty` as the authorized dirty-worktree validation step. | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G6` remains open; do not treat this child as closing the whole G6 family.<br>The exact next implementation child is `WPORG-28R-G6-T3`, and no upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-30` |
| `WPORG-28R-G6-T3` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only read-only request-helper normalizations and narrow suppressions in `includes/modules/admissions/admin-ui.php`, `includes/modules/admissions/admission-tokens.php`, `includes/modules/admissions/pass-claims.php`, `includes/modules/admissions/vendor-guest-portal.php`, `includes/modules/availability-date-dispatch/admin-ui.php`, and `includes/modules/availability-date-dispatch/helpers.php`, the updated focused source suite `tests/add-dispatch-menu-badge-inline-assets-remediation.php`, the three new focused request-state suites `tests/admissions-read-only-request-state-remediation.php`, `tests/admission-token-request-state-remediation.php`, and `tests/availability-date-dispatch-request-state-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded admissions and availability read-only page, tab, notice, filter, identifier, and token request state, that passive navigation can move behind helper-backed scalar, integer, and boolean readers without adding artificial nonces, that verifier-facing token reads now reject array/object input while preserving the existing nonce or token contract, and that the only same-file nonce/input row left in the six touched runtime files is the vendor-guest nested `$_POST['vms_vendor_guest_rules']` mutation boundary already assigned to `WPORG-28R-G6-T5`.<br>The packaged `WordPress.Security.NonceVerification.Recommended` family dropped from `62` to `32`, `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `14` to `12`, and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` dropped from `6` to `4`, while `WordPress.Security.NonceVerification.Missing` stayed at `8`, moving the package from `265 / 1103 / 1368 / 19` to `265 / 1069 / 1334 / 19`; the exact removed rows were the `34` G6-T3-owned occurrences, no G6-T3 row remained, no new rule code appeared, no unrelated rule count increased, and the untouched live tree remained intentionally unsynchronized. | `tests/admissions-rest-permissions.php`<br>`tests/pass-claims-public-form-output-remediation.php`<br>`tests/pass-claims-public-shell-output-remediation.php`<br>`tests/pass-claims-public-status-output-remediation.php`<br>`tests/pass-claims-public-success-output-remediation.php`<br>`tests/portal-notice-sink-remediation.php`<br>`tests/add-dispatch-menu-badge-inline-assets-remediation.php`<br>`tests/admissions-read-only-request-state-remediation.php`<br>`tests/admission-token-request-state-remediation.php`<br>`tests/availability-date-dispatch-request-state-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/request-input-sanitization.php` | Fresh public target characterization, the required focused G6-T3 reruns, the broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-30` while the live `../../vms/` tree remained untouched.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg6-t3-post.ldgG1s --force --allow-dirty` as the authorized dirty-worktree validation step.<br>The two directly relevant ADD baseline conditions remained unchanged and documented: `php tests/add-dispatch-assignment-review.php` still reports `Could not locate wp-load.php.`, and `php tests/add-dispatch-open-vendor-needs.php` still reports `ADD open vendor needs visibility regression: FAIL - Future Event Plan with missing Primary Vendor should appear in ADD open needs.` | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G6` remains open; do not treat this child as closing the whole G6 family.<br>The exact next implementation child is `WPORG-28R-G6-T4`, and no upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-30` |
| `WPORG-28R-G6-T4` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror-only passive-request helper normalizations and narrow suppressions in `includes/core/plugin.php`, `includes/core/registry/admin-menu.php`, `includes/core/slow-request-logger.php`, `includes/core/tours/class-vms-tours.php`, `includes/helpers.php`, `includes/runtime-guards.php`, `includes/tours/class-vms-tours-screen.php`, and `includes/tours/class-vms-tours-service.php`, the two updated slow-request characterization suites `tests/slow-request-logger-request-input-characterization.php` and `tests/slow-request-logger-url-helper-remediation.php`, the drift-aware Tours parity update in `tests/tours-user-state-json-characterization.php`, the three new focused request-state suites `tests/runtime-guard-request-state-remediation.php`, `tests/passive-display-state-remediation.php`, and `tests/tours-passive-request-state-remediation.php`, the disposable pre/post package artifacts recorded in the tracker docs, and the unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the owned packaged rows belong only to bounded shared-wrapper reads, passive admin/menu/display state, tours screen selection, slow-request diagnostics, and runtime-guard request/server probes, that the dynamic `vms_admin_guard_request_value()` key path now enforces an allowlist without widening any caller contract, that passive page, post type, post, and action reads can move behind helper-backed scalar or integer readers without adding artificial nonces, and that the only same-file nonce/input rows left in the touched runtime files are the preserved `WPORG-28R-G6-T5` residuals `B104`, `B113`, and `B114` in `includes/runtime-guards.php` plus `B122` in `includes/tours/class-vms-tours-service.php`.<br>The packaged `WordPress.Security.NonceVerification.Recommended` family dropped from `32` to `0` and `WordPress.Security.ValidatedSanitizedInput.InputNotSanitized` dropped from `12` to `10`, while `WordPress.Security.NonceVerification.Missing` stayed at `8` and `WordPress.Security.ValidatedSanitizedInput.MissingUnslash` stayed at `4`, moving the package from `265 / 1069 / 1334 / 19` to `265 / 1035 / 1300 / 18`; the exact removed rows were the `34` G6-T4-owned occurrences, no G6-T4 row remained, all `22` preserved `WPORG-28R-G6-T5` rows remained, no new rule code appeared, and no unrelated rule count increased. | `tests/reference-keys-map-inline-js-remediation.php`<br>`tests/slow-request-logger-request-input-characterization.php`<br>`tests/slow-request-logger-url-helper-remediation.php`<br>`tests/slow-request-logger-rotation-boundary-remediation.php`<br>`tests/admin-selector-redirect-uri-remediation.php`<br>`tests/tours-user-state-json-characterization.php`<br>`tests/guided-tours-reset-notice-output-remediation.php`<br>`tests/runtime-guard-request-state-remediation.php`<br>`tests/passive-display-state-remediation.php`<br>`tests/tours-passive-request-state-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/strict-post-gate-remediation.php`<br>`tests/request-input-sanitization.php`<br>`tests/admin-request-method-wrapper-remediation.php` | Fresh public target characterization, the required focused G6-T4 reruns, the broader regression gates, PHP lint across every changed PHP file, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-07-30` while the live `../../vms/` tree remained untouched.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg6-t4-post.tWCP83 --force --allow-dirty` as the authorized dirty-worktree validation step, and the packaged strict-json scan required a temporary inactive installed copy of the extracted public package because this local WP-CLI accepted installed plugin slugs rather than arbitrary extracted paths; that temporary copy was removed immediately after the scan. | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G6` remains open; only `WPORG-28R-G6-T5 — Phase-B/V2 mutation arrays and remaining shared mutation helpers` remains active inside the family.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-07-30` |
| `WPORG-28R-G6-T5` | `M` | `verified` | Current repository evidence for this runtime child includes mirror-only request-boundary normalization in `includes/admin/data-tools/actions-event-plan-import.php`, `includes/integrations/ticketing-phase-b.php`, `includes/integrations/ticketing-rules-v2.php`, `includes/modules/admissions/vendor-guest-portal.php`, `includes/runtime-guards.php`, `includes/social-share/admin.php`, `includes/taxonomies/vendor-category.php`, `includes/ticketing/ticket-integrity-cron.php`, and `includes/tours/class-vms-tours-service.php`; the new focused suite `tests/wporg-28r-g6-t5-mutation-request-boundaries.php`; the directly invalidated support-harness update in `tests/ticketing-phase-b-ajax-output-buffer-ownership.php`; the disposable evidence directory `/tmp/wporg-28rg6-t5-work.H3iuzL`; and unchanged read-only live counterparts under `../../vms/`.<br>The child proved that the final `22` packaged nonce/input rows across `17` boundaries belong only to Phase-B/V2 mutation arrays and remaining shared mutation helpers, that structured arrays are top-level shape-checked before one unslash pass, that malformed scalar/array/object values fail closed at the request boundary, and that existing nonce names, nonce actions, capabilities, authentication, ownership checks, hooks, redirects, notices, JSON responses, cron behavior, persistence keys, package identity, and release exclusions remain unchanged.<br>The packaged nonce/input family dropped from `Missing 8`, `Recommended 0`, `InputNotSanitized 10`, and `MissingUnslash 4` to zero, moving the package from `265 / 1035 / 1300 / 18` to `265 / 1013 / 1278 / 15`; all `22` T5 rows disappeared, no T1-T4 row returned, no new rule code appeared, no unrelated rule count increased, and `SUBMISSION_BLOCKER=1149`. | `tests/wporg-28r-g6-t5-mutation-request-boundaries.php`<br>`tests/ticketing-phase-b-ajax-output-buffer-ownership.php`<br>`tests/ticket-integrity-query-filter-boundary-remediation.php`<br>`tests/vendor-type-query-filter-boundary-remediation.php`<br>`tests/administrator-explicit-notice-output-remediation.php`<br>`tests/ticketing-exception-message-boundary-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php`<br>Full frozen support inventory recorded at `/tmp/wporg-28rg6-t5-work.H3iuzL/pre-edit-support-scope.txt` | Fresh focused T5 verification, the 49-command frozen support inventory, PHP lint across every changed PHP file, a dirty public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-08-01` while the live `../../vms/` tree remained untouched.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir /tmp/wporg-28rg6-t5-work.H3iuzL/post-build --allow-dirty --force`; the final ZIP SHA-256 is `c07e34a156a41f6d0971da225470968b16ce8477fb8ec27679fdf315b8b49087`; normalized strict JSON is `/tmp/wporg-28rg6-t5-work.H3iuzL/post-plugin-check/plugin-check.strict.json`. | `1ee8f36604c60c59d3e989ac7c5fa5b71846b7d1` (`Normalize remaining mutation request boundaries`) | Child closed under terminal status `verified`.<br>`WPORG-28R-G6` is now terminal under `verified` with `189 -> 0` rows and `122 -> 0` boundaries.<br>`WPORG-28R-G7` remains retired, `WPORG-28R` remains blocked, and the exact next implementation child is `WPORG-28R-G8 — Staffing And Staff-Task Query Boundaries`.<br>No upload, deployment, reviewer reply, live-tree change, or production convergence occurred. | `2026-08-01` |
| `WPORG-28R-G8` | `M` | `verified` | Current repository evidence for this child now includes the original docs-only inventory and decomposition artifacts under `/tmp/wporg-28rg8-inventory.Z0hOmS`, the verified runtime children `WPORG-28R-G8-T1`, `WPORG-28R-G8-T2`, `WPORG-28R-G8-T3`, `WPORG-28R-G8-T4`, and `WPORG-28R-G8-T5`, the disposable implementation evidence directories `/tmp/wporg-28rg8-t1-work.2rY5sN`, `/tmp/wporg-28rg8-t2-work.54u1i4`, `/tmp/wporg-28rg8-t3-work.qiAdPz`, `/tmp/wporg-28rg8-t4-work.4CeoYs`, and `/tmp/wporg-28rg8-t5-work.ctlcQI`, the updated focused repository suites, and the synchronized Staff Tasks, Staff Portal, and staffing mirror/live counterparts under `../../vms/`.<br>The child proved that the tracker-owned staffing/staff-task DB/SQL family originally reconciled exactly to `233` packaged rows across `11` files and `52` unique function/lifecycle boundaries, that verified children `WPORG-28R-G8-T1` through `WPORG-28R-G8-T5` removed the full `233` rows across all `52` boundaries, and that no G8-owned packaged row remains after the final slow-meta-query and reverse-link slice.<br>The planning-only completion target for a full successful G8 closeout is now realized at `849` remaining DB/SQL blockers and `916` overall blockers, and the exact next implementation child is `WPORG-28R-G9`.<br>The preserved `g8-boundaries.tsv` still assigns `G8-B002` to `vms_staff_user_link_metabox_render()` lines `26-79`, but the current packaged slow-meta query is the later `save_post_vms_staff` closure at line `131`; the terminal post-`T5` accounting therefore uses `/tmp/wporg-28rg8-t5-work.ctlcQI/scope/current-boundary-ranges.tsv`, and the stale mapping does not affect `T1`, `T2`, `T3`, `T4`, or `T5` ownership counts. | `tests/staffing-final-repository-sql-remediation.php`<br>`tests/staffing-matrix-rollup-reporting-repository-sql-remediation.php`<br>`tests/staffing-repository-sql-remediation.php`<br>`tests/staff-task-instance-and-portal-repository-sql-remediation.php`<br>`tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh dirty public packaging, a fresh packaged strict-json Plugin Check rerun with empty stderr, the original plus `T1`/`T2`/`T3` mirror/live audits, the `T4` focused parity coverage, the final `T5` parity coverage, and the focused plus support suites all passed locally on `2026-08-02` while live `store.php`, `admin-ui.php`, `db.php`, `staff-portal.php`, `staffing.php`, `admin/staffing.php`, `staff-list-columns.php`, `staff-user-link.php`, `staff-vendor-link.php`, `vendor-staff-link.php`, and `generator.php` stayed synchronized according to the authorized full-file or owned-boundary parity model.<br>The packaged baseline now stands at `239 / 806 / 1045 / 15` with `KNOWN_ACCEPTED=0`, `KNOWN_NONBLOCKING=129`, `NEW_FINDING=0`, `UNMAPPED=0`, `SUBMISSION_BLOCKER=916`, `DB/SQL=849`, `nonce/input=0`, `date/time=25`, and `logging=42`. | `dd84ae031d034d8cde14bbc826444f9d59793579` (`Characterize staffing reverse-link query fallbacks`) | `WPORG-28R-G8` is now terminal under `verified`.<br>`WPORG-28R-G8-T1`, `WPORG-28R-G8-T2`, `WPORG-28R-G8-T3`, `WPORG-28R-G8-T4`, and `WPORG-28R-G8-T5` are terminal under `verified`, the exact next implementation child is `WPORG-28R-G9`, and no upload, deployment, reviewer reply, or production convergence occurred in this child. | `2026-08-02` |
| `WPORG-28R-G8-T1` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror/live SQL boundary remediation in `includes/modules/staff-tasks/store.php`, `includes/modules/staff-tasks/admin-ui.php`, and `includes/modules/staff-tasks/db.php`; the focused suite `tests/staff-tasks-repository-sql-remediation.php`; the support reruns recorded in `/tmp/wporg-28rg8-t1-work.2rY5sN/post-edit-tests-final.1785625982`; the lint and `git diff --check` evidence recorded in `/tmp/wporg-28rg8-t1-work.2rY5sN/post-edit-lints-final.1785625982`; the final package and packaged Plugin Check artifacts recorded in `/tmp/wporg-28rg8-t1-work.2rY5sN/post-build-final.1785626108` and `/tmp/wporg-28rg8-t1-work.2rY5sN/post-plugin-check-final.1785626169`; and the synchronized live counterparts under `../../vms/`.<br>The child proved that the owned `48` packaged rows across `13` boundaries belong only to Staff Tasks template, checklist, and admin selector repositories, that bounded identifier handling plus completed preparation removes the packaged `UnescapedDBParameter`, `DirectQuery`, `NoCaching`, `InterpolatedNotPrepared`, and `NotPrepared` rows without changing any non-DB blocker family, and that mirror/live untouched regions stayed preserved while `store.php` and `db.php` remained byte-identical after synchronization.<br>The packaged baseline moved from `265 / 1013 / 1278 / 15` to `254 / 976 / 1230 / 15`; the exact removed rows were the `48` T1-owned occurrences, no T1 row remained, no new rule code appeared, no unrelated blocker family increased, and `SUBMISSION_BLOCKER=1101`. | `tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh focused T1 verification, the required support reruns, PHP lint across mirror/live runtime files plus the focused suite, `git diff --check`, a dirty public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-08-01` while the live `../../vms/` tree was synchronized only inside the authorized target functions.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir '/tmp/wporg-28rg8-t1-work.2rY5sN/post-build-final.1785626108' --allow-dirty --force`; the final ZIP SHA-256 is `318fc1c913f664a9778eaeedf8a88816eab44444a2cba8a47ecb753c788486a8`; normalized strict JSON is `/tmp/wporg-28rg8-t1-work.2rY5sN/post-plugin-check-final.1785626169/plugin-check.strict.json`. | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G8-T2`, `WPORG-28R-G8-T3`, and `WPORG-28R-G8-T4` later closed under `verified`, `WPORG-28R-G8` remains `in progress` with the remaining `14` rows assigned to `T5`, and the exact next implementation child is `WPORG-28R-G8-T5`.<br>No upload, deployment, reviewer reply, or production convergence occurred. | `2026-08-01` |
| `WPORG-28R-G8-T2` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror/live SQL boundary remediation in `includes/modules/staff-tasks/store.php` and `includes/portal/staff-portal.php`; the focused suite `tests/staff-task-instance-and-portal-repository-sql-remediation.php`; the directly invalidated support update in `tests/staff-tasks-repository-sql-remediation.php`; the support reruns recorded in `/tmp/wporg-28rg8-t2-work.54u1i4/post-edit-tests`; the final package and packaged Plugin Check artifacts recorded in `/tmp/wporg-28rg8-t2-work.54u1i4/post-build` and `/tmp/wporg-28rg8-t2-work.54u1i4/plugin-check-post`; the preserved-root after-snapshot recorded in `/tmp/wporg-28rg8-t2-work.54u1i4/evidence-after`; and the synchronized live counterparts under `../../vms/`.<br>The child proved that the owned `53` packaged rows across `12` boundaries belong only to task-instance assignment, supersession, and staff-portal read models; that bounded identifier handling plus completed preparation removes the packaged `UnescapedDBParameter`, `DirectQuery`, `NoCaching`, `InterpolatedNotPrepared`, `NotPrepared`, `slow_meta_key`, and `slow_meta_value` rows without changing any non-DB blocker family; that the reverse-link scheduled-role fallback can stay bounded by moving from `get_users()` to a prepared `usermeta` lookup; and that mirror/live untouched regions stayed preserved while `store.php` remained byte-identical after synchronization and `staff-portal.php` still diverged only outside the owned portal helpers.<br>The packaged baseline moved from `254 / 976 / 1230 / 15` to `246 / 931 / 1177 / 15`; the exact removed rows were the `53` T2-owned occurrences, no T1 or T2 row remained, no new rule code appeared, no unrelated blocker family increased, and `SUBMISSION_BLOCKER=1048`. | `tests/staff-task-instance-and-portal-repository-sql-remediation.php`<br>`tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh focused T2 verification, the required support reruns, PHP lint across mirror/live runtime files plus both SQL suites, a dirty public package build, a fresh strict-json packaged Plugin Check rerun, preserved-root before/after evidence capture, and mirror/live parity audits all passed locally on `2026-08-02` while the live `../../vms/` tree was synchronized only inside the authorized target functions.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir '/tmp/wporg-28rg8-t2-work.54u1i4/post-build' --allow-dirty --force`; the final ZIP SHA-256 is `ff397a96d1e77075910683dbec028291d71cbd2ad825f9585173a12c97815348`; normalized strict JSON is `/tmp/wporg-28rg8-t2-work.54u1i4/plugin-check-post/plugin-check.strict.json`; and `/tmp/wporg-28rg8-t2-work.54u1i4/plugin-check-post/target-row-summary.tsv` proves `t1_rows=0` plus `t2_rows=0`. | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G8-T3` and `WPORG-28R-G8-T4` later closed under `verified`, `WPORG-28R-G8` remains `in progress` with the remaining `14` rows assigned to `T5`, and the exact next implementation child is `WPORG-28R-G8-T5`.<br>No upload, deployment, reviewer reply, or production convergence occurred. | `2026-08-02` |
| `WPORG-28R-G8-T3` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror/live SQL boundary remediation in `includes/core/staffing.php`; the focused suite `tests/staffing-repository-sql-remediation.php`; the support reruns recorded in `/tmp/wporg-28rg8-t3-work.qiAdPz/post-edit-tests-final3.1785651654`; the parity and diff evidence recorded in `/tmp/wporg-28rg8-t3-work.qiAdPz/post-edit-structure-final.1785651724`; the final package and packaged Plugin Check artifacts recorded in `/tmp/wporg-28rg8-t3-work.qiAdPz/post-build-final.1785651766` and `/tmp/wporg-28rg8-t3-work.qiAdPz/plugin-check-post-final.1785651766`; and the synchronized live counterpart under `../../vms/`.<br>The child proved that the owned `62` packaged rows across `12` boundaries belong only to staffing template and event-slot repositories, that bounded identifier handling plus completed preparation removes the packaged `UnescapedDBParameter`, `DirectQuery`, `NoCaching`, `InterpolatedNotPrepared`, and `NotPrepared` rows without changing any non-DB blocker family, and that the extracted target functions remained byte-identical after sync while the whole `staffing.php` files still diverge only outside the owned regions.<br>The packaged baseline moved from `246 / 931 / 1177 / 15` to `239 / 876 / 1115 / 15`; the exact removed rows were the `62` T3-owned occurrences, no T1, T2, or T3 row remained, no new rule code appeared, no unrelated blocker family increased, and `SUBMISSION_BLOCKER=986`. | `tests/staffing-repository-sql-remediation.php`<br>`tests/staff-task-instance-and-portal-repository-sql-remediation.php`<br>`tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh focused T3 verification, the required T1 and T2 continuity reruns, the required support reruns, PHP lint across mirror/live `staffing.php` plus the focused suite, `git diff --check`, a dirty public package build, a fresh strict-json packaged Plugin Check rerun, and mirror/live parity audits all passed locally on `2026-08-02` while the live `../../vms/includes/core/staffing.php` tree was synchronized only inside the authorized target functions.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir '/tmp/wporg-28rg8-t3-work.qiAdPz/post-build-final.1785651766' --allow-dirty --force`; the final ZIP SHA-256 is `8fb40748ecd07c761db9971a2136d34c8841341214354f324280c79e5f9dff82`; normalized strict JSON is `/tmp/wporg-28rg8-t3-work.qiAdPz/plugin-check-post-final.1785651766/plugin-check.strict.json`; and `/tmp/wporg-28rg8-t3-work.qiAdPz/plugin-check-post-final.1785651766/t3-post-summary.tsv` proves `owned_rows=0` plus `owned_boundaries=0`. | none; no commit authorized | Child closed under terminal status `verified`.<br>`WPORG-28R-G8-T4` later closed under `verified`, `WPORG-28R-G8` remains `in progress` with the remaining `14` rows assigned to `T5`, and the exact next implementation child is `WPORG-28R-G8-T5`.<br>No upload, deployment, reviewer reply, or production convergence occurred. | `2026-08-02` |
| `WPORG-28R-G8-T4` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror/live SQL boundary remediation in `includes/core/staffing.php` and `includes/admin/staffing.php`; the focused suite `tests/staffing-matrix-rollup-reporting-repository-sql-remediation.php`; the continuity inventory update in `tests/staffing-repository-sql-remediation.php`; the support reruns, parity coverage, and lint evidence recorded under `/tmp/wporg-28rg8-t4-work.4CeoYs`; the final package and packaged Plugin Check artifacts recorded in `/tmp/wporg-28rg8-t4-work.4CeoYs/post-build-v2` and `/tmp/wporg-28rg8-t4-work.4CeoYs/post-plugin-check-v2`; and the synchronized live counterparts under `../../vms/`.<br>The child proved that the owned `56` packaged rows across `6` boundaries belong only to staffing matrix, dirty-mark, rollup, admissions headcount, and rollups-admin reporting repositories, that bounded identifier handling plus completed preparation removes the packaged `UnescapedDBParameter`, `DirectQuery`, `NoCaching`, and `InterpolatedNotPrepared` rows without changing any non-DB blocker family, and that the extracted target functions remained byte-identical after sync while the whole admin/core staffing files still diverge only outside the owned regions.<br>The packaged baseline moved from `239 / 876 / 1115 / 15` to `239 / 820 / 1059 / 15`; the exact removed rows were the `56` T4-owned occurrences, no T1, T2, T3, or T4 row remained, no new rule code appeared, no unrelated blocker family increased, and `SUBMISSION_BLOCKER=930`. | `tests/staffing-matrix-rollup-reporting-repository-sql-remediation.php`<br>`tests/staffing-repository-sql-remediation.php`<br>`tests/staff-task-instance-and-portal-repository-sql-remediation.php`<br>`tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh focused T4 verification, the required T1/T2/T3 continuity reruns, the required support reruns, PHP lint across mirror/live `includes/core/staffing.php` and `includes/admin/staffing.php` plus both staffing SQL suites, a dirty public package build, a fresh strict-json packaged Plugin Check rerun, and mirror/live owned-function parity audits all passed locally on `2026-08-02` while the live `../../vms/` tree was synchronized only inside the authorized target functions.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir '/tmp/wporg-28rg8-t4-work.4CeoYs/post-build-v2' --allow-dirty --force`; the final ZIP SHA-256 is `df54605b68647c4b503dc30795b8e78e0aa76eaff0300dd7e90e2f630b75c8c5`; normalized strict JSON is `/tmp/wporg-28rg8-t4-work.4CeoYs/post-plugin-check-v2/plugin-check.strict.json`; `/tmp/wporg-28rg8-t4-work.4CeoYs/post-plugin-check-v2/t4-post-summary.tsv` proves `owned_rows=0` plus `owned_boundaries=0`; and `/tmp/wporg-28rg8-t4-work.4CeoYs/post-plugin-check-v2/t5-post-summary.tsv` proves the remaining `T5` ownership is exactly `14` rows across `9` boundaries. | current task commit `Normalize staffing matrix and rollup repositories` | Child closed under terminal status `verified`.<br>`WPORG-28R-G8` remains `in progress` with the remaining `14` rows assigned to `T5`, and the exact next implementation child is `WPORG-28R-G8-T5`.<br>No upload, deployment, reviewer reply, or production convergence occurred. | `2026-08-02` |
| `WPORG-28R-G8-T5` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror/live SQL boundary remediation in `includes/admin/staff-list-columns.php`, `includes/admin/staff-user-link.php`, `includes/admin/staff-vendor-link.php`, `includes/admin/vendor-staff-link.php`, `includes/core/staffing.php`, `includes/modules/staff-tasks/admin-ui.php`, and `includes/modules/staff-tasks/generator.php`; the focused suite `tests/staffing-final-repository-sql-remediation.php`; the continuity inventory updates in `tests/staffing-matrix-rollup-reporting-repository-sql-remediation.php` and `tests/staffing-repository-sql-remediation.php`; the analysis artifacts under `/tmp/wporg-28rg8-t5-work.ctlcQI/analysis/post-edit`; the final package and packaged Plugin Check artifacts under `/tmp/wporg-28rg8-t5-work.ctlcQI/post-build` and `/tmp/wporg-28rg8-t5-work.ctlcQI/post-plugin-check`; and the synchronized live counterparts under `../../vms/`.<br>The child proved that the owned `14` packaged rows across `9` boundaries belong only to slow meta queries and reverse-link fallbacks, that prepared direct `usermeta`, `postmeta`, and `posts` reads remove the packaged `slow_meta_key`, `slow_meta_query`, and `slow_meta_value` rows without changing any non-DB blocker family, and that the final staffing-family mirror/live parity model stayed intact across both fully synchronized files and the intentionally divergent owned-boundary files.<br>The packaged baseline moved from `239 / 820 / 1059 / 15` to `239 / 806 / 1045 / 15`; the exact removed rows were the `14` T5-owned occurrences, no T1, T2, T3, or T4 row remained, no new rule code appeared, no unrelated blocker family increased, and `SUBMISSION_BLOCKER=916`. | `tests/staffing-final-repository-sql-remediation.php`<br>`tests/staffing-matrix-rollup-reporting-repository-sql-remediation.php`<br>`tests/staffing-repository-sql-remediation.php`<br>`tests/staff-task-instance-and-portal-repository-sql-remediation.php`<br>`tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh focused T5 verification, the required T1/T2/T3/T4 continuity reruns, the required support reruns, PHP lint across the mirror/live runtime files plus the staffing SQL suites, a dirty public package build, a fresh strict-json packaged Plugin Check rerun, and final ownership reconciliation all passed locally on `2026-08-02` while the live `../../vms/` tree was synchronized only inside the authorized target functions.<br>The successful post-edit package used `php scripts/build-public-release.php --output-dir '/tmp/wporg-28rg8-t5-work.ctlcQI/post-build' --allow-dirty --force`; the final ZIP SHA-256 is `09e768d6afd87d31530436729362d68848e29a20c117993e16f81eadf8562fe8`; the packaged Plugin Check command is preserved at `/tmp/wporg-28rg8-t5-work.ctlcQI/post-plugin-check/plugin-check.command.txt`; the raw strict JSON is `/tmp/wporg-28rg8-t5-work.ctlcQI/post-plugin-check/plugin-check.stdout.raw`; and `/tmp/wporg-28rg8-t5-work.ctlcQI/analysis/post-edit/ownership-reconciliation.txt` proves `t5_rows=0`, `t5_boundaries=0`, `g8_db_rows=0`, and `non_t5_g8_rows=0`. | current task commit `Characterize staffing reverse-link query fallbacks` | Child closed under terminal status `verified`.<br>`WPORG-28R-G8` is now terminal under `verified`, the exact next implementation child is `WPORG-28R-G9`, and no upload, deployment, reviewer reply, or production convergence occurred. | `2026-08-02` |
| `WPORG-28R-G9` | `M` | `verified` | Current repository evidence for this runtime child includes the mirror/live SQL boundary remediation in `includes/modules/admissions/admin-ui.php`, `includes/modules/admissions/admission-tokens.php`, `includes/modules/admissions/audit.php`, `includes/modules/admissions/db.php`, `includes/modules/admissions/pass-claims.php`, `includes/modules/admissions/rest.php`, and `includes/modules/admissions/vendor-guest-portal.php`; the dedicated inventory harness `tests/admissions-claim-state-query-boundaries-remediation.php`; the validation-only companion updates in `tests/admissions-rest-patch-restore.php` and `tests/staffing-final-repository-sql-remediation.php`; the preserved evidence directories `/tmp/wporg-28rg9-work.4s75wi` and `/tmp/wporg-28rg9-finalize.mpwQFZ`; and the synchronized live counterparts under `../../vms/`.<br>The child proved that the owned `255` packaged rows across `39` documented boundaries belong only to admissions and claim-state query boundaries spanning administrator exports, claim locks and counters, admission token issuance, vendor guest portal history and cancel flows, scan and check-in REST helpers, and bounded schema or audit custom-table access; that `%i` identifier preparation plus completed placeholder coverage remove the packaged `UnescapedDBParameter`, `DirectQuery`, `NoCaching`, `InterpolatedNotPrepared`, `NotPrepared`, `UnfinishedPrepare`, `slow_meta_key`, and `slow_meta_query` rows without widening into the deferred same-file input or logging families; and that `rest.php`, `audit.php`, and `admission-tokens.php` remained fully synchronized while the intentionally divergent admissions files were synced only inside the owned G9 regions.<br>The packaged baseline moved from `239 / 806 / 1045 / 15` to `228 / 562 / 790 / 15`; the exact removed rows were the `255` G9-owned occurrences, no G9 row remained, no new rule code appeared, no unrelated blocker family increased, and `SUBMISSION_BLOCKER=661`. | `tests/admissions-claim-state-query-boundaries-remediation.php`<br>`tests/pass-claims-public-form-output-remediation.php`<br>`tests/pass-claims-public-shell-output-remediation.php`<br>`tests/pass-claims-public-status-output-remediation.php`<br>`tests/pass-claims-public-success-output-remediation.php`<br>`tests/pass-claims-public-claimed-card-output-remediation.php`<br>`tests/admissions-rest-permissions.php`<br>`tests/admissions-rest-patch-restore.php`<br>`tests/admissions-read-only-request-state-remediation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php`<br>`tests/staffing-final-repository-sql-remediation.php`<br>`tests/staffing-matrix-rollup-reporting-repository-sql-remediation.php`<br>`tests/staffing-repository-sql-remediation.php`<br>`tests/staff-task-instance-and-portal-repository-sql-remediation.php`<br>`tests/staff-tasks-repository-sql-remediation.php`<br>`tests/staff-tasks-overrides-json-remediation.php`<br>`tests/staff-tasks-signature-json-remediation.php`<br>`tests/vendor-portal-availability-autosave-remediation.php`<br>`tests/authorization-boundary-hardening.php`<br>`tests/runtime-stub-guards.php`<br>`tests/release-compatibility-harness.php`<br>`tests/public-release-build-pipeline.php` | Fresh dedicated G9 inventory verification, the admissions and claim-state focused suites, the required G8 continuity reruns, the required support reruns, PHP lint across every changed mirror/live admissions file plus the changed or new PHP tests, a dirty public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-08-03` while the live `../../vms/` tree was synchronized only inside the authorized target boundaries.<br>The final ZIP is `/tmp/wporg-28rg9-finalize.mpwQFZ/post-build-rerun/backstage-venue-manager-1.2.0-public-release.zip` with SHA-256 `a402d8dedf4146becd36cbedeaa8a8b1d937ad9b1614b6c6d75242fb529f8bd8`; the raw strict JSON is `/tmp/wporg-28rg9-finalize.mpwQFZ/post-plugin-check-rerun/plugin-check.stdout.raw`. | `0722f6dd74dbea3eeca0c7652203b2838733238a` (`Normalize admissions and claim-state query boundaries`) | Child closed under terminal status `verified`.<br>The exact next implementation child is `WPORG-28R-G10 — Ticketing, Availability, And Integrity Query Boundaries`, while same-file admissions logging in `includes/modules/admissions/rest.php` and same-file admissions output rows in `includes/modules/admissions/pass-claims.php` plus `includes/modules/admissions/vendor-guest-portal.php` remain outside G9 ownership.<br>No upload, deployment, reviewer reply, live-tree change outside the authorized admissions boundaries, or production convergence occurred. | `2026-08-03` |
| `WPORG-28R-G10` | `M` | `in progress` | Current repository evidence for this runtime checkpoint includes the mirror/live SQL boundary remediation in `includes/modules/availability-date-dispatch/helpers.php`, `includes/integrations/ticketing-claims-framework.php`, and `includes/integrations/ticketing-phase-b.php`; the dedicated focused harnesses `tests/add-dispatch-repository-sql-remediation.php` and `tests/ticketing-claims-repository-sql-remediation.php`; the bootstrap-alignment companion change in `tests/add-dispatch-assignment-review.php`; the preserved evidence directories `/tmp/wporg-28r-reconcile.cC41Ka`, `/tmp/wporg-28rg10-adddispatch.rGuUaR`, and `/tmp/wporg-28rg10-ticketclaims-final.yEYxQm`; and the synchronized live counterparts under `../../vms/`.<br>The checkpoint proved that the currently completed G10 ownership is the verified ADD, ticketing-claims, and Phase B repository SQL slices, that `%i` identifier preparation plus completed placeholder coverage remove the packaged `UnescapedDBParameter`, `DirectQuery`, `NoCaching`, `InterpolatedNotPrepared`, and `NotPrepared` rows without widening into the deferred same-file date, logging, or remaining ticketing families, and that the current owned parity model remains intact with `includes/integrations/ticketing-claims-framework.php` fully synchronized while `includes/modules/availability-date-dispatch/helpers.php` stays synchronized only inside the owned G10 functions within the intentionally divergent file.<br>The packaged baseline moved from the clean-head `228 / 562 / 790 / 15` starting point to `198 / 460 / 658 / 15`; the exact removed rows were the verified `132` G10-checkpoint-owned occurrences, comprised of `includes/modules/availability-date-dispatch/helpers.php` `85 -> 2 (-83)` and `includes/integrations/ticketing-claims-framework.php` `49 -> 0 (-49)`, with no new rule code, no unrelated file-count increase, and `SUBMISSION_BLOCKER=529`.<br>Coordinated Wave 1 later removed the exact `21` Phase B DB/SQL rows, leaving G10 `in progress` with `95` DB/SQL rows and moving the aggregate package to `520` total findings with `SUBMISSION_BLOCKER=395`. | `tests/add-dispatch-repository-sql-remediation.php`<br>`tests/add-dispatch-assignment-review.php`<br>`tests/availability-date-dispatch-request-state-remediation.php`<br>`tests/ticketing-claims-repository-sql-remediation.php`<br>`tests/ticketing-phase-b-repository-sql-remediation.php`<br>`tests/ticket-claims-assignee-validation.php`<br>`tests/event-plan-legacy-ticketing-integration-smoke.php` | Fresh dedicated ADD and ticketing-claims verification, the required continuity reruns, PHP lint across the changed mirror/live runtime files plus the changed or new PHP tests, `git diff --check`, a dirty public package build, and a fresh strict-json packaged Plugin Check rerun all passed locally on `2026-08-07` while the live `../../vms/` tree was synchronized only inside the authorized target boundaries.<br>The final ZIP is `/tmp/wporg-28rg10-ticketclaims-final.yEYxQm/build/backstage-venue-manager-1.2.0-public-release.zip` with SHA-256 `c2cb0d8dd84d165cf9cfdb27ee041c9d7968c68d230c09ca6f7123e87c527058`; the strict JSON is `/tmp/wporg-28rg10-ticketclaims-final.yEYxQm/plugin-check/plugin-check.strict.json`; stderr remained empty. | `c6c87c6bee4c2ebb606d53b17eb4d99b242added` (`Close WPORG-28R-G10 checkpoint`) | This row closes the current verified checkpoint only; do not treat the whole `WPORG-28R-G10` family as terminal.<br>The exact next handoff is to continue `WPORG-28R-G10` with the Ticketing Rules V2 query-boundary slice in `includes/integrations/ticketing-rules-v2.php`.<br>No upload, deployment, reviewer reply, live-tree change outside the authorized ADD and ticketing-claims boundaries, or production convergence occurred. | `2026-08-07` |
| `Review-13 Final Actions` | `13` | `blocked` | Current repository evidence proves reviewer-source transcription, code state, durable documentation, and a current public `1.2.0` package boundary whose metadata still validates locally.<br>`WPORG-28R-G0` now shows that the packaged residual finding reconciliation is decomposed but still blocked, so the package is not yet defensibly ready for upload or reviewer response. | none | No external WordPress.org action was authorized or performed in this task. | none imported | Keep blocked until explicit authorization covers external WordPress.org actions and `WPORG-28R` is resolved.<br>Cross-reference `Review-2 Name/Slug Closeout` and the blocked packaged boundary in `WPORG-28R` before any resubmission. | `2026-07-26` |

## `WPORG-28R` Current Addendum After Coordinated Wave 4

- Current status: `blocked`. The `WPORG-28R-G10` parent is correctly `in progress`; its finished checkpoints are verified, but the family is not terminal.
- Durable machine-readable state: `docs/wporg-current-scan-state.json`.
- Current packaged baseline: `160` errors, `147` warnings, `307` total findings, `14` unique rule codes, `KNOWN_ACCEPTED=0`, `KNOWN_NONBLOCKING=125`, `NEW_FINDING=0`, `UNMAPPED=0`, and `SUBMISSION_BLOCKER=182`.
- Current blocker-family totals: DB/SQL `115`, nonce/input `0`, date/time `25`, logging `42`.
- Final Wave 4 package: `/tmp/wporg-wave4-integrated.nTzezu/build-authoritative/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `9ce12818e4c19bb684d6e1daa67b31ab7550188b9b10b33d0079af4ae7f06be7`, from clean commit `e8afc44417169a0447a22ee968e41e34e6fc7841`; the builder staged `372` files, linted `269` PHP and `55` JavaScript files, passed all release regressions and dependency load-smokes, and passed package integrity.
- Final strict JSON: `/tmp/wporg-wave4-integrated.nTzezu/plugin-check/plugin-check.strict.json`, SHA-256 `278819f58c585c226824fd89d541fc5ab107c11897240e281683fa6abad8d179`; canonical Plugin Check exited `0`, all scanner and snapshot stderr files were empty, and before/after plugin activation-state snapshots were byte-identical at SHA-256 `3f06f750e24caf2b6349173e3e45caea017f6fd2da66fcccc766e6c87237cb53`.
- `WPORG-28R-G6` is now verified complete: `T1 66`, `T2 33`, `T3 34`, `T4 34`, and `T5 22` reconcile to `189` removed rows; `T1 46`, `T2 23`, `T3 16`, `T4 20`, and `T5 17` reconcile to `122` removed boundaries.
- `WPORG-28R-G7` remains retired. Do not recreate it unless future strict-JSON evidence produces newly unmapped nonce/input rows.
- `WPORG-28R-G8` is now terminal under `verified`; `T1 48`, `T2 53`, `T3 62`, `T4 56`, and `T5 14` reconcile to `233` removed rows, while `T1 13`, `T2 12`, `T3 12`, `T4 6`, and `T5 9` reconcile to `52` removed boundaries.
- Preserved mapping note: the preserved `g8-boundaries.tsv` still assigns `G8-B002` to `vms_staff_user_link_metabox_render()` lines `26-79`, but the current packaged slow-meta query is the later `save_post_vms_staff` closure at line `131`; the terminal post-`T5` G8 accounting therefore uses `/tmp/wporg-28rg8-t5-work.ctlcQI/scope/current-boundary-ranges.tsv`, and the stale mapping does not affect `T1`, `T2`, `T3`, `T4`, or `T5` ownership counts.
- `WPORG-28R-G9` is now terminal under `verified`; its exact `255` removed rows reconcile to `UnescapedDBParameter 39`, `DirectQuery 82`, `NoCaching 74`, `InterpolatedNotPrepared 41`, `NotPrepared 7`, `UnfinishedPrepare 5`, `slow_meta_key 3`, and `slow_meta_query 4`, while the remaining same-file admissions packaged rows are only `includes/modules/admissions/pass-claims.php` `OutputNotEscaped 4`, `includes/modules/admissions/vendor-guest-portal.php` `OutputNotEscaped 11`, and `includes/modules/admissions/rest.php` `error_log_error_log 4`.
- `WPORG-28R-G10` now also verifies `includes/ticketing/ticket-inventory-forensics.php` plus `includes/ticketing/ticket-mutation-audit.php` DB/SQL `27 -> 0`, after the earlier ADD, ticketing-claims, Phase B, and Ticketing Rules V2 slices. Mutation Audit retains exactly `1` error-log and `1` debug-backtrace row for the later logging family, and Phase B retains only its intentionally deferred `2` date, `1` logging, and `1` mapped nonblocking output rows. The parent remains `in progress` with `45` DB/SQL blockers.
- `WPORG-28R-G11` is `in progress`: `includes/core/vendor-user-links.php` DB/SQL moved `36 -> 0`, `includes/core/vendor-application-confirmation.php` moved `27 -> 0`, and `includes/portal/vendor-portal.php` moved `22 -> 0`, leaving `18` G11 DB/SQL blockers.
- `WPORG-28R-G12` is now terminal under `verified`: after social queue `73 -> 0` and the dormant Safety exclusion, coordinated Wave 4 moved notifications/private-files `14 -> 0`, social-support repositories `18 -> 0`, and Email Follow-Ups/Event Plan import meta-query boundaries `6 -> 0`, leaving `0` G12 DB/SQL blockers. The adjacent notifications `error_log` row remains deferred to logging ownership.
- `WPORG-28R-G13` is `in progress`: goals/forecast is `31 -> 0`, vendor-schema plus check-in descriptors are `29 -> 0`, and vendor-link migrations are `16 -> 0`, leaving `52` G13 DB/SQL blockers.
- Exact Wave 1 rule deltas were `UnescapedDBParameter 46 -> 25 (-21)`, `DirectQuery 108 -> 71 (-37)`, `NoCaching 95 -> 62 (-33)`, `InterpolatedNotPrepared 38 -> 18 (-20)`, `NotPrepared 26 -> 15 (-11)`, `UnfinishedPrepare 2 -> 0 (-2)`, `slow_meta_query 83 -> 73 (-10)`, and mapped `OutputNotEscaped 127 -> 123 (-4)`; every other rule-code count stayed unchanged.
- Exact Wave 2 rule deltas were `UnescapedDBParameter 25 -> 17 (-8)`, `DirectQuery 71 -> 42 (-29)`, `NoCaching 62 -> 35 (-27)`, `InterpolatedNotPrepared 18 -> 14 (-4)`, `NotPrepared 15 -> 10 (-5)`, `slow_meta_key 59 -> 58 (-1)`, and `slow_meta_query 73 -> 66 (-7)`; every non-DB rule-code count stayed unchanged.
- Exact Wave 3 rule deltas were `UnescapedDBParameter 17 -> 12 (-5)`, `DirectQuery 42 -> 31 (-11)`, `NoCaching 35 -> 26 (-9)`, `InterpolatedNotPrepared 14 -> 9 (-5)`, `slow_meta_key 58 -> 17 (-41)`, and `slow_meta_query 66 -> 59 (-7)`; every other rule-code count stayed unchanged.
- Exact Wave 4 rule deltas were `UnescapedDBParameter 12 -> 5 (-7)`, `DirectQuery 31 -> 14 (-17)`, `NoCaching 26 -> 13 (-13)`, `InterpolatedNotPrepared 9 -> 3 (-6)`, `NotPrepared 10 -> 7 (-3)`, `slow_meta_key 17 -> 14 (-3)`, and `slow_meta_query 59 -> 54 (-5)`; every other rule-code count stayed unchanged.
- Remaining DB/SQL ownership now reconciles exactly to `G10 45 + G11 18 + G12 0 + G13 52 = 115`; date ownership remains `G14 11 + G15 14 = 25`; logging remains `G16 26 + G17 16 = 42`.
- Wave 1 commits on the primary branch: `e788084` (release safety boundaries), `565946b` plus `fbce45c` (Phase B), `80c2d4c` (vendor user links), and `003f826` plus `01fe866` (social queue).
- Wave 2 commits on the primary branch: `f4aae22` (Ticketing Rules V2), `36ca2eb` (vendor-application confirmation), and `709bb54` plus `b4bd105` (goals/forecast and exact slow-query rule correction); release-harness evidence fixes are `6c740e8` and `18fef69`.
- Wave 3 commits on the primary branch: `7c682b5` (schema/check-in descriptors), `568320e` (vendor portal), and `5b6adb0` (ticket diagnostics); independent reviewers approved each exact slice before integration.
- Wave 4 commits on the primary branch: `9d243ba` (vendor-link migrations), `7f8ff14` (G12 core), `e078136` (social support), and `adaf553` plus `e8afc44` (background meta-query runtime and hardened proof); independent reviewers approved the runtime slices before integration, with the background test hardening completed before acceptance.
- Exact next active work is independent review and integration of the completed Event Plans (`9`) and admin/report (`15`) G13 slices, followed by the remaining `28` G13 long-tail rows.
- No external WordPress.org action is authorized while `WPORG-28R` remains blocked.

## `WPORG-24` Current Breakdown

### `WPORG-24` Summary

- Overall status: `verified`
- Current repository evidence now proves that `WPORG-24` is not merely a prereview placeholder. Multiple historical slices landed in Git and still have current source and test files at HEAD.
- Latest historical production-slice commit evidence tied to accepted child implementations still reaches `b048fcb05c8c6bdff4238b46e7988d04808dbf54 Normalize Event Plan secondary-vendor save HTML response`, and the current tree also carries the accepted package-owned `WPORG-24P` characterization and `WPORG-24Q` Social Sharing finite-renderer normalization work at HEAD.
- This documentation-only closure pass rebuilt the tracker from current Git history, current mirror source files, current focused test filenames, fresh reruns of the accepted parent closeout suites, and `docs/WPORG_PREREVIEW_REMEDIATION.md`.
- `WPORG-24` is now closed under terminal status `verified`: every tracked boundary is either normalized under accepted children, finite and verified safe, intentionally retained architecture, or explicitly outside the parent.
- Shared Administrator-shell raw `$captured_notices_html` and `$content_html` remain intentional page-owned renderer output outside the parent, the separate Settings entitlement image-sync family is accepted as finite and verified safe, `WPORG-24P` plus `WPORG-24Q` resolve Social Sharing, and the stale broader Event Plan HTML residual is removed because every current HTML family already maps to an accepted tested child.

### `WPORG-24A` Portal, docs, and ADD output-contract slices

- Scope or output family: portal notice sinks outside vendor portal, public documentation Markdown rendering, ADD dispatch pills, Staff Portal safe HTML, vendor application confirmation, and ADD public shell output.
- Current status: `implemented`
- Relevant commits: `2a96fc359df03fd2f0f8551a811c91cd9c386515 Normalize portal notice sinks outside vendor portal`; `8025b819c6311a85834fbb07b0dbbabff9c1f13b Normalize public docs Markdown output contract`; `f446aca4016941863ab971af2fc19044dd57c851 Normalize ADD dispatch pill output sinks`; `78dcf887bcf16f02485a3e04b1b0561bddf87713 Normalize staff portal safe HTML sinks`; `6706784bc66b4445ba0d8a50b2094464f4f3afd0 Normalize vendor application confirmation output sink`; `c19c14d30cc451a287ab76b8bd385221ea230384 Normalize ADD public shell output sinks`.
- Current source paths: `includes/modules/admissions/vendor-guest-portal.php`; `includes/portal/vendor-tax-profile.php`; `includes/docs-public.php`; `includes/docs-render.php`; `includes/modules/availability-date-dispatch/admin-ui.php`; `includes/portal/staff-portal.php`; `includes/core/vendor-application-confirmation.php`; `includes/modules/availability-date-dispatch/public.php`.
- Focused test paths on record: `tests/portal-notice-sink-remediation.php`; `tests/docs-public-markdown-output-remediation.php`; `tests/add-dispatch-pill-output-remediation.php`; `tests/staff-portal-safe-html-output-remediation.php`; `tests/vendor-application-confirmation-output-remediation.php`; `tests/add-dispatch-public-shell-output-remediation.php`.
- Mirror/live synchronization: not freshly proven in this correction pass. These are shared runtime slices, but this task rebuilt evidence from mirror Git history and current mirror files only.
- Remaining verification or remaining slices: no fresh rerun of the slice-specific tests was performed for this historical subentry in the documentation-only `WPORG-24R` pass. Historical broader Administrator-shell, Pass Claims, and Event Plan follow-up language is superseded by `WPORG-24R`, which closes the parent without reopening production work.

### `WPORG-24B` Administrator shell base sink reductions

- Scope or output family: Administrator shell explicit status-notice sink and header-action sink reductions.
- Current status: `implemented`
- Relevant commits: `185c27c39c18f0d67b070e909ea62d8a3ba8289b Normalize administrator explicit notice output sink`; `2c3ecf001cf484304985b7f36e26fa32c7513753 Normalize administrator header action output sink`.
- Current source paths: `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/administrator-explicit-notice-output-remediation.php`.
- Mirror/live synchronization: not freshly proven in this correction pass. The shared shell file is present in the mirror, but no fresh mirror/live diff was run.
- Remaining verification or remaining slices: at the time of these base sink reductions, the tracker still carried captured-notice fragments and full page content as separate follow-up work. `WPORG-24R` later reclassifies the raw page-owned shared-shell sinks as intentional architecture outside the parent.

### `WPORG-24C` Administrator captured-notice and rich-notice slice reductions

- Scope or output family: Continuity Binder, Due Dates, Square Sync Protection, Staff Certifications empty-state, Social Sharing, Email Follow-Ups, Ticket Integrity, Settings, and Venue or Calendar Reconciliation notice families routed through explicit sinks.
- Current status: `implemented`
- Relevant commits: `33cdbfa5299ca8817cbd2cad31d85b71435d0963 Route Continuity Binder notice through explicit sink`; `a099f1976cd5f5fab1f6d4e1ff7453ce96de4335 Route Due Dates notices through explicit sink`; `88d8d7a77da96e570964edffb2e2372fb199f078 Route Square Sync Protection notices through explicit sink`; `ac7d300a9d0dbd95265abc3df598b18391f7eb70 Route Staff Certifications empty-state notice through explicit sink`; `d54bef4ef5975b1aef049bba9ddadcfbc60edf4b Route Social Sharing notice through explicit sink`; `c1595cb3c5db46af99cd98956c5c49c6937d5b5f Route Email Follow-ups notices through explicit sink`; `ce28f703545db4a61b902b7ea83c8979ec26fcf7 Route Email Follow-ups preview warning through explicit sink`; `97782f3bc69c9599efcef12989fb263904482798 Route Ticket Integrity notices through explicit sink`; `ae525a527472322737152fc06830eba40ed68748 Route Settings notice through explicit sink`; `f34207f71e9bffd8f004efd030accd1763066b1b Route Venue Reconciliation notices through rich explicit sink`; `436537b3b3b74422df7818cbc5a3eb225f7c5f6e Route Calendar Reconciliation notices through rich explicit sink`; `bcff7dc7687237f2b2d6c02732886a5dc75d7059 Route Settings ticketing stock notices through explicit sink`.
- Current source paths: `includes/admin/continuity-binder.php`; `includes/admin/due-dates.php`; `includes/admin/square-sync-protection.php`; `includes/admin/staff-certifications.php`; `includes/social-share/admin.php`; `includes/modules/email-followups/admin-ui.php`; `includes/admin/ticket-integrity-page.php`; `includes/admin/settings-page.php`; `includes/admin/integrity-venue-reconcile.php`; `includes/admin/integrity-calendar-reconcile.php`; `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/administrator-explicit-notice-output-remediation.php`.
- Mirror/live synchronization: not freshly proven in this correction pass. These are shared runtime slices, but this task did not re-diff the sibling live tree.
- Remaining verification or remaining slices: at the time of this slice, the tracker still carried shared raw `$captured_notices_html` and `$content_html`, unknown or extensible notice sources, and page-family follow-up work. `WPORG-24R` later closes the parent by keeping the raw page-owned sinks outside the parent and relying on the accepted page-family inventory instead of a generic residual.

### `WPORG-24D` Pass Claims admin and public output-contract slices

- Scope or output family: Pass Claims administrator notices, public status screens, already-claimed card, success confirmation, public form, and public shell caller reduction.
- Current status: `implemented`
- Relevant commits: `f9c28be364ca2c767cf4475aa02df17c0ae15618 Route Pass Claims admin notices through explicit sink`; `724923fc2076cda7d37c9df5f2fb10bc92f156c1 Normalize Pass Claims public status sink`; `1f76565c77bf4c03b96c1d640bac2eecd8d8f0bd Normalize Pass Claims already-claimed card sink`; `c1436c1df77d30c064ca8fef64d3aaeb6aa20bad Normalize Pass Claims success confirmation sink`; `230e46fee9ec93d6ef2e1bbcf6bb4e353621710e Normalize Pass Claims public form sink`; `dd05dee1ec102b716dddb55d61c91bd619e47ce1 Replace Pass Claims public shell raw content sink`.
- Current source paths: `includes/modules/admissions/pass-claims.php`.
- Focused test paths on record: `tests/administrator-explicit-notice-output-remediation.php`; `tests/pass-claims-public-status-output-remediation.php`; `tests/pass-claims-public-claimed-card-output-remediation.php`; `tests/pass-claims-public-success-output-remediation.php`; `tests/pass-claims-public-form-output-remediation.php`; `tests/pass-claims-public-shell-output-remediation.php`.
- Mirror/live synchronization: not freshly proven in this correction pass. The Pass Claims runtime file is shared, but this task rebuilt evidence from mirror history and current mirror files only.
- Remaining verification or remaining slices: the historical tracker treated the Pass Claims public-output inventory as closed for this slice family, but this documentation-only `WPORG-24R` pass did not rerun those slice-specific tests. Historical broader non-Pass-Claims residual language is superseded by the parent closeout recorded in `WPORG-24R`.

### `WPORG-24E` Event Plan Import and Event Feedback shared-shell reductions

- Scope or output family: Event Plan Import notices, Event Plan Import rows-payload preview error sink, Event Feedback redirect notices, and Event Feedback missing-plan notice.
- Current status: `implemented`
- Relevant commits: `e9bc4a620fb67dea73d81f8919d70da8b4c0ed6c Route Event Plan Import notices through explicit sink`; `82b3c660e15157105a47a7733f30a6a1db43874b Route Event Feedback notices through explicit sink`; `b645954645e7235c3e751dfa7f7815afd43f20a3 Route Event Feedback missing-plan notice through explicit sink`; `5b7b0375691d123e9c9f995307ca6e09999cef07 Normalize Event Plan Import rows-payload error sink`.
- Current source paths: `includes/admin/data-tools/page-event-plan-import.php`; `includes/admin/event-feedback.php`.
- Focused test paths on record: `tests/administrator-explicit-notice-output-remediation.php`; `tests/event-plan-import-rows-payload-output-remediation.php`.
- Mirror/live synchronization: not freshly proven in this correction pass. These are shared runtime slices, but this task did not re-diff the sibling live tree.
- Remaining verification or remaining slices: at the time of these reductions, the tracker still carried broader shared Administrator-shell raw sink boundaries and separate Event Plan partial or AJAX output families. `WPORG-24R` later records the raw shell sinks as outside the parent and removes the stale generic Event Plan HTML residual.

### `WPORG-24F` Event Plan partial and AJAX HTML response families

- Scope or output family: Event Plan compensation-options, supporting-vendor options, readiness-details, staff, Secondary Vendors lazy-load, and Secondary Vendors save-response HTML families.
- Current status: `implemented`
- Relevant commits: `0a767272c992b3317ad1291e3c6cec4c8f75563c Normalize Event Plan compensation-options HTML response`; `25217c0361dfa4d8cea11e45c0f07eca43062651 Normalize Event Plan supporting-vendor HTML response`; `79f2abee5854509c98d0c3c3066b081b3f6761b4 Normalize Event Plan readiness-details HTML response`; `6a54b96c85d3742c9366bd2e468a6d64ec74fa9f Normalize Event Plan staff HTML response`; `6625757dd4c91382d030313e11bb91a4b400229a Normalize Event Plan secondary-vendor lazy-load HTML response`; `b048fcb05c8c6bdff4238b46e7988d04808dbf54 Normalize Event Plan secondary-vendor save HTML response`.
- Current source paths: `includes/cpt/event-plans.php`; `includes/cpt/event-plans/partials/compensation.php`.
- Focused test paths on record: `tests/event-plan-comp-options-output-remediation.php`; `tests/event-plan-supporting-vendor-options-output-remediation.php`; `tests/event-plan-readiness-details-output-remediation.php`; `tests/event-plan-staff-output-remediation.php`; `tests/event-plan-secondary-vendors-lazy-load-output-remediation.php`; `tests/event-plan-secondary-vendors-save-output-remediation.php`; `tests/event-plan-dead-editor-scripts-partial-removal.php`; `tests/event-plan-shell-controller-inline-js-remediation.php`; `tests/event-plan-secondary-vendor-inline-js-remediation.php`.
- Mirror/live synchronization: not freshly proven in this correction pass. These are shared runtime slices, but this task rebuilt evidence from mirror Git history and current mirror files only.
- Remaining verification or remaining slices: the historical tracker still carried a broader Event Plan output-contract follow-up note even after `b048fcb05c8c6bdff4238b46e7988d04808dbf54`. `WPORG-24R` later removes that stale residual because every current Event Plan HTML family is already mapped to accepted tested children and remaining handlers are data-only JSON.

### `WPORG-24G` Structured JSON-LD fallback script sink

- Scope or output family: package-owned public Event JSON-LD fallback output on `wp_head` in `includes/public/event-details.php`.
- Current status: `verified`
- Relevant commits: `d3fa5a4396158d25323d2cb963cb8e3c4a4c6562 Fix Event schema value normalization` is now explicitly recorded as the producer/value-normalization step for this family; the current tree now also contains the fallback sink hardening through `vms_event_details_encode_fallback_json_ld()`.
- Current source paths: `includes/public/event-details.php`.
- Focused test paths on record: `tests/event-details-schema-normalization.php`; `tests/event-details-fallback-json-ld-output-remediation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: the package-owned fallback sink is now freshly verified. The separate TEC-owned final JSON-LD emitter path remains an explanation-only boundary here and was intentionally left unchanged. `WPORG-24R` later closes the parent by classifying the shared Administrator-shell raw page-owned sinks as accepted architecture outside the parent, accepting Settings entitlement image-sync as finite and verified safe, and removing the stale generic Event Plan residual.

### `WPORG-24H` Settings integrity-scan result output

- Scope or output family: package-owned Settings integrity-scan completion result output in `includes/admin/settings-page.php`, including the page-local wrapper that keeps the rich result family out of shared Administrator-shell captured-notice extraction.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Settings integrity-scan result output`.
- Current source paths: `includes/admin/settings-page.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/settings-integrity-scan-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html` boundaries, and broader Event Plan follow-up language. `WPORG-24R` later reconciles those residuals and closes the parent without reopening production work.

### `WPORG-24I` Settings default-venue alert output

- Scope or output family: package-owned nested default-venue alert output in `vms_field_default_venue()` inside `includes/admin/settings-page.php`, including the page-local context builder / renderer split that keeps the alert inside the Settings field content path.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Settings default-venue alert output`.
- Current source paths: `includes/admin/settings-page.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/settings-default-venue-alert-output-remediation.php`; `tests/settings-integrity-scan-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html` boundaries, and broader Event Plan follow-up language. `WPORG-24R` later reconciles those residuals and closes the parent without reopening production work.

### `WPORG-24J` Guided Tours reset-success notice output

- Scope or output family: package-owned Guided Tours reset-success notice output in `VMS_Tours_Admin::render_page_content()` inside `includes/tours/class-vms-tours-admin.php`, including the page-local context builder / renderer split that keeps the notice nested inside the Guided Tours page wrapper.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Guided Tours reset notice output`.
- Current source paths: `includes/tours/class-vms-tours-admin.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/guided-tours-reset-notice-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, the separate Schedule and Vendor Availability content-local notice families, shared Administrator-shell raw `$captured_notices_html` / `$content_html`, and broader Event Plan follow-up language. Later accepted child slices plus `WPORG-24R` closed those historical gaps.

### `WPORG-24K` Staff Certifications pending-review warning output

- Scope or output family: package-owned Staff Certifications pending-review Administrator warning output in `includes/admin/staff-certifications.php`, including the family-specific context builder / renderer split and named `admin_notices` callback that keep the warning on the existing global hook.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Staff Certifications pending-review notice`.
- Current source paths: `includes/admin/staff-certifications.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`; inspected unchanged count-provider path in `includes/core/staffing.php`.
- Focused test paths on record: `tests/staff-certifications-pending-review-notice-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html`, the remaining Schedule unpublished-venue rich notice family, the Social Sharing event-panel lazy-load HTML family, and broader Event Plan follow-up language. Later accepted child slices plus `WPORG-24R` closed those historical gaps.

### `WPORG-24L` Vendor Availability list-view empty-state output

- Scope or output family: package-owned Vendor Availability list-view empty-state notice output in `vms_render_vendor_availability_list_view()` inside `includes/admin/vendor-availability.php`, including the page-local context builder / renderer split that keeps the notice nested inside `.vms-va-list`.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Vendor Availability empty-state output`.
- Current source paths: `includes/admin/vendor-availability.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/vendor-availability-list-empty-state-output-remediation.php`; `tests/vendor-availability-ux.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html`, the remaining Schedule unpublished-venue rich notice family, the Social Sharing event-panel lazy-load HTML family, and broader Event Plan follow-up language. Later accepted child slices plus `WPORG-24R` closed those historical gaps.

### `WPORG-24M` Schedule invalid-bounds notice output

- Scope or output family: package-owned repeated Schedule invalid-bounds notice output in `vms_render_schedule_list_view()`, `vms_render_schedule_calendar_view()`, `vms_render_schedule_list_view_all()`, and `vms_render_schedule_calendar_view_all()` inside `includes/admin/schedule.php`, including the page-local context builder / renderer split that keeps the notice nested inside the existing Schedule content path.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Schedule invalid-bounds notice output`.
- Current source paths: `includes/admin/schedule.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/schedule-invalid-bounds-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html`, the remaining Schedule unpublished-venue rich notice family, the Social Sharing event-panel lazy-load HTML family, and broader Event Plan follow-up language. Later accepted child slices plus `WPORG-24R` closed those historical gaps.

### `WPORG-24N` Schedule scope warning notice output

- Scope or output family: package-owned Schedule no-selection and no-venues warning notice output in `vms_render_schedule_page_content()` inside `includes/admin/schedule.php`, including the page-local context builder / renderer split that keeps both warnings nested inside the existing Schedule content path.
- Current status: `verified`
- Relevant commits: current task working tree `Normalize Schedule scope warning notice output`.
- Current source paths: `includes/admin/schedule.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/schedule-warning-notice-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/schedule-invalid-bounds-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html`, the Social Sharing event-panel lazy-load HTML family, and broader Event Plan follow-up language. Later accepted child slices plus `WPORG-24R` closed those historical gaps.

### `WPORG-24O` Schedule unpublished-venue rich notice output

- Scope or output family: package-owned Schedule unpublished-venue rich notice output in `vms_render_schedule_page_content()` inside `includes/admin/schedule.php`, including the page-local context builder / renderer split that keeps both unpublished branches nested inside the existing Schedule content path.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Schedule unpublished-venue notice output`.
- Current source paths: `includes/admin/schedule.php`; inspected unchanged shell extraction logic in `includes/admin-ui/shell.php`.
- Focused test paths on record: `tests/schedule-unpublished-venue-notice-output-remediation.php`; `tests/schedule-warning-notice-output-remediation.php`; `tests/schedule-invalid-bounds-output-remediation.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice is now freshly verified. At the time of this slice, the tracker still carried the separate Settings entitlement image-sync structured-result family, shared Administrator-shell raw `$captured_notices_html` / `$content_html`, the Social Sharing event-panel lazy-load HTML family, and broader Event Plan follow-up language. Later accepted child slices plus `WPORG-24R` closed those historical gaps.

### `WPORG-24P` Social Sharing event-panel lazy-load output characterization

- Scope or output family: package-owned test-only characterization of the complete coupled Social Sharing event-panel lazy-load HTML family in `includes/social-share/event-plan-panel.php`, `includes/social-share/admin.php`, and `assets/js/vms-social-admin.js`, covering the collapsed lazy shell, synchronous metabox output, AJAX `html`, AJAX `footer_forms_html`, detached footer-form registration/rendering, JavaScript lazy-load triggers, `shell.innerHTML`, `wrapper.innerHTML`, replace-by-ID detached-form insertion, the JavaScript-local fallback paragraph, and visible-control/detached-form ID coupling.
- Current status: `verified`
- Relevant commits: current task commit `Characterize Social event-panel lazy-load output`.
- Current source paths: `includes/social-share/event-plan-panel.php`; `includes/social-share/admin.php`; `assets/js/vms-social-admin.js`.
- Focused test paths on record: `tests/social-event-panel-lazy-load-output-characterization.php`; `tests/social-event-panel-lazy-load-js-characterization.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice intentionally leaves all production PHP and JavaScript unchanged and only characterizes the current coupled Social Sharing family. The later package-owned `WPORG-24Q` slice resolves that production boundary while preserving this characterization baseline. `WPORG-24R` later closes the parent by accepting Settings entitlement image-sync as finite and verified safe, keeping the shared raw Administrator-shell page-owned sinks outside the parent, and removing the stale generic Event Plan residual.

### `WPORG-24Q` Social Sharing event-panel finite renderer boundaries

- Scope or output family: package-owned production normalization of the coupled Social Sharing event-panel lazy-load HTML family in `includes/social-share/event-plan-panel.php`, preserving the existing collapsed shell, synchronous metabox path, AJAX `html`, AJAX `footer_forms_html`, detached footer forms, request-local footer registry, `admin_footer` replay, public producer signatures, and unchanged JavaScript consumers while separating external reads from finite rendering.
- Current status: `verified`
- Relevant commits: current task commit `Normalize Social event-panel renderer boundaries`.
- Current source paths: `includes/social-share/event-plan-panel.php`; inspected unchanged adjacent consumers `includes/social-share/admin.php` and `assets/js/vms-social-admin.js`.
- Focused test paths on record: `tests/social-event-panel-finite-renderer-output-remediation.php`; `tests/social-event-panel-lazy-load-output-characterization.php`; `tests/social-event-panel-lazy-load-js-characterization.php`; `tests/administrator-explicit-notice-output-remediation.php`; `tests/settings-integrity-scan-output-remediation.php`; `tests/event-plan-supporting-vendor-options-output-remediation.php`; `tests/event-plan-ticket-ui-overrides-isolated.php`; `tests/event-plan-calendar-unpublished-suppress-save.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: this task explicitly stayed package-source only because the local WordPress bootstrap still loads the installed sibling `vms` tree and the task forbade copying package files into it. No live-tree sync proof was attempted in this pass.
- Remaining verification or remaining slices: this slice resolves the Social Sharing event-panel implementation family under `WPORG-24` by routing the public producers through `vms_social_build_event_panel_view()` plus read-free `vms_social_render_event_panel_html()` and `vms_social_build_event_panel_footer_forms_view()` plus read-free `vms_social_render_event_panel_footer_forms_markup()`, while preserving the exact `WPORG-24P` PHP/AJAX/JS contract, detached-form coupling, AJAX keys, failures, and generic client fallback. `WPORG-24R` later closes the parent: the shared Administrator-shell raw `$captured_notices_html` / `$content_html` sinks are recorded as intentional page-owned architecture outside the parent, the separate Settings entitlement image-sync family is accepted as finite and verified safe, and no broader Event Plan HTML residual remains.

### `WPORG-24R` Residual tracker reconciliation and parent closure

- Scope or output family: documentation-only reconciliation of the stale `WPORG-24` residual tracker in `docs/wporg-remediation-ledger.md` and `docs/WPORG_PREREVIEW_REMEDIATION.md`.
- Current status: `verified`
- Relevant commits: current task commit `Close WPORG-24 residual tracker`.
- Current source paths: `docs/wporg-remediation-ledger.md`; `docs/WPORG_PREREVIEW_REMEDIATION.md`.
- Production/test changes: none. This slice edits documentation only and does not alter runtime, tests, assets, packaging, deployment state, or the protected stash.
- Focused test paths on record: `tests/administrator-explicit-notice-output-remediation.php`; `tests/settings-integrity-scan-output-remediation.php`; `tests/settings-default-venue-alert-output-remediation.php`; `tests/social-event-panel-finite-renderer-output-remediation.php`; `tests/social-event-panel-lazy-load-output-characterization.php`; `tests/social-event-panel-lazy-load-js-characterization.php`; `tests/event-plan-comp-options-output-remediation.php`; `tests/event-plan-supporting-vendor-options-output-remediation.php`; `tests/event-plan-readiness-details-output-remediation.php`; `tests/event-plan-staff-output-remediation.php`; `tests/event-plan-secondary-vendors-lazy-load-output-remediation.php`; `tests/event-plan-secondary-vendors-save-output-remediation.php`; `tests/event-plan-ticket-ui-overrides-isolated.php`; `tests/event-plan-calendar-unpublished-suppress-save.php`; `tests/admin-notice-scope-remediation.php`; `tests/decoded-json-validation.php`.
- Mirror/live synchronization: not applicable for this documentation-only slice. No mirror/live sync or sibling-tree copy was attempted.
- Closure reconciliation: `WPORG-24` is now closed under terminal status `verified`: every tracked boundary is either normalized under accepted children, finite and verified safe, intentionally retained architecture, or explicitly outside the parent.
- Shared Administrator-shell reclassification: raw `$captured_notices_html` and `$content_html` remain intentional page-owned renderer output. The shell's explicit notice and action contracts stay unchanged, and any future shell redesign belongs outside `WPORG-24`.
- Settings entitlement image-sync reconciliation: the family remains accepted and closed based on finite markup, sink-specific escaping, capability and nonce gating, read-only rendering, and the adjacent focused evidence already on record; no new implementation child was required.
- Social Sharing reconciliation: `WPORG-24P` characterized the coupled event-panel contract and `WPORG-24Q` normalized the finite renderer boundaries, so the Social Sharing family no longer remains a parent residual.
- Event Plan reconciliation: every current Event Plan HTML-transporting AJAX family is already mapped to accepted tested children, and remaining handlers are data-only JSON; the generic broader Event Plan follow-up residual was stale and is now removed.
- Outside-parent note: any future trusted printable-export-shell inventory, including `vms_safety_render_export_shell()` in `includes/safety/admin.php`, belongs to a separate non-blocking issue outside `WPORG-24` and does not block resubmission.
- Remaining verification or remaining slices: none under `WPORG-24`. The parent is closed by this documentation-only reconciliation.

### `Review-10A` Event Plan CSV preview upload API

- Scope or output family: package-owned Event Plan CSV preview source upload in `includes/admin/data-tools/actions-event-plan-import.php`, including the scoped upload-directory filter, deterministic `<token>-source.csv` staging contract, preview rollback, preview payload storage-key lifecycle, and commit cleanup.
- Current status: `verified`
- Relevant commits: `88a90cce7087e2d013493236b0280da6d1bd31ea Use WordPress upload API for Event Plan import preview`.
- Current source paths: `includes/admin/data-tools/actions-event-plan-import.php`; inspected unchanged adjacent boundaries in `includes/admin/data-tools/page-event-plan-import.php` and `includes/services/event-plan-import/event-plan-import-engine.php`.
- Focused test paths on record: `tests/event-plan-import-upload-api-remediation.php`.
- Verification state in this pass: reran the refreshed Event Plan harness and confirmed the current importer still owns its own scoped `wp_handle_upload()` path, keeps the deterministic source staging contract, keeps the preview/report storage keys, does not use `vms_private_files_store_validated_upload()`, and does not consume a returned public URL.
- Remaining verification or remaining slices: none under `Review-10`. This child is closed and verified.

### `Review-10B` Shared private-file broker upload API

- Scope or output family: package-owned shared private-file upload broker in `includes/core/private-files.php`, including current W-9, staff-certification, vendor tech-document, and non-image ticket-verification callers.
- Current status: `verified`
- Relevant commits: `7441c7008d8bd38834f4f8cb50b84e61d6fabda2 Use WordPress upload API for private files`.
- Current source paths: `includes/core/private-files.php`; inspected current caller boundaries in `includes/portal/vendor-portal.php` and `includes/integrations/ticketing-verifications.php`.
- Focused test paths on record: `tests/private-file-upload-api-remediation.php`; `tests/upload-validation-guards.php`; `tests/verification-proof-normalization.php`.
- Verification state in this pass: reran the focused private-file broker source/runtime harnesses and confirmed the broker now uses `wp_handle_upload()` with exact caller MIME maps, keeps UUID storage keys and private buckets, preserves metadata and `0640`, safely handles mismatch cleanup, does not create attachments, does not use returned public URLs, and leaves the verification-image normalization branch outside the broker.
- Remaining verification or remaining slices: none under `Review-10`. This child is closed and verified.

### `Review-10` Upload APIs parent closure

- Scope or output family: documentation-backed closure of the reviewer-upload-API family identified in `docs/wporg-review-source.md` section `10. WordPress File Upload APIs`.
- Current status: `verified`
- Parent reconciliation: the two plugin-owned raw upload movers that remained in mirror production for this family were the Event Plan preview source path and the shared private-file broker path. Both now route through `wp_handle_upload()` under the verified children above.
- Current source reconciliation: no live `move_uploaded_file()` matches remain under mirror `includes/`; remaining alternative-upload API matches are existing WordPress media attachment flows in `includes/portal/vendor-portal.php` and a non-upload `rename()` log-rotation path in `includes/core/slow-request-logger.php`.
- Focused verification on record: `tests/event-plan-import-upload-api-remediation.php`; `tests/private-file-upload-api-remediation.php`; `tests/upload-validation-guards.php`; `tests/verification-proof-normalization.php`.
- Remaining verification or remaining slices: none under `Review-10`. This closes the final known upload-API implementation target in the mirror repository.

## Update Rule For New Entries

Every completed remediation task must append or update the relevant row with:

- selected reviewer issue identifier
- issue family
- status
- current repository evidence
- focused tests on record
- verification state in the current task
- commit SHA when applicable
- remaining risks or follow-up
- most recent focused verification on record

## Mirror/Live Reconciliation Note

- The completed no-edit audit found multiple mirror/live divergences in shared runtime areas.
- Future remediation batches must not assume every differing shared file should be made byte-identical.
- Each batch must determine whether the relevant remediation lines should match, whether surrounding divergence is intentional, whether the live-only or newer feature sits outside the public package, or whether an unexplained mismatch requires correction.

## Historical `WPORG-28R` Addendum After G16 Operational Logging

- This addendum supersedes the older “current” status language in the historical checkpoint table and coordinated-Wave-4 addendum above. Historical counts remain preserved as evidence of those checkpoints. In particular, the historical `WPORG-28`, `WPORG-28Q`, `WPORG-28R`, and `WPORG-28R-G10` table rows retain their then-current counts and handoffs and must not be read as the current packaged state.
- Current status: `blocked` only because `WPORG-28R-G17` remains. The DB/SQL, nonce/input, date/time, and G16 operational-logging families are terminal under `verified`.
- Durable machine-readable state: `docs/wporg-current-scan-state.json`.
- Current clean source commit: `d4079b0bf2c8affb1f50ac587ddb858aa94197e2`.
- Current public package: `/tmp/wporg-g16-checkpoint-final.aOSh8U/build/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `6a9b45cd6e2cbb1fd6723f064b1c867a425b99c8604750791badfedfb2f62798`; the clean builder finished at `2026-08-08T19:52:45+00:00` after staging `372` files and checking `269` PHP plus `55` JavaScript files.
- Current strict JSON: `/tmp/wporg-g16-checkpoint-final.aOSh8U/plugin-check.strict.json`, SHA-256 `b0ebbddec1d17ce9a8770ae9ec385665f49962c6ebc1a3f2f1520e81d281b49c`; canonical Plugin Check exited `0` with empty stderr.
- Current packaged baseline: `125` errors, `16` warnings, `141` total findings, `5` unique rule codes, `KNOWN_ACCEPTED=0`, `KNOWN_NONBLOCKING=125`, `NEW_FINDING=0`, `UNMAPPED=0`, and `SUBMISSION_BLOCKER=16`.
- Current blocker-family totals: DB/SQL `0`, nonce/input `0`, date/time `0`, logging `16`.
- `WPORG-28R-G10` is terminal under `verified`: all ticketing, availability, claims, diagnostics, integrity, and support-repository DB/SQL slices are scanner-zero. Its same-file date/logging rows remain correctly owned by G15-G17.
- `WPORG-28R-G11` is terminal under `verified`: vendor-user links, application confirmation, vendor portal, vendor applications, payables, onboarding, public profiles, category propagation, and export query boundaries are scanner-zero.
- `WPORG-28R-G12` remains terminal under `verified` at `0` DB/SQL rows.
- `WPORG-28R-G13` is terminal under `verified`: goals, schema/check-in, migrations, Event Plans, admin/report, tail, and core-service DB/SQL slices are scanner-zero.
- `WPORG-28R-G14` is terminal under `verified` at `11 -> 0` date rows.
- `WPORG-28R-G15` is terminal under `verified`: Staff Notifications `5 -> 0`, Ticket Integrity and Phase B `5 -> 0`, and Payables/Vendor Tax/Event Credits `4 -> 0`; the clean extracted-package scan proves the date family is scanner-zero.
- `WPORG-28R-G16` is terminal under `verified`: runtime guards `2 -> 0`, Group A `11 -> 0`, Group B `9 -> 0`, and Group C `4 -> 0`; the clean extracted-package scan proves G16 `26 -> 0` while the exact G17 inventory remains unchanged.
- Exact remaining ownership is `G17 16 = 16`, comprising `error_log 15 + debug_backtrace 1`. The accepted nonblocking set remains exactly `OutputNotEscaped 123 + OffloadedContent 1 + NonEnqueuedStylesheet 1 = 125`.
- An initial G16 checkpoint scan exposed one `parse_url()` alternative-function blocker in the new bounded adapter. Commit `d4079b0bf2c8affb1f50ac587ddb858aa94197e2` replaced it with `wp_parse_url()`, and the final strict JSON contains no alternative-function finding.
- The build staged `372` files, linted `269` PHP and `55` JavaScript files, passed all release regressions and dependency load smokes, and passed package-integrity checks. Scanner activation-state snapshots were not measured for this checkpoint; no activation command was issued.
- No push, deployment, WordPress.org upload, reviewer reply, tag, production/staging change, or protected-stash mutation occurred.

## Historical Recommended Remaining Work Order After G16

1. Complete `WPORG-28R-G17`, consuming the G16 adapter for genuine failures while removing unsafe developer traces and default public-path performance logging.
2. Build and scan a final clean package; update the durable state only when it proves `SUBMISSION_BLOCKER=0` and the mapped nonblocking set remains unchanged.
3. Only after explicit authorization, let `Review-2 Name/Slug Closeout` handle slug-reservation, final artifact preparation, and corrected upload, then let `Review-13 Final Actions` handle the reviewer reply.

- Separate production-convergence work remains outside this WordPress.org order: Backstage Outreach extraction, duplicate-core safety, and live `vms 1.1.0` replacement or migration.

- This order is a recommended execution sequence only. It is not proof that every listed remaining item is a confirmed defect.

## `WPORG-28R` Final Technical Scanner Closeout

- This addendum supersedes all older “current” status language above while preserving the older table rows and coordinated checkpoints as historical evidence. In particular, the historical `WPORG-28`, `WPORG-28Q`, `WPORG-28R`, and `WPORG-28R-G10` counts, statuses, and handoffs are not the current packaged scanner state.
- Current technical status: `verified`; `WPORG-28R` has satisfied its blocker closeout condition. All active `WPORG-28R-G1`-`G6` and `WPORG-28R-G8`-`G17` technical children are terminal under `verified`, `WPORG-28R-G7` remains retired with no owned residual, and the final clean extracted-package scan proves `SUBMISSION_BLOCKER=0`.
- `Review-2 Name/Slug Closeout` and `Review-13 Final Actions` remain separately `blocked` pending explicit authorization. Their older references to unresolved `WPORG-28R` technical findings are superseded by this closeout; no external WordPress.org action is implied.
- Durable machine-readable state: `docs/wporg-current-scan-state.json`.
- Current clean source commit: `a14b4a67a2f8273bb45b8b4569b6282efb6d5743`.
- Current public package: `/tmp/wporg-final-g17.BmolWv/build/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `27d2fdd8b7d7bf3020807eb5ccb3b5654d733fd0a0eb4150d4e388a307c82d82`; the clean builder finished at `2026-08-08T21:00:43+00:00` after staging `372` files, linting `269` PHP files, and syntax-checking `55` JavaScript files.
- Current strict JSON: `/tmp/wporg-final-g17.BmolWv/plugin-check.strict.json`, SHA-256 `3c23ee9d81d601c73a73cae5e82a2917e023273eb1ca500314fd8af33b707a73`; canonical Plugin Check exited `0` with empty stderr.
- Current packaged baseline: `125` errors, `0` warnings, `125` total findings, `3` unique rule codes, `KNOWN_ACCEPTED=0`, `KNOWN_NONBLOCKING=125`, `NEW_FINDING=0`, `UNMAPPED=0`, and `SUBMISSION_BLOCKER=0`.
- Current blocker-family totals: DB/SQL `0`, nonce/input `0`, date/time `0`, logging `0`.
- `WPORG-28R-G10`, `WPORG-28R-G11`, `WPORG-28R-G12`, and `WPORG-28R-G13` remain scanner-verified at `0` DB/SQL rows; `WPORG-28R-G14` and `WPORG-28R-G15` remain scanner-verified at `0` date/time rows; `WPORG-28R-G16` remains scanner-verified at `26 -> 0` operational-logging rows.
- `WPORG-28R-G17` is terminal under `verified`: Slice A Event Plan performance diagnostics `6 -> 0`, Slice B admin and metadata logging `5 -> 0`, and Slice C staff/ticket diagnostics `5 -> 0`, reconciling exactly to `16 -> 0`. The sole former `debug_backtrace` row is scanner-zero through one exact line-local suppression that retains `DEBUG_BACKTRACE_IGNORE_ARGS`, depth `40`, and immediate reduction to sanitized function-only frames.
- G17 isolated commits `901ed6e4eeff6860842904cc0b11eaa4611d820d` (A), `12f834d0a3ee62aaad69d9da1e66b6a5c41e545a` (B), and `43b9857d358be6fc9e85475a1dfd2c6505ac7bff` (C) each received an independent `CLEAN` review that reproduced focused/scanner, lint, diff, and representative continuity evidence. Primary integration commits are `1b71e50b7a7ba742a84519677396af34f5dc6a50` (A), `be2fa558e7ddd9a0585449d24ee0542da30e550a` (B), and `a14b4a67a2f8273bb45b8b4569b6282efb6d5743` (C).
- The exact remaining accepted nonblocking set is `OutputNotEscaped 123 + OffloadedContent 1 + NonEnqueuedStylesheet 1 = 125`; no new or unmapped code appeared. Its semantic multiset is identical to the G16 nonlogging inventory; only the same `/privateincludes/helpers.php` `OutputNotEscaped` location moved from line `4190` to `4200`.
- State deltas reconcile exactly: from the post-G10 checkpoint, `658 -> 125` total findings removes `533` and `529 -> 0` blockers removes `529`; from Wave 1, `520 -> 125` and `395 -> 0` remove `395`; from Wave 2, `439 -> 125` and `314 -> 0` remove `314`; from Wave 3, `361 -> 125` and `236 -> 0` remove `236`; from Wave 4, `307 -> 125` and `182 -> 0` remove `182`; logging is `42 -> 0` from each of those baselines.
- The earlier G16 checkpoint exposed one `parse_url()` alternative-function blocker in the bounded adapter. Commit `d4079b0bf2c8affb1f50ac587ddb858aa94197e2` replaced it with `wp_parse_url()`, and the final strict JSON contains no alternative-function finding.
- Manual-review notes from the final build remain: no version-matched `BUILD-NOTES` file exists for `1.2.0`, and the activation-hook smoke was intentionally skipped because it would mutate a WordPress site. Scanner activation-state snapshots were not measured, and no activation command was issued.
- No push, deployment, WordPress.org upload, reviewer reply, slug reservation, tag, production/staging change, production convergence, or protected-stash mutation occurred.

## Recommended Authorized-Action Gate

1. `STOP / AWAIT EXPLICIT AUTHORIZATION`; no technical-remediation or scanner-blocker child remains.
2. If separately authorized, let `Review-2 Name/Slug Closeout` accept the final artifact, handle WordPress.org-side slug reservation, and perform the corrected upload.
3. If separately authorized after the Review-2 boundary is satisfied, let `Review-13 Final Actions` handle the reviewer reply.

- Separate production-convergence work remains outside this WordPress.org order: Backstage Outreach extraction, duplicate-core safety, and live `vms 1.1.0` replacement or migration.
- This sequence records prerequisites only; it grants no authorization for an external or production action.

## WordPress.org Reviewer Identity Alignment — Phase A

Date: 2026-08-25

- Current status: `verified`; the definite reviewer-identity work is complete and the broad prefix surface remains on strict hold pending reviewer clarification.
- Isolation: work was performed only in `/private/tmp/bvm-wporg-identity-phase-a/packages/vms-github-reconcile` on branch `codex/wporg-reviewer-identity-alignment`, rooted exactly at `0d48194b3fb47a29742068757d43787d66c316f6`. The implementation commits are `eb8f39372a3aca0fd68acbcda5b1075f84f5d6c5` and `3c783f8474d471970e90211024858cb22767596e`.
- Public identity: the canonical product name is now `Backstage Venue Manager` across the top-level menu and badge restoration, dashboard and first-run copy, settings/navigation/docs/reference/guided-tour headings, product-agent notices, email/CLI headings, readme navigation, and the directly related public strings audited in this phase. The text domain remains `backstage-venue-manager`.
- Main filename: `backstage-venue-manager.php` is the one canonical plugin-header/bootstrap file. `vendor-management-system.php` is a headerless same-directory bridge, and `vms.php` delegates to the canonical bootstrap. The built ZIP contains exactly one root `Plugin Name` header.
- Basename compatibility: `includes/plugin-basename-compat.php` provides exact same-directory legacy/canonical pairing, single-site and multisite active-basename migration with network activation timestamp preservation, canonical/legacy lifecycle recognition, and legacy activation/deactivation hook coverage. It does not rewrite arbitrary plugin basenames or unrelated active-plugin entries. A `vms/` to `backstage-venue-manager/` directory transition remains a controlled replacement boundary.
- Focused verification: `tests/plugin-identity-alignment.php`, `tests/release-compatibility-harness.php` (`15` self-test cases), `tests/public-release-build-pipeline.php` (`18` cases), `tests/runtime-stub-guards.php`, `tests/admin-notice-scope-remediation.php`, and `tests/administrator-explicit-notice-output-remediation.php` passed. PHP lint passed across all changed PHP, both changed JavaScript files passed `node --check`, and `git diff --check` passed.
- Package verification: the clean builder passed after staging `374` files, linting `271` PHP files, syntax-checking `55` JavaScript files, validating the canonical root/main file, and passing all package-integrity checks. The default WordPress-dependent release scripts were intentionally skipped by the standalone builder and replaced by the disposable compatibility harness.
- Disposable compatibility: the full seven-scenario dependency matrix passed without fatals on the code-identical pre-metadata candidate. The final exact-hash candidate then passed standalone and supported-stack boots, activation, deactivation, reactivation, admin/public requests, controlled upgrade from `vms/vendor-management-system.php`, repeat upgrade, migration-interruption recovery, fixture preservation, and uninstall preservation. The final report status is `WARN` because the dependency stack emits PHP 8.5 deprecations and one non-fatal interruption-path warning; standalone WordPress is clean.
- Scanner: the first current-environment run exposed only `outdated_tested_upto_header` because WordPress had advanced from `7.0` to `7.1`. The completed disposable WordPress `7.1` lifecycle evidence authorized the one-line readme update. The final strict scan exited `0` with empty stderr and exactly the historical nonblocking baseline: `125` errors, `0` warnings, `125` total, `3` rule codes, `OutputNotEscaped 123 + OffloadedContent 1 + NonEnqueuedStylesheet 1`, `NEW_FINDING=0`, `UNMAPPED=0`, and `SUBMISSION_BLOCKER=0`. The three new filename/compatibility files have zero findings.
- Candidate: `/private/tmp/bvm-wporg-identity-phase-a/artifacts/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `e2b7546b7dde917208db7cf12a6100feaaa8ac1d3b965daca83b7d552e70cf81`, source commit `3c783f8474d471970e90211024858cb22767596e`.
- Intentional retention: the live `/vms/` Plugin URI remains because it resolves while the guessed canonical route does not; operational terms such as `VMS-managed`, `VMS-native`, VMS ticket intent/map labels, technical headers, keys, and internal diagnostics remain compatibility terminology; historical remediation records retain their then-current filenames and branding as evidence.
- Prefix hold: no broad change was made to `vms_*` functions/hooks/options/meta/transients/tables/cron/AJAX identifiers, `VMS_*` classes/constants, the `vms/v1` REST namespace, shortcodes, or script/style handles.
- Mirror/live boundary: the sibling live tree remained read-only and untouched because this task explicitly required all edits and tests to stay in the isolated release worktree. No sync, upload, reviewer reply, push, deployment, staging/production change, tag, production convergence, or protected-stash mutation occurred.

## WordPress.org Reviewer Prefix Alignment — Phase B0 Persistence and B1

Date: 2026-08-26

- Current status: `verified`; B0 was persisted exactly as the approved substantive design, and B1 inventory, compatibility preparation, migration-state architecture, and pre-migration guardrails are complete. B2-B8 remain unstarted.
- Isolation: only `/private/tmp/bvm-wporg-identity-phase-a/packages/vms-github-reconcile` on `codex/wporg-reviewer-identity-alignment` was edited. The primary worktree and the read-only sibling live/add-on trees were not modified.
- B0 authority: `docs/WPORG_PREFIX_MIGRATION_B0.md`; documentation checkpoint `3fb278c` (`Persist WPORG prefix migration design`). Removing the two-line committed authority note and its separating newline reproduces the supplied SHA-256 `7893dc878cff48e86a981771c0e52f3119a4a3202307c73ab24817bb863f3dc9` exactly.
- B1 architecture: `docs/WPORG_PREFIX_MIGRATION_B1.md`, `docs/wporg-prefix-migration-manifest.json`, immutable allowance set `docs/wporg-prefix-prohibited-global-baseline.json`, `scripts/generate-wporg-prefix-manifest.php`, `scripts/lib/wporg-prefix-inventory.php`, and `scripts/lib/wporg-prefix-migration-state.php`. The current task commit is the B1 checkpoint.
- Inventory: all 25 B0 categories; 4,521 public functions at 4,541 sites, 23 classes, one interface, 107 constants at 116 sites, 44 global slots at 232 sites, 4,696 ratcheted prohibited short-prefix declarations/slots, 3,613 function-resolution requirements, 13 public APIs, and five known add-on maps.
- Evidence correction: B1 found two by-reference functions omitted by the B0 probe because its name lookup did not skip PHP's ampersand token. The functions remain ordinary B3 Strategy 1 work. B1 also froze 20 optional/external dynamic function contracts. Neither refinement changes the canonical family, strategy model, compatibility classes, migration batches, or B0 risk classifications.
- Add-ons: `vms-events-slider`, `vms-fill-dates`, `vms-data-tools`, `vms-express-bar`, and `vms-refer-a-friend` were inspected read-only. Exact functions, hooks, retained physical identifiers, handles, and evidence files are frozen in the manifest; no planned core canonical function target collides with an installed add-on declaration. No external tree changed.
- Migration state: independent `bvmgr_prefix_migration_version` marker plus per-site retry journal; idempotent steps, target-version isolation, downgrade prevention, interruption cursor, multisite iteration, verified copy-before-cutover, no legacy deletion, canonical-first read/write policy, and opt-in rollback-safe write mirroring are regression-covered. The reference is release-excluded and production-inert; no persistent data changed.
- Focused verification: `php -d memory_limit=1G scripts/generate-wporg-prefix-manifest.php --check`; `php tests/wporg-prefix-manifest-guardrails.php`; `php tests/wporg-prefix-migration-state.php`; `php tests/wporg-prefix-addon-contracts.php`; PHP lint on all changed PHP; and `git diff --check` passed. The prefix guard is now a required default release precondition.
- Package/scanner boundary: all B1 files are release-excluded under `docs/`, `scripts/`, and `tests/`; no public-package source or `docs/wporg-current-scan-state.json` changed. The scanner reference therefore remains 125 known nonblocking findings, no unmapped finding, and no submission blocker. No package or ZIP was created.
- Non-actions: no B2/B3 symbol migration, persistent migration, public legacy wrapper, private bridge, add-on edit, live synchronization, package, scanner campaign, push, merge, upload, reviewer reply, deployment, tag, production/staging change, production convergence, or protected-stash mutation occurred.

## WordPress.org Reviewer Prefix Alignment — Phase B2

Date: 2026-08-26

- Historical B2 checkpoint status: `implementation complete; architecture review required before B3`. The exact authorized B2 map was implemented at `d781aa4ef8f0941384502d4a8740e5c9176ef957`; this historical hold is superseded by the verified B2.5 section below, and B3 still requires separate explicit authorization.
- Recovery: the unverified `233`-path partial implementation was preserved outside Git at `/private/tmp/bvm-wporg-b2-partial-recovery/` before only the isolated core was reset to `87d071629677007dc97c6906c4088b859c4fa003`. The tracked binary patch SHA-256 is `b11f2ff562fbd99b009ae3311ed05ac43853a4f861be7c8f65fe0b3085de26bf`; the archive SHA-256 is `6781a3a11f0df7b2b417ac7a2e681a7eb969b5d88938ba8a826f3117eff92ffd`.
- B1 correction: semantic scans of all five installed add-ons proved exactly seven B2 core-symbol dependencies and no additional dependency. Commit `3430925213b1750a8741d5acf321f04de4705da3` records the corrected manifest, B1 handoff, and guardrails.
- Add-on isolation: no canonical Git repository exists for Events Slider, Data Tools, or Express Bar. Disposable copies under `/private/tmp/bvm-wporg-b2-addon-isolation/workspaces/` received only the seven required canonical-symbol consumer changes; their installed trees remained untouched. The final compatibility harness passes against the migrated core and retained `vms_calendar_feed_cache_bust`, `_vms_vendor_user_id`, `_vms_vendor_id`, `vms_venue`, and `vms_event_plan` values.
- B2 implementation: `23` classes / `23` sites, one interface / one site, `107` constants / `116` sites, and `44` frozen global slots / `232` sites moved to `BVMGR_` / `bvmgr_`. The exact `175`-entry map is `docs/wporg-prefix-migration-manifest.json#/completed_batches/B2/symbol_map`. The prohibited-global ratchet is exactly `4,696 -> 4,521 (-175)`; all `4,521` unique B3 functions / `4,541` declarations remain resolvable and no public legacy aliases ship.
- Verification: deterministic generation, B1/B2 guardrails, dynamic/collision coverage, add-on contracts, plugin identity, release tests, runtime guards, changed-PHP lint, and diff checks passed. Full disposable dependency evidence passed all seven states without fatals. The exact final package then passed standalone/supported-stack boot, activation/deactivation/reactivation, authenticated admin/public requests, legacy-basename upgrade, interruption recovery, fixture preservation, and uninstall preservation; `WARN` contains third-party TEC PHP 8.5 deprecations only.
- Package: `/private/tmp/bvm-wporg-b2-final/build/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `81e67194396108e312d4c989fc014bb66003e9d15743327110383a5bc1a9b848`, from clean source `d781aa4ef8f0941384502d4a8740e5c9176ef957`. The builder passed after staging `374` files, linting `271` PHP files, syntax-checking `55` JavaScript files, and completing all integrity checks.
- Scanner: the normalized strict JSON is `/private/tmp/bvm-wporg-b2-final/plugin-check/plugin-check.strict.json`, SHA-256 `315c84098035526ded25fb58f56850f4e0845dfccd6bf90fe0dc941d9d9cd92d`. The historical `125` residual rows are exactly identical, but introducing the first valid four-plus-character prefix activates `PrefixAllGlobals` while B3/B6/B7 remain intentionally deferred. The scan therefore has `125` errors plus `5,206` prefix warnings across four new codes: functions `4,541`, literal hooks `187`, dynamic hook `1`, and variables `477`.
- Architecture contradiction: `4,723` warnings map to planned later batches and `478` are understood include-template/external-hook rows, but five genuine loader-scope sites across `$tax_file`, `$pt`, and `$hook` were omitted from the frozen B1 `44`-slot map. The requested zero-new-code/zero-new-finding gate cannot be asserted without either accepting a phase-aware intermediate scanner policy or redefining the package integration sequence and correcting the B1 global inventory. Details and machine state are in `docs/WPORG_PREFIX_MIGRATION_B2.md` and `docs/wporg-prefix-b2-scan-state.json`.
- Handoff: exact verdict `B2 COMPLETE — ARCHITECTURE REVIEW REQUIRED BEFORE B3`. No B3 work, push, merge, upload, tag, deployment, live sync, primary-worktree modification, installed add-on edit, staging/production change, production convergence, reviewer reply, or protected-stash mutation occurred.

## WordPress.org Reviewer Prefix Alignment — Phase B2.5

Date: 2026-08-26

- Current status: `verified`; exact verdict `B2.5 COMPLETE — SCANNER MODEL RECONCILED — READY FOR B3`. Readiness does not authorize B3, which was not started.
- Correction: implementation checkpoint `d1f9a0e441fc496793052132a6fe62b2b55a2337` prefixes all `38` globally executed vendor-profile template variables and the three genuine loader globals, including scanner-missed `$tag`, without a closure/template-loader redesign. The additive map is `41` semantic slots / `194` token sites / `57` eliminated Plugin Check rows. No Event Plan partial variable or external/core hook changed.
- Semantic record: original B1/B2 `4,696 -> 4,521 (-175)` and its `175`-entry B2 map remain immutable historical evidence. The corrected complete ledger is `4,737` pre-B2, `4,562` after original B2, and `4,521` after B2.5. Semantic-slot counts and scanner-row counts remain separate.
- Scanner architecture: `docs/wporg-prefix-scanner-inventory.json` records every remaining prefix finding. The migration gate passes with `REQUIRED_MIGRATION_B3=4,541`, `REQUIRED_MIGRATION_B7=182`, `SCANNER_FALSE_POSITIVE_METHOD_SCOPE=420`, `THIRD_PARTY_OR_CORE_CONTRACT=6`, `UNEXPECTED_PREFIX_FINDING=0`, `UNMAPPED_PREFIX_FINDING=0`, and no completed-batch residual. The historical `125` rows are exact and separate.
- Package/scan: clean package `/private/tmp/bvm-wporg-b2-5-final.74cU0p/build/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `26f550399fb43ae8628a61f1f33d1ebb4fcfdd792ee7c05f975d44a0385c7156`; strict JSON `/private/tmp/bvm-wporg-b2-5-final.74cU0p/plugin-check/plugin-check.strict.json`, SHA-256 `e09ed3d9f43dc0b8553888300a8aa1f8185b59b2c31ec8f7fd97645aeddcad62`, empty stderr, `5,274` total / `5,149` prefix / `420` variable rows.
- Lifecycle: the clean builder passed package integrity after staging `374` files, linting `271` PHP files, and syntax-checking `55` JavaScript files. The exact ZIP completed the seven-scenario disposable matrix plus standalone/supported-stack boot, activation/deactivation/reactivation, authenticated admin/public smoke, legacy-basename upgrade, interruption recovery, fixture preservation, and uninstall preservation without a fatal. Compatibility status `WARN` reflects captured dependency-era warnings/deprecations.
- Boundaries: only the isolated release worktree changed. The primary worktree, installed/live plugin, installed add-ons, protected stash, staging, production, and WordPress.org state remained untouched. No push, merge, upload, tag, deployment, reviewer reply, or B3 work occurred.

## WordPress.org Reviewer Prefix Alignment — Phase B3 Control Checkpoint

Date: 2026-08-26

- Current status: `in progress`; B3 is explicitly authorized, and the pre-implementation control checkpoint is green at the exact B2.5 source commit `634211d1d5bbd250fc13b19d02f39acd4a4bc96b` on `codex/wporg-reviewer-identity-alignment`.
- Frozen authority: `docs/wporg-prefix-b3-function-map.json` records exactly `4,521` unique `vms_* -> bvmgr_*` mappings, `4,541` declaration sites, `20` two-site duplicate families, `4,521` collision-free canonical targets, `4,541` authoritative scanner rows, public-extension classifications, known-add-on consumers, and the B2.5 dynamic-site inventory. The generator refuses to overwrite this map.
- Dependency and waves: `docs/wporg-prefix-b3-dependency-graph.json` and `docs/wporg-prefix-b3-waves.json` reconcile all functions exactly once across `11` ordered waves. The largest wave contains `640` functions. No duplicate family crosses a wave; W2 is the atomic union of `55` known-add-on-consumed functions and `12` public-extension function APIs with one overlap, for `66` functions / `68` declaration sites.
- Hybrid-state gate: `docs/wporg-prefix-b3-progress.json`, `scripts/generate-wporg-prefix-b3.php`, `scripts/lib/wporg-prefix-b3.php`, `scripts/lib/wporg-prefix-b3-waves.php`, and `tests/wporg-prefix-b3-guardrails.php` establish the phase-aware ratchet. At this checkpoint it passes with `0` migrated / `4,521` remaining and all `7,363` frozen exact function-literal sites accounted for. Retained `bvmgr_*` storage/global contracts that share a suffix with a planned function target are excluded unless the exact B2.5 function-literal site authorizes them.
- Verification: manifest generation and guardrails, B2 foundation, B2.5 runtime, five installed-add-on read-only contracts, migration state, scanner generation and guardrails, new B3 guardrails, plugin identity, runtime stubs, the `15`-case compatibility self-test, the `18`-case public-release pipeline self-test, PHP lint for the new control tooling, and `git diff --check` pass at the unchanged source baseline.
- Add-on boundary: installed add-ons remain read-only. W2 has not started; before it may start, all five add-ons require freshly hashed disposable copies, explicit provenance, canonical consumer cutover, and a harness that resolves both core and add-on roots only inside the disposable B3 workspace.
- Implementation state: no production PHP function has been renamed at this checkpoint; no wrapper or dual declaration exists; no package, ZIP, activation, deactivation, Plugin Check campaign, push, merge, upload, tag, deployment, reviewer reply, staging/production change, live-tree sync, installed-add-on edit, or protected-stash mutation occurred.

### B3 W1 activation/basename pilot

- Status: `verified`, pending its isolated commit; W1 migrated exactly `35` functions / `35` declaration sites with no duplicate family and no legacy wrapper.
- Ratchet: `35 / 4,521` functions migrated; `4,486` legacy functions and `4,506` declaration sites remain. The phase-aware manifest, frozen-map gate, and scanner inventory pass with zero stale/forward, unexpected, or unmapped rows.
- Focused verification: B2 foundation, plugin identity, activation public-page ownership, retired Square cleanup, runtime stubs, release compatibility self-tests, changed-PHP lint, release-pipeline self-tests, and diff checks pass.
- Disposable package/lifecycle: dev checkpoint SHA-256 `fbe187297234a4a3da309b21b6af910950fdac817f8335814d5fa52525f98225` passed package integrity and the VMS-only disposable matrix. Activation, deactivation, reactivation, repeated activation, upgrade, interruption resume, fixture preservation, and uninstall completed without a fatal; compatibility report SHA-256 `afd9ff4c0b1ce90a368564996b1677e169951582f50870795b3af306e6481098` is `WARN` only for captured deprecations.
- Strict packaged scan: `5,239` total rows = `125` historical errors + `5,114` phase-aware prefix warnings. Exact categories are B3 `4,506`, B7 `182`, method scope `420`, and external/core `6`; the gate reports exactly `35` monotonic removals and zero unexpected/unmapped findings. Normalized JSON SHA-256: `ee65a3b1a7c7ac1f428f867d920b489958a31629dcd472d88ea691fdd3d8b54f`.
- Boundary evidence: `docs/wporg-prefix-b3-untouched-tree-baseline.json` records reproducible hashes for the installed/live core plus all five installed add-ons after W1 verification and before W2 disposable preparation. No installed/live tree was edited; no push, merge, upload, tag, deployment, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W2 add-on/public-API atomic boundary

- Status: `verified` for the isolated W2 commit; W2 migrated exactly `66` functions / `68` declaration sites, including both two-site duplicate families, without a legacy wrapper or dual declaration.
- Ratchet: cumulative B3 progress is `101 / 4,521` functions and `103 / 4,541` declaration sites; `4,420` legacy functions / `4,438` B3 scanner rows remain. Phase-aware manifest, frozen-map, and scanner gates report no stale/forward, unexpected, unmapped, or completed-batch residual.
- Literal-boundary recovery: an initial exact-literal transformation changed six retained hook/option/source contracts. Its full tracked patch is preserved at `/private/tmp/bvm-wporg-b3-w2-failed-exact-literal.patch`, SHA-256 `f77ad24f05e9a699d63cd3bfa6fb8b20a4eaea1d4bbbc3d785ed8d06a184cedb`. The wave was restored to W1 and reapplied with function literals limited to proven executable-resolution contexts; all six retained contracts remain legacy by design.
- Add-on isolation: all five add-ons were source-snapshotted into `/private/tmp/bvm-wporg-b3-addon-isolation`; existing B2 copies were reused only after freshness verification, while Fill Dates and Refer a Friend received fresh copies. Deterministic patches and provenance are recorded per add-on. The B3 disposable-root harness passes `63` callable entries / `53` unique core functions across all five, and the installed-tree contract harness passes without editing installed callers.
- Focused verification: B3/manifest/scanner guardrails, B2/B2.5 regressions, migration-state, add-on, runtime-stub, plugin-identity, Event Plan, guided-tour, social-share, release-compatibility, public-release pipeline, changed-PHP lint, and diff checks pass.
- Package/lifecycle: `/private/tmp/bvm-wporg-b3-w2-checkpoint/backstage-venue-manager-1.2.0-public-release-dev.zip`, SHA-256 `c0217f9b2a1dbaf9184786dd05ffeeb90d4839801f4f0f14240aa186d78782fe`, passed package integrity and the full activation/deactivation/reactivation, upgrade, interruption, fixture, and uninstall lifecycle. Compatibility report SHA-256 `b581ef24cba9b8a5531c928652c7cd8639ecfec31fecf695843b51a90f0a85f9` is `WARN` only for captured dependency/PHP deprecations.
- Strict scan: `5,171` total = `125` historical errors + `5,046` warnings, categorized exactly as B3 `4,438`, B7 `182`, method scope `420`, and external/core `6`; no unexpected or unmapped row. Normalized JSON SHA-256: `8991509152b0ed4ee70fd063b036cfec5df21b3722b54aafffef8ff999276896`.
- Boundaries: installed/live core and add-on hashes remain equal to the frozen untouched-tree baseline. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W3 shared-runtime boundary

- Status: `verified` at `bf89b7f674af4a21e89ce8e1663036e0ae72790a`; W3 migrated exactly `457` functions / `460` declaration sites and all three two-site duplicate families without a legacy wrapper or dual declaration.
- Ratchet: cumulative B3 progress is `558 / 4,521` functions and `563 / 4,541` declaration sites; `3,963` legacy functions / `3,978` B3 scanner rows remain. Phase-aware manifest, frozen-map, and scanner gates report no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: `docs/wporg-prefix-b3-literal-decisions.json` records the exhaustive W3 split of `13` callable/source identities renamed and `30` hook, option, or other persistent contracts retained. Explicit test-site exceptions keep retained AJAX/hook and release-excluded safety identities unchanged.
- Focused verification: primary B3/manifest/scanner, B2/B2.5, installed/disposable add-on, identity, runtime-stub, compatibility, public-release pipeline, `51` applicable changed-test, changed-PHP lint, and diff gates pass. Environment-bound live-parity, historical-artifact, explicit-WordPress-root, and unrelated pre-existing expectation harnesses were excluded rather than altered.
- Package and strict scan: exact dev ZIP `/private/tmp/bvm-wporg-b3-w3-checkpoint/backstage-venue-manager-1.2.0-public-release-dev.zip`, SHA-256 `1b95ad6c84c794a2687e5a41823d834a393435348be7508845d3217eadebabe9`, passed integrity. Strict normalized JSON SHA-256 `81a831916c37518352de55bf415a160e95859cce9a34b6c51169070bb31acde0` contains historical `125` errors plus exactly B3 `3,978`, B7 `182`, method-scope `420`, and external/core `6` warnings, with no unexpected row.
- Lifecycle: an initial transient supported-stack HTTP-process failure (report SHA-256 `8b458da979cc8ff7d4c8c42d93aa5cf9f8c87efeabd6956187d4dbc3744c0b86`) did not reproduce in the isolated exact-scenario rerun (SHA-256 `777ce8dd47e13bd38d4bcdd7d148305b67748ef30eae41da9fd73e8afa1a5ac6`). The subsequent full exact-ZIP lifecycle completed all dependency scenarios, activation/deactivation/reactivation, upgrade, interruption recovery, fixture preservation, and uninstall preservation without a plugin fatal; report SHA-256 `4e5c6dd63ae690d9db7bd78ae476e640e616b5f095ab43449ef12a85233b7740`, with `WARN` limited to dependency/PHP deprecations and non-authoritative login probes.
- Boundaries: installed/live core and add-on trees remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W4 Event Plan boundary

- Status: `verified` at `c4c1eba453ec539309be696dda2d5cf52a88d8e0`; exactly `640` functions / `647` declaration sites and all seven two-site duplicate families migrated atomically without a legacy wrapper or dual declaration.
- Ratchet: cumulative progress is `1,198 / 4,521` functions and `1,210 / 4,541` sites; `3,323` legacy functions / `3,331` B3 scanner rows remain with no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: `7` executable callback identities moved while `32` hooks, admin-post actions, audit/provenance labels, and telemetry labels remain legacy. Nine W4 test-site exceptions preserve retained contract/global-slot assertions.
- Focused verification: review JSON, performance request ID, secondary-vendor bootstrap/inline/lazy-load/save, import file/upload/rows output, continuity/integrity output, authorization/request, changed-PHP lint, diff, and all primary B3/B2/B2.5/add-on/identity/runtime/release gates pass. Live-parity, unavailable historical-artifact, and explicit-WordPress-root harnesses remain excluded under the isolated-worktree boundary.
- Package/smoke: exact dev ZIP `/private/tmp/bvm-wporg-b3-w4-checkpoint/backstage-venue-manager-1.2.0-public-release-dev.zip`, SHA-256 `d025f12d156855992c5065e0a334a933f6b678a39b5047cb23704f188de938fb`, passed integrity and the supported WooCommerce/TEC/Event Tickets stack without a plugin fatal. Smoke report SHA-256: `800e0a99f658271c17b6e80acea538f5e798b4c9b2a4eb8ccd574b85018bf061`.
- Strict scan: `4,064` total = historical `125` errors plus B3 `3,331`, B7 `182`, method-scope `420`, and external/core `6` warnings; zero unexpected row. Strict JSON SHA-256: `03222e7c742c982abf93c8b34c39d3e5f19d4353b202735f7eb1dba38890ac46`.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W5 ticketing boundary

- Status: `verified` at `7482df66ab0b3b6995239fb06030f1d03e7b2750`; exactly `616` functions / `616` sites migrated without a duplicate family, legacy wrapper, or dual declaration.
- Ratchet: cumulative progress is `1,814 / 4,521` functions and `1,826 / 4,541` sites; `2,707` legacy functions / `2,715` B3 scanner rows remain with no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: two activation-bootstrap callables moved while `27` ticketing hook, action, audit, and telemetry/provenance contracts remain legacy.
- Focused verification: Phase B/V2 output-buffer ownership, output lifecycle, claims admin/mutation/request, verification mutation/request, disabled-ticket suppression, path/search/server-mount/text, authorization/input, changed-PHP lint, diff, and all primary B3/B2/B2.5/add-on/identity/runtime/release gates pass. Heredoc/nowdoc source fixtures are now phase-aware without rewriting historical fixture bodies.
- Checkpoint sequencing: the adjacent W5/W6 exact-ZIP package, strict scan, and supported-stack checkpoint is performed after W6; W5 remains an independent green commit.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W6 ticket-integrity boundary

- Status: `verified` at `1f244034e42ba1993c534e8f83e01d5cb1ae3d06`; exactly `533` functions / `533` sites migrated without a duplicate family, legacy wrapper, or dual declaration.
- Ratchet: cumulative progress is `2,347 / 4,521` functions and `2,359 / 4,541` sites; `2,174` legacy functions / `2,182` B3 scanner rows remain with no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: `13` executable callback sites moved while `36` database, filter, audit, and telemetry contracts remain legacy; two explicit W6 test fixtures preserve retained table/telemetry values.
- Focused verification: integrity scan lock/output, Square cleanup, State of the Range, admin output, mutation boundaries, runtime stubs, W5 continuity, changed-PHP lint, diff, and all primary B3/B2/B2.5/add-on/identity/release gates pass. Live-parity/projection, unavailable historical-artifact, and WordPress-root tests remain excluded.
- Package/smoke: exact dev ZIP `/private/tmp/bvm-wporg-b3-w6-checkpoint/backstage-venue-manager-1.2.0-public-release-dev.zip`, SHA-256 `7e674bec1564059d937dc37a86a2f11ecb444fe1cb42cbe123cfef529ce5a420`, passed integrity and supported-stack smoke without a plugin fatal. Smoke report SHA-256: `105a99df44bf03152ed5f153bc7b4b9152f60d8c71ad2e21d7f86ce7faad82d2`.
- Strict scan: `2,915` total = historical `125` errors plus B3 `2,182`, B7 `182`, method-scope `420`, and external/core `6` warnings; zero unexpected row. Strict JSON SHA-256: `a1f7e12e4a7076a11b788fd8546c73aa2b1a9126b32f8706f30b68239563fb29`.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W7 vendor/venue boundary

- Status: `verified` at `34fe7afcd06355b781896a17606e01308cb7069e`; exactly `419` functions / `419` sites migrated without a duplicate family, legacy wrapper, or dual declaration.
- Ratchet: cumulative progress is `2,766 / 4,521` functions and `2,778 / 4,541` sites; `1,755` legacy functions / `1,763` B3 scanner rows remain with no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: seven shell/array-map/activation callback sites moved while ten vendor filter/form contracts remain legacy.
- Focused verification: Vendor Application JS/CSS, confirmation output, ICS safe fetch, secondary-vendor continuity, Administrator output, authorization/input, schedule notice, runtime, changed-PHP lint, diff, and all primary phase gates pass. Isolation-incompatible live-parity/hash/history/root tests remain excluded.
- Checkpoint sequencing: W7 remains an independent green commit; W8 carries the adjacent exact-ZIP package/strict/runtime checkpoint.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W8 portal/calendar boundary

- Status: `verified` at `f53107c90574d4a2ae4d79a44395553f9fc228b1`; exactly `524` functions / `524` sites migrated without a duplicate family, legacy wrapper, or dual declaration.
- Ratchet: cumulative progress is `3,290 / 4,521` functions and `3,302 / 4,541` sites; `1,231` legacy functions / `1,239` B3 scanner rows remain with no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: two executable admin-page render callbacks moved while six admin-post/filter/AJAX/public-tab contracts remain legacy; the B2 vendor-profile sidebar collision fixture is also explicitly retained.
- Focused verification: ADD, availability, Event Plan Review, Plan Your Visit, uploads, public Event Sidebar, Staff/Vendor Portal, Vendor Application, Vendor Availability, B2.5 runtime, changed-PHP lint, diff, and all primary phase gates pass. Isolation-incompatible live-parity/hash/history/root tests remain excluded.
- Package/smoke: exact dev ZIP `/private/tmp/bvm-wporg-b3-w8-checkpoint/backstage-venue-manager-1.2.0-public-release-dev.zip`, SHA-256 `11c8904b93d22853e15bad764cb508d9a1cc35b649ea7e80444bf38202528311`, passed integrity and supported-stack smoke without a VMS fatal. Smoke report SHA-256: `7767ae6d87c3cc95d87c841d2e783fb6964900f65e63a6d19615935f13edb828`.
- Strict scan: `1,972` total = historical `125` errors plus B3 `1,239`, B7 `182`, method-scope `420`, and external/core `6` warnings; zero unexpected row. Strict JSON SHA-256: `94157bc176f3b6824e7d2a0ee8d192bd2ab3ac5e7653b549758371b75ecb7ac0`.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W9 staffing/schedule boundary

- Status: `verified` at `71c747f75dcb06940ac8c606c9de3894b909d3bf`; exactly `441` functions / `445` sites and all four W9 duplicate families migrated without a legacy wrapper or dual declaration.
- Ratchet: cumulative progress is `3,731 / 4,521` functions and `3,747 / 4,541` sites; `790` legacy functions / `794` B3 scanner rows remain with no stale/forward, unexpected, unmapped, split-duplicate, or completed-wave residual.
- Exact literals: ten admin-shell, REST, activation-bootstrap, and source-introspection identities moved while `43` database-table, cron-hook, and performance-telemetry contracts remain legacy.
- Focused verification: schedule output, Staff Certification notices, strict POST behavior, Administrator shell, authorization, changed-PHP lint, diff, and all primary phase gates pass. Isolation-incompatible live/shadow parity and explicit-WordPress-root tests remain excluded.
- Checkpoint sequencing: W9 remains an independent green commit; W11 carries the final exact-ZIP package, strict scan, and full lifecycle proof.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W10 admissions/email boundary

- Status: `verified` at `dd1dcf9ef85ea921586ac587d2b136137d61ff62`; exactly `428` functions / `428` sites migrated without a duplicate family, legacy wrapper, or dual declaration.
- Ratchet: cumulative progress is `4,159 / 4,521` functions and `4,175 / 4,541` sites; `362` legacy functions / `366` B3 scanner rows remain with no stale/forward, unexpected, unmapped, or completed-wave residual.
- Exact literals: eight array-map, activation-bootstrap, provider-registry, and admin-shell callback sites moved while `13` filter-hook, nonce-action, table, and option contracts remain legacy; three test-only collision/filter values are explicitly retained.
- Focused verification: admission request/REST, Event Details JSON-LD/schema, nonce, Pass Claims output, public Event Sidebar, mutation, changed-PHP lint, diff, and all primary phase gates pass. Isolation-incompatible live/shadow parity and unavailable historical-artifact tests remain excluded.
- Checkpoint sequencing: W10 remains an independent green commit; W11 carries the final exact-ZIP package, strict scan, full lifecycle, and closeout proof.
- Boundaries: installed/live core and add-ons remain unchanged. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

### B3 W11 terminal function-prefix boundary

- Status: `verified` at `11a88a88ed3b7879cf5b9e74fe6528b54df2afe5`; exactly `362` functions / `366` sites and all four W11 duplicate families migrated without a legacy wrapper or dual declaration.
- Terminal ratchet: cumulative progress is `4,521 / 4,521` functions and `4,541 / 4,541` sites; the prohibited semantic surface, legacy declaration count, B3 scanner-row count, stale/forward references, split duplicates, collisions, unexpected rows, unmapped rows, and completed-batch residuals are all zero.
- Exact literals: nine executable identities moved while `17` cron-hook, filter-hook, nonce/action, audit, and other later-batch contracts remain legacy. A release-excluded phase-aware fixture-seeder fallback preserves comparable historical-upgrade setup without adding any public-package wrapper.
- Verification: W11 admin/request/auth/goals/social/webhook/mutation suites, changed-PHP lint, diff checks, all primary B3/B2/B2.5/add-on/identity/runtime/release gates, disposable five-add-on resolution (`63` callable entries / `53` unique core functions), and final installed-tree hash verification pass.
- Final package/scan: exact dev ZIP `/private/tmp/bvm-wporg-b3-final2-checkpoint/backstage-venue-manager-1.2.0-public-release-dev.zip`, SHA-256 `57b932e5cc6507151a13df58758d2efa21ebb533e15b5d1a6a3a0e67b04537a0`, passed integrity with `374` staged files, `271` PHP lints, and `55` JavaScript syntax checks. Strict JSON `/private/tmp/bvm-wporg-b3-final2-checkpoint/plugin-check/plugin-check.strict.json`, SHA-256 `850ca9ed6b662e1d9ac0c98b296aef41ef87d1049da671c73aba1aab42de84a7`, contains `733` total = historical `125` + B3 `0` + B7 `182` + method scope `420` + external/core `6`; zero unexpected or unmapped row.
- Lifecycle: the exact final ZIP passed all seven dependency scenarios without a plugin fatal plus activation/deactivation/reactivation/repeated activation, historical-basename upgrade, interruption recovery, fixture preservation, and uninstall preservation. Report `/private/tmp/bvm-wporg-b3-final2-checkpoint/compatibility-full/backstage-venue-manager-1.2.0-release-compatibility.report.json`, SHA-256 `3ecd1471f9d8fb86c036648586f7c89f2ab4959cdf43ec4da641e6db5e3c44a6`, is `WARN` only for captured dependency/PHP deprecations; upgrade fixtures are present before/after with no preservation regressions. The superseded first-run false negative is retained at SHA-256 `b44e9ed43bdabc68f8cf8dc1684460ccda53340af054922fbb99da5e8a340d3c` and the corrected same-package rerun at `13ade858664db547b3455311901286dac0de6535021971630546d557ed174f11`.
- Boundaries: primary development HEAD remains `bf13cc94d5fd08b789cd1189cb50c1297227632b`; installed/live core and all five add-ons retain their frozen hashes; protected stash `WPORG-16D preserve unrelated sidebar+doc work` remains intact. No B4-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.

## WordPress.org Reviewer Prefix Alignment — Phase B4

- Status: B4 is verified complete in the isolated `codex/wporg-reviewer-identity-alignment` branch. B5-B8 have not begun.
- Frozen authority: `docs/wporg-prefix-b4-identifier-map.json` freezes 29 browser globals, 64 asset handles, 218 nonce-action families, 73 nonce fields, 14 query vars, four rewrite tags, seven rewrite rules, and three CLI paths. The prior evidence was corrected before runtime work: static nonce actions are 154 rather than 156, the browser inventory is 29 rather than the five documented uppercase globals, and Refer a Friend's `vms-admin` is a retained admin parent slug rather than an asset-handle consumer.
- Implementation: browser producers/consumers and handle graphs moved atomically with no alias or duplicate URL; nonce output is canonical while exact legacy actions/fields remain bounded inbound compatibility through native WordPress verifier calls; new rewrite targets and generated URLs are canonical while legacy query/tag inputs remain accepted; canonical `bvmgr` CLI paths and their transitional `vms` aliases resolve to the same implementation.
- Persistent boundary: the only new state is the guarded/versioned `bvmgr_prefix_b4_rewrite_version` marker for one soft rewrite flush. Existing `vms_rewrite_*` options, retained CPT/taxonomy values, the vendor-confirmation hash salt, B7 hooks/AJAX/REST contracts, and B5/B6 storage families remain unchanged.
- Compatibility/add-ons: `docs/wporg-prefix-b4-compatibility-retirement.json` contains exactly 308 reviewed entries (`294` temporary, `14` indefinite). Fresh disposable semantic scans of all five add-ons prove zero B4 consumers and require no patch; installed/live copies remain untouched.
- Checkpoints: `19c5e0a` browser/assets; `9fccd66` nonces; `ff9b753` query/rewrite/CLI/add-on proof; one final verification/documentation commit contains the native nonce-verifier visibility repair and exact closeout evidence.
- Package and strict scan: exact ZIP `/private/tmp/bvm-wporg-b4-9d031cc/backstage-venue-manager-1.2.0-public-release.zip`, SHA-256 `367d534ede2e8c6ced2c8576e3ca8c8044abacc662c1ad1266006203c554b660`, passed integrity with `375` staged files, `272` PHP lints, and `55` JavaScript checks. Strict JSON SHA-256 `738c37c327175e2cb44b985d4b1b439e8822440101cb928fbf00b195c08389dd` contains exactly `733` rows = `125` historical errors + B7 `182` + method scope `420` + external/core `6`, with no B3/B4 residual, unexpected, or unmapped finding.
- Lifecycle and browser QA: exact-ZIP lifecycle report SHA-256 `d35fd28e57d12a460e784726c713ec12f1e480b6ed350b11f4ba345cdadbf242` completed all seven dependency scenarios, activation/deactivation/reactivation, historical-basename upgrade, interruption recovery, fixture preservation, and uninstall preservation without a plugin fatal or preservation regression; warnings are limited to captured dependency/PHP deprecations. Disposable browser report SHA-256 `d0b30a273b5cb901d49de9595c67dc0448b5273cea0dcb640e0606152a814a78` proves canonical-only global initialization, canonical handle IDs, no duplicate asset URLs or console errors, and canonical/legacy pass-claim route parity.
- Boundaries: primary development HEAD remains `bf13cc94d5fd08b789cd1189cb50c1297227632b`; installed/live core and all five add-ons retain their frozen hashes; protected stash `WPORG-16D preserve unrelated sidebar+doc work` remains intact. No B5-B8 work, push, merge, upload, tag, deployment, live sync, staging/production change, WordPress.org action, reviewer reply, or protected-stash mutation occurred.
## Post-closeout feature note: published occurrence safety

- Scope: published Event Plan date/time lock, canonical reschedule/repair service, admin and WP-CLI preview/apply interfaces, append-only occurrence history, integrity checks, and Event-Day mismatch warning.
- Status: implemented and locally verified on isolated branch `fix/vms-event-reschedule-safety` from `bf13cc94d5fd08b789cd1189cb50c1297227632b`.
- Current source paths: `includes/core/event-reschedule.php`, `includes/core/cli/event-reschedule.php`, `includes/admin/event-reschedule.php`, `includes/admin/event-day-report.php`, the external Event-Day CSS/JS assets, and the Event Plan editor/bootstrap integrations.
- Focused test: `tests/event-reschedule-safety.php` (62 assertions, including every post-meta mutation API, authorization/nonce refusal, repair mode, invariants, ambiguity refusal, idempotence, and injected transaction rollback).
- Documentation: `docs/event-reschedule-safety.md` and the closed follow-up boundary in `docs/follow-up-completed-event-occurrence-lock-and-reschedule.md`.
- Runtime synchronization: corresponding logical changes were applied to the isolated sibling live tree while preserving its existing text-domain/add-on/bootstrap differences.
- External actions: none. No push, deployment, package, ZIP, tag, WordPress.org submission, staging/production data repair, email, refund, or protected-stash mutation occurred.
