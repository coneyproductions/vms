<?php
defined('WP_UNINSTALL_PLUGIN') || exit;

// Intentionally minimal.
// We do not delete data on uninstall in v0.1.0.
// When the importer starts storing logs/temp files, we can add cleanup here.
